# Plan Maestro de Implementación — Vantare Overlays v1.0

> **Versión**: 1.0.0 | **Fecha**: 2026-06-01 | **Estado**: Planificación  
> Plan integral que unifica arquitectura, features, frontend, backend e infraestructura.

---

## 0. Índice

1. [Visión General](#1-visión-general)
2. [Arquitectura del Sistema (Resumen)](#2-arquitectura-del-sistema-resumen)
3. [Catálogo de Features](#3-catálogo-de-features)
4. [Frontend](#4-frontend)
5. [Backend — Electron Main Process](#5-backend--electron-main-process)
6. [Telemetry Pipeline](#6-telemetry-pipeline)
7. [Multi-Sim Adapter Layer](#7-multi-sim-adapter-layer)
8. [IPC Bridge](#8-ipc-bridge)
9. [Sistema de Temas](#9-sistema-de-temas)
10. [Autenticación y Licencias](#10-autenticación-y-licencias)
11. [HTTP Server + OBS Integration](#11-http-server--obs-integration)
12. [Sprint Plan](#12-sprint-plan)
13. [Testing Strategy](#13-testing-strategy)
14. [Infrastructure & CI/CD](#14-infrastructure--cicd)
15. [Dependency Graph](#15-dependency-graph)
16. [Features Faltantes Detectadas](#16-features-faltantes-detectadas--auditoría-completa)

---

## 1. Visión General

### 1.1 Producto

Vantare Overlays es una aplicación de escritorio profesional para simracing que proporciona
overlays de telemetría en tiempo real para streaming (OBS Studio) y uso multi-monitor.
Compitiendo directamente con RaceLabs, se diferencia por su soporte multi-sim (iRacing + LMU + AC),
theme system completo, stream alerts nativos y modelo freemium.

### 1.2 MVP (v1.0)

| Componente | Alcance |
|---|---|
| **Sims** | iRacing, Le Mans Ultimate, Assetto Corsa |
| **Overlays** | Standings, Relative, Delta Bar, Stream Alerts |
| **Dashboard** | Tab dentro de Electron (Hub con 5 páginas) |
| **Temas** | 3 themes built-in + custom (CSS variables + Tailwind v4) |
| **Auth** | Supabase Auth + PostgreSQL + Feature Gating |
| **Distribución** | `.exe` via Electron Builder + Auto-update |
| **Streaming** | HTTP Server (localhost:3200) + SSE + OBS Browser Sources |

### 1.3 Métricas de Éxito

| Métrica | Target |
|---|---|
| Renderizado de overlay | < 16ms (60 FPS) |
| Memoria renderer | < 150MB |
| Arranque app | < 3 segundos |
| Cobertura E2E | > 70% |
| Tasa de errores en beta | < 1% |

---

## 2. Arquitectura del Sistema (Resumen)

### 2.1 Stack

| Capa | Tecnología |
|---|---|
| Desktop Shell | Electron + Electron Builder |
| Frontend | React 19 + TypeScript 5 + Tailwind CSS v4 |
| Build | Vite 6 (3 entry points: main, preload, renderer) |
| State | Zustand 5 |
| Monorepo | Turborepo + pnpm |
| Testing | Vitest + Playwright |
| Auth | Supabase (PostgreSQL + Auth + RLS) |
| Telemetry | N-API C++ addons (shared memory + mmap) |
| IPC | contextBridge (32 methods) |
| HTTP | http module (SSE + CORS) |
| Storage Local | electron-store |
| Auto Update | electron-updater + GitHub Releases |

### 2.2 Flujo de Datos (High-Level)

```
SIM (Shared Memory) → N-API C++ Adapter → Normalizer → UnifiedTelemetry
    → SimManager → IPC (contextBridge) → Zustand Store → React Overlay (render)
    → HTTP Server (SSE) → OBS Browser Source
```

### 2.3 Monorepo

```
vantare-overlays/
├── apps/desktop/         # @vantare/desktop - Electron app
├── packages/sim-core/    # @vantare/sim-core - Telemetry types, adapters, calculations
├── packages/ui-core/     # @vantare/ui-core - Shared React components, hooks, stores
├── packages/auth/        # @vantare/auth - Supabase client, license validation
├── packages/types/       # @vantare/types - Public type exports
├── shared/types/         # Bridge IPC types (VantareBridge, Settings, Profile, Theme)
├── test-data/            # Mock telemetry data
├── tools/                # Build/release scripts
└── docs/                 # Architecture documentation (8 docs, 14k lines)
```

---

## 3. Catálogo de Features

### 3.1 Features v1.0

```
FEATURES v1.0
├── F-001: Telemetry Pipeline (sim → unified → store)
├── F-002: Multi-Sim Adapter Layer (SimAdapter + Normalizer)
├── F-003: Overlay System (registry + Electron windows + HTTP pages)
├── F-004: Standings Overlay
├── F-005: Relative Overlay
├── F-006: Delta Bar Overlay
├── F-007: Stream Alerts Overlay
├── F-008: Hub Dashboard (settings, themes, profiles)
├── F-009: Theme System (3 built-in + custom + Tailwind v4 @theme)
├── F-010: Auth & Licensing (Supabase + Free/Pro/Ultimate tiers)
├── F-011: Profile Management (CRUD, import/export, switching)
├── F-012: HTTP Server + OBS Integration (SSE, Browser Sources)
├── F-013: Electron Windows Mode (multi-monitor overlays)
├── F-014: System Tray + Auto-start + Key Bindings
├── F-015: Auto-Updater (GitHub Releases)
├── F-016: Feature Gating (premium feature toggles)
├── F-017: Storybook UI Dev Environment (isolated overlay development)
├── F-018: Overlay Configuration System (Zod schemas + auto-generated settings UI)
├── F-019: Calculations Module (gaps, fuel, delta, relative, interpolation)
├── F-020: Debug Tools Overlay (FPS, pipeline latency, re-renders, memory)
├── F-021: Offline Mode (license caching, grace period, degraded features)
├── F-022: Logging System (electron-log, structured logging)
├── F-023: Telemetry Inspector (raw data viewer for debugging)
├── F-024: CSS Animation System (transitions, keyframes, alert animations)
├── F-025: Security Model (contextBridge isolation, CSP, Electron fuses, RLS)
├── F-026: Code Quality Pipeline (ESLint flat config, Prettier, Husky, lint-staged)
├── F-027: Git Strategy + Conventional Commits + Changesets
└── F-028: Preview Overlay (dev-only overlay showing real-time perf stats)
```

### 3.2 Features Future (post-v1)

```
FEATURES v1.1: Track Map, Input Telemetry, Fuel Calculator, Flags
FEATURES v1.2: Data Blocks (80+ modular widgets), Setup Comparison
FEATURES v1.3: Head to Head, Blind Spot, Pitlane Helper
FEATURES v1.4: Fastest Cars From Behind, Slow Car Ahead, Rejoin Indicator
FEATURES v2.0: Plugin System for community, AC Evo support
FEATURES v2.1: Heart Rate (HypeRate), Sim Calendars, Garage Cover
FEATURES v2.2: Corner Name Overlay, Telemetry Recorder/Replay
FEATURES v3.0: Mobile Companion App
```

---

## 4. Frontend

### 4.1 Tecnologías

| Layer | Tech |
|---|---|
| Framework | React 19 + TypeScript 5 |
| State | Zustand 5 (3 stores: telemetry, settings, profile) |
| CSS | Tailwind CSS v4 (browser runtime dev, compiled prod) |
| CSS Tokens | CSS Custom Properties (theme system) |
| Icons | Lucide React |
| Animations | CSS transitions + keyframes |

### 4.2 Component Tree

```
VantareApp
├── ThemeProvider (CSS variables en <html>)
├── AuthProvider (Supabase session)
├── Route: /hub
│   └── HubLayout
│       ├── Sidebar (navigation)
│       └── Pages:
│           ├── DashboardPage (sim status, quick settings)
│           ├── OverlaySettingsPage (per-overlay config)
│           ├── ThemesPage (theme selector + editor)
│           ├── ProfilesPage (CRUD profiles)
│           ├── AccountPage (login, license, plan)
│           └── CalendarPage (future)
├── Route: /overlay/standings
│   └── StandingsOverlay
│       ├── StandingsHeader (session name, time, flags)
│       └── StandingsTable
│           └── DriverRow[] (position, name, gap, pit, iRating)
├── Route: /overlay/relative
│   └── RelativeOverlay
│       ├── RelativeHeader
│       └── RelativeTable
│           └── RelativeRow[] (gap, driver, car, delta)
├── Route: /overlay/delta
│   └── DeltaBarOverlay
│       ├── DeltaBar (colored progress with prediction)
│       └── LapTimeDisplay (current, best, last)
├── Route: /overlay/stream-alerts
│   └── StreamAlertsOverlay
│       └── AlertQueue
│           └── AlertBanner[] (overtake, pole, fastest lap, PB)
```

### 4.3 Shared UI Components (packages/ui-core)

| Component | File | Props |
|---|---|---|
| **GlassPanel** | `components/GlassPanel.tsx` | children, className, opacity |
| **TimeDisplay** | `components/TimeDisplay.tsx` | timeMs, showHundredths |
| **PositionBadge** | `components/PositionBadge.tsx` | position, total? |
| **DeltaIndicator** | `components/DeltaIndicator.tsx` | delta (number) |
| **DriverRow** | (to build) | stands: all driver data |
| **RevLights** | (to build) | rpm, maxRpm, gear |
| **TyreWidget** | (to build) | tyre compound, wear |
| **FuelGauge** | (to build) | fuelLevel, fuelCapacity |

### 4.4 Hooks (packages/ui-core)

| Hook | Source | Returns |
|---|---|---|
| `useTelemetry()` | `hooks/useTelemetry.ts` | `Telemetry \| null` |
| `useSimState()` | `hooks/useSimState.ts` | `{ connected, simName }` |
| `useTheme()` | `hooks/useTheme.ts` | `{ themeId }` |
| `useProfile()` | (to build) | `{ profiles, active, setActive }` |

### 4.5 Zustand Stores

| Store | File | State |
|---|---|---|
| **telemetry-store** | `stores/telemetry-store.ts` | `{ telemetry, connected, session, warnings }` |
| **settings-store** | (to build) | `{ settings, update, reset }` |
| **profile-store** | (to build) | `{ profiles, activeId, activeProfile }` |

### 4.6 Routing

| URL | Component | Mode |
|---|---|---|
| `/hub` | HubLayout | Electron Hub Window |
| `/overlay/standings` | StandingsOverlay | Electron / HTTP |
| `/overlay/relative` | RelativeOverlay | Electron / HTTP |
| `/overlay/delta` | DeltaBarOverlay | Electron / HTTP |
| `/overlay/stream-alerts` | StreamAlertsOverlay | Electron / HTTP |

---

## 5. Backend — Electron Main Process

### 5.1 Entry Points

| File | Purpose |
|---|---|
| `src/main/index.ts` | App lifecycle (createMainWindow, createTray, quit) |
| `src/main/ipc/handlers.ts` | 26 IPC handlers (see Section 8) |
| `src/main/windows/overlay-manager.ts` | BrowserWindow creation/positioning per overlay |
| `src/main/server/http-server.ts` | HTTP server (SSE + overlay HTML pages) |
| `src/main/sim/sim-manager.ts` | Polling loop, sim auto-detection |
| `src/main/sim/adapters/base.ts` | SimAdapter interface |
| `src/main/auth/license.ts` | Supabase auth client |
| `src/main/updates/auto-updater.ts` | electron-updater |

### 5.2 Services — Implementation Order

```
1. SimManager (Sprint 2) — auto-detection polling
   ├── SimAdapter interface (base.ts)
   ├── IRacingAdapter (C++ N-API)
   │   ├── native/ (C++ mmap reader)
   │   ├── index.ts (adapter wrapper)
   │   └── mock.ts (mock data generator)
   ├── LMUAdapter (C++ N-API)
   │   ├── native/ (C++ shared memory reader)
   │   ├── index.ts
   │   └── mock.ts
   ├── ACAdapter (UDP reader)
   │   ├── index.ts
   │   └── mock.ts
   └── Normalizer (sim raw → UnifiedTelemetry)

2. OverlayManager (Sprint 3) — transparent BrowserWindows
   ├── 4 default overlays (standings, relative, delta, stream-alerts)
   ├── show/hide/position/size per overlay
   └── destroy cleanup

3. HttpServer (Sprint 3) — SSE + overlay HTML pages
   ├── GET /events → SSE stream (telemetry)
   ├── GET /overlays/:id → HTML page for OBS
   └── broadcastTelemetry(data)

4. AuthService (Sprint 6) — Supabase client
   ├── init(), login(), register(), logout()
   └── getLicenseStatus() → tier + validity

5. AutoUpdater (Sprint 7) — electron-updater
   └── checkForUpdates(), installUpdate()

6. IPC Handlers (Sprint 1) — electron-store CRUD
   ├── 26 handlers across 7 domains
   └── store.get()/store.set() persistence
```

---

## 6. Telemetry Pipeline

### 6.1 Pipeline Steps

```
Step 1: Sim writes to shared memory (iRacing/LMU) or sends UDP (AC)
Step 2: N-API C++ addon reads via mmap (zero-copy)
Step 3: Adapter calls callback with raw sim data
Step 4: Normalizer maps sim-specific fields → UnifiedTelemetry
Step 5: SimManager receives UnifiedTelemetry
Step 6: SimManager broadcasts via IPC (ipcMain → ipcRenderer)
Step 7: Preload script receives and forwards to renderer
Step 8: Zustand telemetryStore.setTelemetry(data)
Step 9: React overlays re-render via useTelemetry() selector
```

### 6.2 Unified Telemetry Type

```typescript
export interface Telemetry {
  sim: 'iracing' | 'lmu' | 'ac';
  timestamp: number;
  isConnected: boolean;
  player: PlayerData;      // speed, rpm, gear, position, fuel
  engine: EngineData;      // rpm, maxRpm, fuel levels, temps
  tyres: TyreData;         // fl, fr, rl, rr (temp, pressure, wear)
  lap: LapData;            // current, best, last, sector, delta
  session: SessionData;    // type, state, time, flags
  vehicles: VehicleData[]; // all drivers with position, gap, laps
  track: TrackData;        // name, length, sectors
  inputs: InputData;       // throttle, brake, clutch, steering
  weather: WeatherData;    // air, track temp, humidity, wind
}
```

### 6.3 Update Rate

| Rate | Constraint |
|---|---|
| 16 Hz (60ms) | Target update rate for overlays |
| 60 Hz | iRacing SDK native rate |
| 16 Hz | LMU shared memory refresh |
| 60 Hz | AC UDP packet rate |
| Zustand selector | Only re-render on changed fields |
| React.memo | All overlay components |

### 6.4 Mock System

Every adapter has a `mock.ts` that generates realistic telemetry data:
- Configurable scenarios: practice, qualifying, race
- Realistic driver names, lap times, gaps
- Simulates session progression
- Enables development without any sim installed
- Used by Storybook and unit tests

---

## 7. Multi-Sim Adapter Layer

### 7.1 SimAdapter Interface (already defined in `packages/sim-core/src/adapters/base.ts`)

```typescript
export interface SimAdapter {
  readonly name: SimType;
  readonly displayName: string;
  isAvailable(): boolean;
  connect(): Promise<void>;
  disconnect(): void;
  onTelemetry(callback: (data: RawTelemetry) => void): () => void;
  onSessionData(callback: (data: RawSession) => void): () => void;
  onConnectionState(callback: (state: ConnectionState) => void): () => void;
  getSimInfo(): SimInfo;
  destroy(): void;
}
```

### 7.2 SimManager

- Polls every 1000ms to check which sim is running
- Priority: iRacing > LMU > AC
- Auto-connects to detected sim
- Disconnects gracefully when sim closes
- Broadcasts sim state via IPC
- Retries on failure with exponential backoff

### 7.3 iRacing Adapter

- Reads shared memory via C++ N-API addon
- Uses `irsdk-node` or custom N-API binding
- Variable names: `Speed`, `RPM`, `Throttle`, `Brake`, `Clutch`, `Gear`, `FuelLevel`, `Lap`, `LapTime`, `LastLapTime`, `BestLapTime`, etc.
- Session data: `SessionType`, `SessionState`, `SessionTimeRemain`, `CarsInSession`
- Mock available for macOS/development

### 7.4 LMU Adapter

- Reads shared memory via C++ N-API addon (LMU shared memory interface v1.2+)
- Or via Python sidecar + FFI (alternative approach)
- Mock available for development

### 7.5 AC Adapter

- Reads UDP packets (port 9996 by default)
- Binary protocol defined by AC telemetry
- Mock available for development

### 7.6 Normalizer

One normalizer per sim, mapping sim-specific fields to UnifiedTelemetry:
- `normalizeIRacing(raw: IRacingTelemetry): Telemetry`
- `normalizeLMU(raw: LMUTelemetry): Telemetry`
- `normalizeAC(raw: ACTelemetry): Telemetry`
- Unit conversions: m/s → km/h, mi/h → km/h, F → C, PSI → kPa
- Default values for missing fields
- Vehicle data normalization (position, gaps, colors)

---

## 8. IPC Bridge

### 8.1 VantareBridge Interface (32 methods, already defined in `shared/types/bridge.ts`)

Grouped by domain:

| Domain | Methods | Direction |
|---|---|---|
| Telemetry | `onTelemetry`, `onSessionData`, `onSimState` | Main → Renderer (push) |
| Settings | `getSettings`, `saveSettings` | Renderer → Main → Renderer (invoke) |
| Profiles | `getProfiles`, `getActiveProfile`, `saveProfile`, `deleteProfile`, `setActiveProfile`, `importProfile`, `exportProfile` | Renderer → Main → Renderer |
| Auth | `login`, `register`, `logout`, `getSession`, `getLicenseStatus` | Renderer → Main → Supabase → Main → Renderer |
| Overlays | `getOverlayWindows`, `showOverlay`, `hideOverlay`, `setOverlayPosition`, `setOverlaySize` | Renderer → Main → OverlayManager |
| Sim | `getAvailableSims`, `getActiveSim` | Renderer → Main → SimManager → Main → Renderer |
| Themes | `getThemes`, `getActiveTheme`, `saveTheme`, `setActiveTheme`, `deleteTheme` | Renderer → Main → Renderer |
| System | `getVersion`, `checkForUpdates`, `installUpdate`, `openExternal`, `toggleOverlayVisibility`, `minimizeToTray` | Renderer → Main |

### 8.2 IPC Channels (34 total)

The full channel map is documented in `docs/IPC-BRIDGE.md` Section 7.

### 8.3 Preload Script (already implemented in `src/preload/index.ts`)

Exposes all 32 methods via `contextBridge.exposeInMainWorld('vantare', { ... })`.

### 8.4 Main Handlers (already stubbed in `src/main/ipc/handlers.ts`)

26 handlers registered via `ipcMain.handle()` and `ipcMain.on()`.

---

## 9. Sistema de Temas

### 9.1 Architecture

```
ThemeProvider (React Context)
  ├── Reads active theme from electron-store
  ├── Applies CSS variables on document.documentElement
  ├── Exposes useTheme() hook
  │
  ├── Tailwind CSS v4 @theme reads CSS variables
  │     └── Utility classes: bg-surface, text-primary, etc.
  │
  └── Components consume tokens via:
        ├── Tailwind classes
        ├── CSS var(--token-name)
        └── useTheme() hook (JS access)
```

### 9.2 Theme Token Reference (56 tokens)

Complete reference in `docs/THEME-SYSTEM.md` Section 3. Key categories:
- Colors (bg, text, accent, positive, negative, warning)
- Typography (size, weight, family, letter spacing)
- Spacing (xs, sm, md, lg, xl)
- Border radius (sm, md, lg, full)
- Shadows (sm, md, glass)
- Animations (speed, easing)
- Glassmorphism (bg, border, blur)

### 9.3 Built-in Themes

| Theme | File | Style |
|---|---|---|
| Dark | `dark.json` | Deep black/gray with cyan accent |
| Blood | `blood.json` | Dark red/black with red accent |
| Midnight | `midnight.json` | Deep blue/purple with blue accent |

### 9.4 Implementation

Key files to build:
- `themes/defaults.ts` — Built-in theme objects
- `themes/ThemeProvider.tsx` — React context, CSS variable application
- `themes/useTheme.ts` — Consumer hook
- `themes/ThemeEditor.tsx` — Custom theme creation UI
- `themes/ThemeSelector.tsx` — Theme switching UI

---

## 10. Autenticación y Licencias

### 10.1 Architecture

```
Electron Main Process
  └── Supabase Client (@supabase/supabase-js)
        ├── Auth: register, login, session, logout
        ├── Database: licenses, subscriptions (PostgreSQL)
        └── Edge Functions: license validation, feature gating

Renderer Process (React)
  └── AuthProvider
        ├── Login/Register forms
        ├── Session management
        └── Feature gating UI
```

### 10.2 Database Schema

```sql
-- Managed by Supabase Auth
-- auth.users (id, email, ...)

-- Custom tables
CREATE TABLE licenses (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  tier TEXT NOT NULL DEFAULT 'free',  -- 'free' | 'pro' | 'ultimate'
  hwid TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ,
  is_active BOOLEAN DEFAULT TRUE,
  UNIQUE(user_id)
);

CREATE TABLE subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  plan TEXT NOT NULL,
  status TEXT NOT NULL,
  started_at TIMESTAMPTZ,
  expires_at TIMESTAMPTZ
);
```

### 10.3 Tiers & Feature Gating

| Feature | Free | Pro ($4.99/mo) | Ultimate ($9.99/mo) |
|---|---|---|---|
| Standings, Relative, Delta Bar | ✅ | ✅ | ✅ |
| Stream Alerts | ✅ | ✅ | ✅ |
| Fuel Calculator | ❌ | ✅ | ✅ |
| Flags, Track Map | ❌ | ✅ | ✅ |
| Input Telemetry, Head to Head, Blind Spot | ❌ | ✅ | ✅ |
| Data Blocks (80+ widgets) | ❌ | ❌ | ✅ |
| Custom Themes | ❌ | ✅ | ✅ |
| iRacing | ✅ | ✅ | ✅ |
| LMU | ✅ | ✅ | ✅ |
| AC | ❌ | ✅ | ✅ |

### 10.4 Implementation

Key files:
- `packages/auth/src/index.ts` — Already stubbed (AuthService, AuthResult, LicenseStatus)
- `apps/desktop/src/main/auth/license.ts` — Supabase client, license validation
- `apps/desktop/src/renderer/hub/pages/Account.tsx` — Login/Register UI

---

## 11. HTTP Server + OBS Integration

### 11.1 Architecture

```
HttpServer (apps/desktop/src/main/server/http-server.ts)
  ├── GET /events → SSE stream (text/event-stream)
  │     └── Clients connect, receive telemetry in real-time
  │     └── CORS headers for local network access
  │
  ├── GET /overlays/:id → HTML page
  │     └── Inline HTML with <style> and <script>
  │     └── EventSource connects to /events
  │     └── Background: transparent
  │
  └── broadcastTelemetry(data: Telemetry)
        └── Writes to all connected SSE clients
```

### 11.2 OBS Integration

User workflow:
1. Open Vantare app
2. Go to Hub → enable HTTP server
3. Copy overlay URL: `http://localhost:3200/overlays/standings`
4. In OBS: Add Browser Source → paste URL
5. Set width/height, check "Transparent background"
6. Done — overlay appears on stream

### 11.3 Implementation

Already stubbed in `apps/desktop/src/main/server/http-server.ts`:
- `start()` — Creates http.Server, listens on port 3200
- `stop()` — Closes server and client connections
- `broadcastTelemetry()` — SSE write to all clients
- `renderOverlayPage()` — Generates HTML for each overlay

To implement fully:
- Serve actual Vite-built overlay bundles
- Handle port conflicts (auto fallback)
- Network access toggle (0.0.0.0 vs 127.0.0.1)
- Connection monitoring

---

## 12. Sprint Plan

### 12.1 Timeline

```
Semana  1-2  ████████  Sprint 1:  Foundation
Semana  3-4  ████████  Sprint 2:  Telemetry Pipeline
Semana  5-6  ████████  Sprint 3:  Core Overlays
Semana  7-8  ████████  Sprint 4:  More Overlays + Hub UI
Semana  9-10 ████████  Sprint 5:  Multi-Sim + LMU
Semana 11-12 ████████  Sprint 6:  Themes + Auth
Semana 13-14 ████████  Sprint 7:  Polish + Distribution
Semana 15-16 ████████  Sprint 8:  Testing + Release v1.0
```

### 12.2 Sprint Details

---

#### SPRINT 1: Foundation (Semanas 1-2)

**Objetivo**: App Electron funcional con Hello World, IPC, Tailwind.

| Task | File(s) | Status |
|---|---|---|
| T1.1 | Verificar monorepo (pnpm, turbo, package.json, tsconfig) | ✅ |
| T1.2 | Verificar Electron shell (main, preload, renderer, vite configs) | ✅ |
| T1.3 | Verificar IPC bridge (contextBridge, handlers) | ✅ |
| T1.4 | Verificar Tailwind CSS v4 (globals.css, @import "tailwindcss") | ✅ |
| T1.5 | Verificar Zustand store boilerplate | ✅ |
| T1.6 | Configurar ESLint + Prettier + Husky + lint-staged + commitlint | 🔲 |
| T1.7 | Configurar GitHub Actions CI (lint, typecheck, test) | 🔲 |
| T1.8 | Configurar Storybook (init + config) | 🔲 |
| T1.9 | Configurar electron-log wrapper (F-022) | 🔲 |
| T1.10 | Verificar Security Model (CSP headers, Electron fuses) (F-025) | 🔲 |
| T1.11 | Configurar Conventional Commits + Changesets (F-027) | 🔲 |
| T1.12 | `pnpm install` → verificar que todo compila | 🔲 |
| T1.13 | `pnpm dev` → verificar que Electron abre con React | 🔲 |

**Entregables**: App Electron arranca, muestra React con Tailwind, IPC funcional.

---

#### SPRINT 2: Telemetry Pipeline (Semanas 3-4)

**Objetivo**: Datos de telemetría fluyendo desde mock sim → store → React.

| Task | File(s) | Status |
|---|---|---|
| T2.1 | Finalizar types en `packages/sim-core/src/types/index.ts` (15 interfaces) | 🔲 |
| T2.2 | Implementar `SimAdapter` interface en `sim/adapters/base.ts` | 🔲 |
| T2.3 | Implementar `SimManager` (polling loop, auto-detect, reconnect) | 🔲 |
| T2.4 | Crear iRacing mock adapter (`sim/adapters/iracing/mock.ts`) | 🔲 |
| T2.5 | Implementar normalizer (`packages/sim-core/src/normalizer.ts`) | 🔲 |
| T2.6 | **F-019: Calculations Module** — gaps, fuel, delta, interpolation, track | 🔲 |
| T2.7 | Integrar SimManager con IPC handlers (`sim:available`, `sim:active`) | 🔲 |
| T2.8 | Conectar IPC telemetry push (main → renderer: `telemetry` channel) | 🔲 |
| T2.9 | Finalizar Zustand `telemetry-store` (selectors, performance) | 🔲 |
| T2.10 | Refinar `useTelemetry()` hook (error boundaries, loading state) | 🔲 |
| T2.11 | **F-020: Debug Tools Overlay** — FPS counter, latency display | 🔲 |
| T2.12 | **F-028: Preview Overlay** — dev-only overplay con perf stats | 🔲 |
| T2.13 | Test e2e: mock data aparece en un componente React simple | 🔲 |

**Entregables**: Telemetry mock → store → React component funcional.

---

#### SPRINT 3: Core Overlays (Semanas 5-6)

**Objetivo**: Standings + Relative overlays funcionando en Electron windows y OBS.

| Task | File(s) | Status |
|---|---|---|
| T3.1 | Implementar `GlassPanel` shared component | ✅ |
| T3.2 | Implementar `TimeDisplay`, `PositionBadge`, `DeltaIndicator` | ✅ |
| T3.3 | Crear Overlay Registry (JSON-based overlay registration) | 🔲 |
| T3.4 | Implementar `OverlayManager` (BrowserWindow creation per overlay) | 🔲 |
| T3.5 | Implementar `HttpServer` (SSE + overlay HTML pages) | 🔲 |
| T3.6 | **Standings Overlay** — Full implementation | 🔲 |
| T3.7 | **Relative Overlay** — Full implementation | 🔲 |
| T3.8 | IPC handlers para overlays (show, hide, position, size) | 🔲 |
| T3.9 | **F-017: Storybook stories** — Standings, Relative, Delta stories | 🔲 |
| T3.10 | **F-018: Overlay Config System** — Zod schemas para Standings + Relative | 🔲 |
| T3.11 | Test OBS Browser Source con ambos overlays | 🔲 |

**Entregables**: 2 overlays funcionando en Electron + OBS Browser Sources.

---

#### SPRINT 4: More Overlays + Hub UI (Semanas 7-8)

**Objetivo**: Delta Bar + Stream Alerts + Hub Dashboard funcional.

| Task | File(s) | Status |
|---|---|---|
| T4.1 | **Delta Bar Overlay** — Full implementation | 🔲 |
| T4.2 | **Stream Alerts Overlay** — Full implementation + alert engine | 🔲 |
| T4.3 | Implementar alert engine (queue, priority, display duration) | 🔲 |
| T4.4 | **F-024: CSS Animation System** — fade, slide, pulse, glow clases | 🔲 |
| T4.5 | **Hub Dashboard** — Layout con sidebar | 🔲 |
| T4.6 | Hub → DashboardPage (sim status, quick settings) | 🔲 |
| T4.7 | Hub → OverlaySettingsPage (per-overlay configs con Zod schemas) | 🔲 |
| T4.8 | Hub → ProfilesPage (CRUD profiles) | 🔲 |
| T4.9 | Settings persistence (electron-store integration complete) | 🔲 |
| T4.10 | **F-017: Storybook stories** — DeltaBar, StreamAlerts stories | 🔲 |

**Entregables**: 4 overlays + Hub Dashboard funcional.

---

#### SPRINT 5: Multi-Sim + LMU (Semanas 9-10)

**Objetivo**: iRacing real + LMU adapters funcionando con auto-detección.

| Task | File(s) | Status |
|---|---|---|
| T5.1 | Implementar iRacing C++ N-API adapter (shared memory reader) | 🔲 |
| T5.2 | Implementar iRacing normalizer (full field mapping) | 🔲 |
| T5.3 | Implementar LMU C++ N-API adapter (shared memory reader) | 🔲 |
| T5.4 | Implementar LMU normalizer (full field mapping) | 🔲 |
| T5.5 | Finalizar SimManager auto-detection (iRacing → LMU → AC priority) | 🔲 |
| T5.6 | Sim switching UI in Hub | 🔲 |
| T5.7 | AC adapter skeleton (UDP reader, for future) | 🔲 |
| T5.8 | **F-023: Telemetry Inspector** — Raw data viewer overlay | 🔲 |
| T5.9 | Test with real iRacing running | 🔲 |
| T5.10 | Test with real LMU running | 🔲 |

**Entregables**: iRacing + LMU reales conectan y envían telemetría.

---

#### SPRINT 6: Themes + Auth (Semanas 11-12)

**Objetivo**: Sistema de temas y autenticación Supabase completos.

| Task | File(s) | Status |
|---|---|---|
| T6.1 | Crear `ThemeProvider` (React Context + CSS variables) | 🔲 |
| T6.2 | Crear 3 built-in themes JSON (dark, blood, midnight) | 🔲 |
| T6.3 | Crear theme `defaults.ts` (built-in theme objects) | 🔲 |
| T6.4 | Implementar `useTheme()` hook + dynamic switching | 🔲 |
| T6.5 | Hub → ThemesPage (selector + editor UI) | 🔲 |
| T6.6 | Setup Supabase project (users, licenses, subscriptions tables) | 🔲 |
| T6.7 | Configurar RLS policies | 🔲 |
| T6.8 | Implementar `AuthService` (login, register, logout, session) | 🔲 |
| T6.9 | Implementar `LicenseValidator` (tier check, HWID binding) | 🔲 |
| T6.10 | Hub → AccountPage (login/register forms, license display) | 🔲 |
| T6.11 | Feature gating integration (disable premium features for free tier) | 🔲 |
| T6.12 | **F-021: Offline Mode** — license caching, TTL, grace period, degraded features | 🔲 |

**Entregables**: Temas funcionando + Auth completo con Supabase.

---

#### SPRINT 7: Polish + Distribution (Semanas 13-14)

**Objetivo**: App pulida y lista para distribución.

| Task | File(s) | Status |
|---|---|---|
| T7.1 | System Tray integration (minimize to tray, context menu) | 🔲 |
| T7.2 | Keyboard shortcuts (Alt+H toggle overlays) | 🔲 |
| T7.3 | Auto-start option (Windows startup registry) | 🔲 |
| T7.4 | Auto-updater (electron-updater + GitHub Releases) | 🔲 |
| T7.5 | OBS integration testing (all overlays, all themes) | 🔲 |
| T7.6 | Network access mode for dual-PC streaming | 🔲 |
| T7.7 | Port conflict auto-fallback | 🔲 |
| T7.8 | Browser Source optimization (CSS variables, no iframes) | 🔲 |
| T7.9 | Electron Builder config verification (nsis, icons, publish) | 🔲 |
| T7.10 | First `.exe` build and local installation test | 🔲 |

**Entregables**: App distribuible como `.exe` con auto-update.

---

#### SPRINT 8: Testing + Release v1.0 (Semanas 15-16)

**Objetivo**: v1.0 released.

| Task | File(s) | Status |
|---|---|---|
| T8.1 | E2E tests con Playwright (overlay rendering, IPC, auth) | 🔲 |
| T8.2 | Performance profiling (render time, memory, CPU) | 🔲 |
| T8.3 | Cross-sim testing (iRacing → LMU switching) | 🔲 |
| T8.4 | Bug fixes from sprint 7 testing | 🔲 |
| T8.5 | Documentation finalization | 🔲 |
| T8.6 | Landing page (marketing site) | 🔲 |
| T8.7 | Discord community setup | 🔲 |
| T8.8 | GitHub Release v1.0.0 with changelog | 🔲 |
| T8.9 | Create `.exe` installer with Electron Builder | 🔲 |
| T8.10 | Smoke test on clean Windows install | 🔲 |

**Entregables**: v1.0.0 released to GitHub + website.

---

## 13. Testing Strategy

### 13.1 Levels

| Level | Tool | Scope | Frequency |
|---|---|---|---|
| Unit | Vitest | Hooks, utilities, store logic, normalizers | Per commit |
| Component | React Testing Library | Overlay components, shared UI | Per commit |
| Integration | Vitest | IPC handlers, SimManager, HttpServer | Per PR |
| E2E | Playwright | Full flow: mock sim → overlay → OBS | Per sprint |
| Performance | Vitest benchmarking | Render time, FPS, memory | Per sprint |

### 13.2 Test Files (to create)

| Package | Tests |
|---|---|
| `@vantare/sim-core` | Unit: normalizer (each sim), calculations (gaps, fuel, delta) |
| `@vantare/ui-core` | Unit: hooks (useTelemetry, useTheme), Component: GlassPanel, TimeDisplay, PositionBadge, DeltaIndicator |
| `@vantare/auth` | Unit: feature gating, license validation |
| `@vantare/desktop` | Unit: IPC handlers, Integration: SimManager, HTTP Server, E2E: full overlay flow |

### 13.3 Mock Strategy

- All sim adapters have `mock.ts` generators
- Mock telemetry data in `test-data/` directory
- Mock VantareBridge for unit testing overlays
- Playwright route interception for E2E SSE mocking

---

## 14. Infrastructure & CI/CD

### 14.1 CI Pipeline (GitHub Actions)

```yaml
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps: [checkout, setup-pnpm, pnpm lint]
  typecheck:
    runs-on: ubuntu-latest
    steps: [checkout, setup-pnpm, pnpm typecheck]
  test:
    runs-on: ubuntu-latest
    steps: [checkout, setup-pnpm, pnpm test]
  test-e2e:
    runs-on: windows-latest
    steps: [checkout, setup-pnpm, pnpm test:e2e]
  build:
    runs-on: windows-latest
    steps: [checkout, setup-pnpm, pnpm build]
```

### 14.2 Release Pipeline

```yaml
on:
  push:
    tags: ['v*.*.*']
jobs:
  release:
    runs-on: windows-latest
    steps:
      - checkout
      - pnpm install
      - pnpm build
      - pnpm --filter=@vantare/desktop run package
      - Upload .exe to GitHub Release
      - Publish to electron-updater feed
```

### 14.3 Versioning

Semantic Versioning: `v{major}.{minor}.{patch}`

---

## 15. Dependency Graph

### 15.1 Feature Dependencies

```
Telem. Pipeline (F-001)
├── Calculations Module (F-019) ─── requires F-001 (needs unified types)
├── Multi-Sim Adapters (F-002) ─── requires F-019 (normalizer uses calculations)
│   └── SimManager
│       ├── IRacingAdapter + mock
│       ├── LMUAdapter + mock
│       └── ACAdapter + mock
│
├── Overlay System (F-003)
│   ├── Standings (F-004) ────── requires F-001, F-019
│   ├── Relative (F-005) ─────── requires F-001, F-019 (gap interpolation)
│   ├── Delta Bar (F-006) ────── requires F-001, F-019 (delta calc)
│   └── Stream Alerts (F-007) ── requires F-001, F-005 (gap detection for overtakes)
│
├── Overlay Config System (F-018) ─── requires F-003 (per-overlay configs)
├── Debug Tools (F-020) ─── requires F-001 (telemetry stream)
├── Telemetry Inspector (F-023) ─── requires F-001, F-002 (raw data viewer)
│
├── Theme System (F-009) ─── requires F-003 (to apply to overlays)
│   ├── CSS Animation System (F-024) ─── uses theme tokens
│   └── Preview Overlay (F-028) ─── requires F-009, F-020 (dev-only)
│
├── Auth (F-010)
│   ├── Feature Gating (F-016) ─── requires F-010
│   └── Offline Mode (F-021) ─── requires F-010 (license cache)
│
├── Security Model (F-025) ─── independent, architectural
├── Logging System (F-022) ─── independent, all components use it
│
├── HTTP Server (F-012) ─── requires F-003 (to serve overlay HTML)
├── Profiles (F-011) ─── requires F-003, F-009 (save overlay + theme configs)
│
├── Storybook (F-017) ─── requires F-003, F-009 (isolated component dev)
├── Code Quality (F-026) ─── independent (project-wide)
├── Git Strategy (F-027) ─── independent (project-wide)
│
└── Hub Dashboard (F-008) ─── requires all other features (unified UI)
```

### 15.2 Package Dependency Graph

```
@vantare/desktop
├── @vantare/sim-core (telemetry types + normalizer)
├── @vantare/ui-core (shared components + hooks)
├── @vantare/auth (Supabase client)
├── @vantare/types (public types)
└── shared/types (bridge IPC types)

@vantare/ui-core
├── @vantare/sim-core (Telemetry type)
└── react, zustand

@vantare/sim-core
└── zod (config schemas)

@vantare/auth
├── @supabase/supabase-js
└── zod

shared/types
├── @vantare/sim-core (Telemetry, Session, SimState, SimType, SimInfo)
└── @vantare/auth (AuthResult, LicenseStatus)
```

### 15.3 Build Order

```
1. @vantare/sim-core     (types + normalizer — no deps)
2. @vantare/ui-core      (components + hooks — depends on sim-core)
3. @vantare/auth         (Supabase client — no internal deps)
4. @vantare/types        (public types — no deps)
5. shared/types          (bridge types — depends on sim-core, auth)
6. @vantare/desktop      (Electron app — depends on all above)
```

---

## Apéndice A: Archivos ya Implementados

| File | Status | Lines |
|---|---|---|
| `package.json` (root) | ✅ | 30 |
| `pnpm-workspace.yaml` | ✅ | 4 |
| `turbo.json` | ✅ | 25 |
| `tsconfig.base.json` | ✅ | 32 |
| `.gitignore` | ✅ | 21 |
| `.prettierrc` | ✅ | 8 |
| `apps/desktop/package.json` | ✅ | 49 |
| `apps/desktop/tsconfig.json` | ✅ | 15 |
| `apps/desktop/vite.main.config.ts` | ✅ | 21 |
| `apps/desktop/vite.preload.config.ts` | ✅ | 21 |
| `apps/desktop/vite.renderer.config.ts` | ✅ | 21 |
| `apps/desktop/electron-builder.yml` | ✅ | 29 |
| `apps/desktop/src/main/index.ts` | ✅ | 80 |
| `apps/desktop/src/preload/index.ts` | ✅ | 46 |
| `apps/desktop/src/renderer/index.html` | ✅ | 12 |
| `apps/desktop/src/renderer/main.tsx` | ✅ | 16 |
| `apps/desktop/src/renderer/App.tsx` | ✅ | 24 |
| `apps/desktop/src/renderer/styles/globals.css` | ✅ | 15 |
| `apps/desktop/src/main/ipc/handlers.ts` | ✅ | 94 |
| `apps/desktop/src/main/windows/overlay-manager.ts` | ✅ | 109 |
| `apps/desktop/src/main/server/http-server.ts` | ✅ | 80 |
| `apps/desktop/src/main/sim/sim-manager.ts` | ✅ | 19 |
| `apps/desktop/src/main/sim/adapters/base.ts` | ✅ | 12 |
| `apps/desktop/src/main/auth/license.ts` | ✅ | 16 |
| `apps/desktop/src/main/updates/auto-updater.ts` | ✅ | 10 |
| `packages/sim-core/package.json` | ✅ | 44 |
| `packages/sim-core/src/types/index.ts` | ✅ | 100 |
| `packages/sim-core/src/normalizer.ts` | ✅ | 10 |
| `packages/ui-core/package.json` | ✅ | 45 |
| `packages/ui-core/src/index.ts` | ✅ | 8 |
| `packages/ui-core/src/stores/telemetry-store.ts` | ✅ | 13 |
| `packages/ui-core/src/hooks/useTelemetry.ts` | ✅ | 14 |
| `packages/ui-core/src/hooks/useSimState.ts` | ✅ | 16 |
| `packages/ui-core/src/hooks/useTheme.ts` | ✅ | 13 |
| `packages/ui-core/src/components/GlassPanel.tsx` | ✅ | 15 |
| `packages/ui-core/src/components/TimeDisplay.tsx` | ✅ | 16 |
| `packages/ui-core/src/components/PositionBadge.tsx` | ✅ | 12 |
| `packages/ui-core/src/components/DeltaIndicator.tsx` | ✅ | 11 |
| `packages/auth/package.json` | ✅ | 33 |
| `packages/auth/src/index.ts` | ✅ | 25 |
| `packages/types/package.json` | ✅ | 27 |
| `shared/types/bridge.ts` | ✅ | 81 |
| `shared/types/settings.ts` | ✅ | 20 |
| `shared/types/profile.ts` | ✅ | 17 |
| `shared/types/theme.ts` | ✅ | 9 |

## Apéndice B: Overlays v1 — Especificación Detallada

### Standings Overlay

```
Layout:
┌─────────────────────────────────────────────┐
│  [Flag] Session Name  ▲ Time Remaining       │  ← Header
│  ████████████████░░░░  Progress Bar          │
├────┬──────┬────────────────┬────────┬───────┤
│ P  │ Car  │ Driver / Team  │ Gap    │ iR   │
├────┼──────┼────────────────┼────────┼───────┤
│ P1 │ #23  │ 🏴 J. Smith   │ 0.000  │ 3521 │
│ P2 │ #12  │ 🏴 A. Brown   │ +1.234 │ 3150 │
│ P3 │ #99  │ ▶ Driver You  │ +2.567 │ ---- │  ← Highlighted
│ P4 │ #5   │ 🏴 M. Davis   │ +3.890 │ 2901 │
│ P5 │ #7   │ 🏴 R. Jones   │ +4.321 │ 2810 │
└────┴──────┴────────────────┴────────┴───────┘
```

Features:
- Positions, gaps, pit status indicators, iRating
- Player row highlighted
- Multi-class support (separators)
- Compact mode (3 levels)
- Column reordering
- Session progress bar
- Flag display
- Driver tags (custom colors + icons)

### Relative Overlay

Features:
- Cars near player only (configurable radius)
- Interpolated gaps
- Compact mode
- Pit status indicators
- Flag contour around widget

### Delta Bar Overlay

Features:
- Real-time delta to best lap
- Predicted lap time
- Sector deltas
- Color thresholds (green/yellow/red)
- Lap history

### Stream Alerts Overlay

Features:
- Overtake alerts (when you pass or are passed)
- Pole position achieved
- Fastest lap
- Personal best lap
- Alert queue with priority
- CSS transition animations
- Configurable display duration

---

## Apéndice C: Riesgos y Mitigaciones

| Riesgo | Prob | Impacto | Mitigación |
|---|---|---|---|
| iRacing SDK no disponible via npm | Media | Alto | Fork irsdk-node, mantener nuestro propio binding |
| LMU shared memory cambia de formato | Baja | Alto | Seguir changelog de LMU, test con cada update |
| Electron performance en overlays | Media | Medio | React.memo, Zustand selectors, CSS animations |
| Supabase free tier límites | Baja | Bajo | Empezar con free, escalar cuando tengamos usuarios |
| Electron Builder config compleja | Media | Medio | Usar config ya probada en irDashies |
| Multi-sim complexity | Media | Medio | SimAdapter interface bien definida, mocks para dev |

---

## 16. Features Faltantes Detectadas — Auditoría Completa

Esta sección documenta todas las features que fueron detectadas como faltantes
durante la auditoría de cross-reference entre los docs y el plan original.
Cada feature se vincula al doc fuente y al sprint donde debe implementarse.

---

### 16.1 F-017: Storybook UI Dev Environment

**Origen**: `docs/OVERLAY-DEV-GUIDE.md` Section 9
**Objetivo**: Entorno de desarrollo aislado para overlays sin necesidad de sim ni Electron.

**Comportamiento**:
- Cada overlay tiene un archivo `*.stories.tsx`
- Mock data providers inyectan datos de telemetría realistas
- Theme switching en Storybook para probar temas
- Componentes compartidos tienen sus propias stories

**Depende de**: F-003 (Overlay System), F-009 (Theme System)
**Sprint**: Sprint 3 (coincide con desarrollo de overlays)

**Stories requeridas**:
```
Standings.stories.tsx — con datos mock (race, qualifying, practice)
Relative.stories.tsx — con datos mock (3-5 cars near player)
DeltaBar.stories.tsx — con delta positivo, negativo, zero
StreamAlerts.stories.tsx — con cada tipo de alerta
GlassPanel.stories.tsx — variantes (opacity, width)
TimeDisplay.stories.tsx — formatos (mm:ss.fff, mm:ss)
PositionBadge.stories.tsx — posiciones (P1, P2, P3, ...)
DeltaIndicator.stories.tsx — positivo, negativo, zero
```

---

### 16.2 F-018: Overlay Configuration System

**Origen**: `docs/OVERLAY-DEV-GUIDE.md` Section 6
**Objetivo**: Sistema tipado de configuración por overlay con Zod schemas y UI auto-generada.

**Comportamiento**:
- Cada overlay exporta un Zod schema de configuración
- El Hub auto-genera la UI de settings a partir del schema
- Settings se persisten via IPC → electron-store
- Valores por defecto definidos en el schema
- Settings changes se propagan via IPC push al overlay

**Patrón de implementación**:
```typescript
// standings.config.ts
const standingsConfigSchema = z.object({
  maxDrivers: z.number().min(3).max(50).default(20),
  showIRating: z.boolean().default(true),
  nameFormat: z.enum(['full', 'first_initial', 'last_initial']).default('full'),
  compactMode: z.enum(['off', 'compact', 'ultra']).default('off'),
  columnOrder: z.array(z.string()).default(['pos', 'car', 'driver', 'gap', 'irating']),
  highlightPlayer: z.boolean().default(true),
  showFlags: z.boolean().default(true),
});
```

**Depende de**: F-003 (Overlay System)
**Sprint**: Sprint 4 (con Hub Settings)

---

### 16.3 F-019: Calculations Module

**Origen**: `docs/ARCHITECTURE.md` Section 5, `docs/SIM-ADAPTER-GUIDE.md` Section 7
**Objetivo**: Funciones puras de cálculo matemático compartidas entre overlays y normalizers.

**Módulos**:
```
packages/sim-core/src/calculations/
├── gaps.ts          — Cálculo de gaps (seconds, laps, interpolation)
├── fuel.ts          — Consumo, pit window, fuel-to-finish
├── delta.ts         — Delta a best lap, predicted lap
├── relative.ts      — Relative position/gap calculation
├── interpolation.ts — Linear, cubic spline gap interpolation
└── track.ts         — Track distance, sector boundaries
```

**Depende de**: F-001 (Telemetry Pipeline — necesita UnifiedTelemetry types)
**Sprint**: Sprint 2 (se desarrolla junto con el pipeline)

---

### 16.4 F-020: Debug Tools Overlay

**Origen**: `docs/ARCHITECTURE.md` Section 13.4
**Objetivo**: Overlay de desarrollo que muestra métricas de rendimiento en tiempo real.

**Métricas mostradas**:
- FPS del overlay (frames por segundo)
- Latencia del pipeline (shared memory → render)
- Número de re-renders por segundo
- Uso de memoria del proceso renderer
- Número de actualizaciones IPC recibidas
- Tasa de updates SSE (si en modo browser source)

**Depende de**: F-001 (Telemetry Pipeline)
**Sprint**: Sprint 2 (es útil durante todo el desarrollo)

---

### 16.5 F-021: Offline Mode

**Origen**: `docs/AUTH-GUIDE.md` Section 10, `docs/ARCHITECTURE.md` Section 12
**Objetivo**: La app funciona sin conexión a internet usando licencia cacheada.

**Comportamiento**:
- Última licencia válida se cachea en electron-store
- TTL de 24h para uso offline
- Al expirar TTL, se muestran features Free tier
- Reconexión automática y revalidación cuando hay internet
- Notificación al usuario del estado del modo offline

**Depende de**: F-010 (Auth & Licensing)
**Sprint**: Sprint 6 (junto con Auth)

---

### 16.6 F-022: Logging System

**Origen**: `docs/ARCHITECTURE.md` Section 8 (implícito en IPC error handling)
**Objetivo**: Sistema de logging estructurado para diagnóstico y debugging.

**Implementación**:
- `electron-log` para main process
- Custom logger wrapper que expone niveles (debug, info, warn, error)
- Los logs se escriben a archivo y se pueden exportar desde Advanced Settings
- Métricas de errores para diagnóstico

**Depende de**: Ninguna (independiente)
**Sprint**: Sprint 1 (infraestructura)

---

### 16.7 F-023: Telemetry Inspector

**Origen**: `docs/SIM-ADAPTER-GUIDE.md` Section 12, `docs/OVERLAY-DEV-GUIDE.md` (referencia a dev tools)
**Objetivo**: Visor de datos crudos de telemetría para debugging de adapters.

**Comportamiento**:
- Muestra todos los campos del UnifiedTelemetry en tiempo real
- Árbol JSON expandible
- Filtro por categoría (player, engine, tyres, vehicles, session)
- Indicador visual de campos que cambian (highlight en cada update)
- Útil para verificar que los normalizers funcionan correctamente

**Depende de**: F-001 (Telemetry Pipeline), F-002 (Multi-Sim Adapters)
**Sprint**: Sprint 5 (útil cuando se testean adapters reales)

---

### 16.8 F-024: CSS Animation System

**Origen**: `docs/THEME-SYSTEM.md` Section 1 (animation tokens), `docs/OVERLAY-DEV-GUIDE.md`
**Objetivo**: Sistema de animaciones CSS que usan tokens del tema y se aplican a overlays.

**Componentes**:
- Token de velocidad de animación (`--animation-speed: 200ms`)
- Easing tokens (`--easing-default`, `--easing-bounce`)
- Clases utilitarias de animación (fade-in, slide-up, slide-down)
- Animaciones específicas de alertas (pulse, glow, shake)
- Transiciones de entrada/salida para overlays

**Depende de**: F-009 (Theme System — comparte tokens)
**Sprint**: Sprint 4 (se usa en Stream Alerts)

---

### 16.9 F-025: Security Model

**Origen**: `docs/ARCHITECTURE.md` Section 14, `docs/IPC-BRIDGE.md` Section 8
**Objetivo**: Modelo de seguridad completo documentado e implementado.

**Componentes**:
- `contextBridge` isolation — renderer sin acceso a Node.js
- Electron fuses — `RunAsNode: false`, `OnlyLoadAppFromAsar: true`
- Content Security Policy (CSP) headers en HTML renderer
- Supabase Row Level Security (RLS) para todas las tablas
- Channel whitelisting en IPC (solo canales registrados)
- `electron-safe-storage` para JWT tokens (nunca en renderer)

**Depende de**: Ninguna (arquitectural, transversal)
**Sprint**: Sprint 1 (se configura desde el inicio)

---

### 16.10 F-026: Code Quality Pipeline

**Origen**: `docs/ARCHITECTURE.md` Section 8 (ESLint), `docs/TECH-DECISIONS.md` DEC-020
**Objetivo**: Pipeline automatizado de calidad de código.

**Herramientas**:
```json
{
  "eslint": "^9.0.0",        // Flat config (eslint.config.mjs)
  "prettier": "^3.4.0",      // Formatting consistente
  "husky": "^9.0.0",         // Git hooks (pre-commit, pre-push)
  "lint-staged": "^15.0.0",  // Lint solo archivos staged
  "commitlint": "^19.0.0"    // Validación de conventional commits
}
```

**Configuración**:
- ESLint con `@typescript-eslint` strict
- Prettier con reglas del proyecto (singleQuote, semi, trailingComma)
- pre-commit hook: `lint-staged` (eslint + prettier en staged files)
- pre-push hook: `pnpm typecheck && pnpm test`

**Depende de**: Ninguna
**Sprint**: Sprint 1 (T1.6)

---

### 16.11 F-027: Git Strategy + Conventional Commits + Changesets

**Origen**: `docs/TECH-DECISIONS.md` DEC-022, DEC-023, `docs/ROADMAP.md` TASK-008
**Objetivo**: Estrategia de branching, commits y releases con versionado semántico.

**Componentes**:
- **Trunk-based development**: `main` branch + feature branches
- **Conventional Commits**: `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`, `test:`
- **Changesets**: Documentar cambios para generar changelog automático
- **Release flow**: `changeset add` → PR merge → `changeset version` → `changeset publish`
- **Branch protection**: Require PR review, require CI passing

**Depende de**: Ninguna (procesos)
**Sprint**: Sprint 1 (T1.8 en ROADMAP), Sprint 8 (release real)

---

### 16.12 F-028: Preview Overlay (Dev Only)

**Origen**: `docs/ARCHITECTURE.md` Section 13.4 (monitoreo de performance)
**Objetivo**: Overlay de desarrollo que muestra estadísticas de rendimiento.

**Comportamiento**:
- Accesible via `?overlay=debug` en dev mode
- Muestra FPS, latencia pipeline, re-renders, memoria
- Indicador visual de "frame budget" (barra que se acerca al límite de 16ms)
- Solo disponible en `NODE_ENV=development`

**Depende de**: F-020 (Debug Tools), F-009 (Theme System — para estilos)
**Sprint**: Sprint 2 (se desarrolla con el pipeline)

---

### 16.13 Resumen de Features por Sprint

| Sprint | Features Implementadas |
|---|---|
| Sprint 1 | F-017 (Storybook init), F-022 (Logging), F-025 (Security), F-026 (Code Quality), F-027 (Git Strategy) |
| Sprint 2 | F-001 (Pipeline), F-019 (Calculations), F-020 (Debug Tools), F-028 (Preview Overlay) |
| Sprint 3 | F-003 (Overlay System), F-004 (Standings), F-005 (Relative), F-012 (HTTP), F-013 (Windows), F-017 (Stories) |
| Sprint 4 | F-006 (Delta), F-007 (Stream Alerts), F-008 (Hub), F-018 (Config), F-024 (Animations) |
| Sprint 5 | F-002 (Adapters reales), F-023 (Telemetry Inspector) |
| Sprint 6 | F-009 (Themes), F-010 (Auth), F-016 (Feature Gating), F-021 (Offline Mode) |
| Sprint 7 | F-011 (Profiles), F-014 (Tray + Keys), F-015 (Auto-update) |
| Sprint 8 | Testing + Release (todas las features) |
