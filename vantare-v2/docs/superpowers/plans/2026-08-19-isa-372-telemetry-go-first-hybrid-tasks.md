# Tasks (SDD · TASKS): Telemetría Go-first híbrida — primera ola (P0, F0, F1, F2, F3, F4, F5)

Fecha: 2026-08-19. Plan: `docs/superpowers/plans/2026-08-19-isa-372-telemetry-go-first-hybrid-plan.md`. Spec: `docs/superpowers/specs/2026-08-19-isa-372-telemetry-go-first-hybrid-spec.md`. Evidencia: `docs/research/telemetry-architecture-2026/` (rama `isa-371`).
Reglas para el worker (Codex gpt-5.6-sol medium u Opus): una tarea = un commit pequeño en la rama de la issue de su fase; test antes del cambio cuando la tarea lo indique; `go test ./internal/telemetry/... ./internal/app/... -count=1` y `pnpm --dir frontend test` (si toca TS) verdes antes de commit; `git diff --check`; no tocar archivos fuera del alcance de la tarea; no push/PR/promoción sin pedirlo; actualizar el handoff `docs/vantare-program/handoffs/telemetry-core.md` al cerrar cada fase. Las fases F6–F13 se descomponen en TASKS cuando arranquen (tras gate de F6).

Leyenda: `[ ]` pendiente · `[x]` hecho · **Gate** = aprobación de Isaac.

## P0 — Prerrequisitos (fuera del worktree de ISA-372; issues propias)

- [ ] **P0.1** Issue "Aislar cambio local de `internal/updater`": mover `internal/updater/{updater,version}.go` + tests del working tree principal a su rama; `go test ./internal/updater/...` verde. *(S)*
- [ ] **P0.2** Issue "LMU: `mDeltaBest` nativo como señal observada": committear el bloque DELTA-TELEMETRY (`internal/telemetry/drivers/lmu/{format,layout,fusion,batch_mapper,driver}.go` + tests, `derive/{delta,pipeline}.go` + tests, `catalog/{ids,catalog}.go` + tests, `core/reducer.go`, `projection/overlay/v1.go` + test, goldens `lmu-1.4-delta-overlay-v1.golden.json`, `overlay_v1.golden.json`, `canonical-integration-v1.golden.json`, `menu_track_pit_disconnect_v1.golden.json`, `docs/telemetry-core/*.md` tocados, `docs/current-plan.md` nota DELTA-TELEMETRY) en rama propia; gates `go test ./internal/telemetry/...` + replay parity. *(M)*
- [ ] **P0.3** Issue "Widget Delta: referencia seleccionable por instancia": committear DELTA-REFERENCES (`frontend/src/overlay/widget-types/delta/*`, `frontend/src/overlay/projection/*`, `frontend/src/overlay/core/telemetry-snapshot.ts`, inspector + tests, i18n en/es/it/pt, `docs/changelog.md`, nota DELTA-REFERENCES) detrás de P0.2; `pnpm --dir frontend test` + build verdes. *(M)*
- [ ] **P0.4** Promoción de P0.2/P0.3 a `nightly` (**Gate Isaac**). Alternativa P0.5: archivar en `archive/delta-native-local-2026-08` y regenerar goldens desde HEAD limpio.
- [ ] **P0.6** ADR 0008 Proposed → Accepted (**Gate Isaac**; bloquea F1).
- [ ] **P0.7** Verificar condición de arranque: `git status --short` limpio en `internal/telemetry/**`, `internal/app/telemetry*`, `frontend/src/overlay/**` en el worktree de F0.

## F0 — Red de seguridad (issue #5; rama `vantareapp/isa-XXX-tc-f0-red-de-seguridad`)

Convención: cada test nuevo que hoy falla se escribe completo y se marca `t.Skip("ISA-371 D-xx: activar en Fx")` / `test.skip(...)` para que CI siga verde; el PR demuestra en su descripción que, sin el Skip, falla por la causa esperada.

- [ ] **F0.1** `internal/app/telemetrytransport/payload_ceiling_test.go`: `TestOverlayPayloadStaysUnderTransportLimit` (1/20/44/104 vehículos con fixture sintético derivado de `projection/overlay/testdata`; aserción `len(payload) <= MaxPayloadBytes`) y `TestEngineerPayloadStaysUnderTransportLimit`. Documenta en el Skip: rechazo desde 103 / 85.
- [ ] **F0.2** `internal/app/telemetry_core_runtime_grid_test.go`: `TestRuntimePublishes104VehiclesEndToEnd` (runtime real con driver fake de 104 slots; tras N frames `lifecycle == running`, Overlay y Engineer entregados). Skip D-08/D-02.
- [ ] **F0.3** `internal/telemetry/core/commit_boundary_test.go`: `TestPostReducerStageFailureKeepsCursorsAligned` (inyectar error en `coord.Apply` y en `derive.Apply` mediante un wrapper del sink; el frame siguiente no devuelve `ErrStaleBatch`). Skip D-01.
- [ ] **F0.4** `internal/app/telemetry_core_runtime_consumer_test.go`: `TestPublishFailureIsNotTerminal` (hub con límite de 1 byte → contador +1, lifecycle `running`), `TestConsumerPanicDoesNotKillProcess` (Engineer fake que hace panic → recuperado y contado), `TestSlowEngineerDoesNotBlockDriverLoop` (Engineer con `sleep 50 ms`; intervalo entre lecturas del driver fake no se degrada > 20 %). Skip D-02 / #13 / Engineer síncrono.
- [ ] **F0.5** `internal/telemetry/drivers/lmu/identity_grace_test.go`: `TestSlotMissingOneFrameKeepsVehicleIdentity` (player y no-player; `ControlsHistory` y referencia de delta sobreviven), `TestSlotReusedByAnotherCarGetsNewIdentity` (`mID`/`driverName`/`class` distintos → identidad nueva), `TestSessionSignatureStaleDoesNotMergeSessions` (P→Q→R con firma `Stale`). Skip D-03 / escenario 17 / #11. Añadir comentario en `batch_mapper_test.go:326` indicando que fija el comportamiento **actual**.
- [ ] **F0.6** `internal/telemetry/core/session_coordinator_overflow_test.go`: `TestVehicleHistoryDoesNotOverflowInLongSession` (300 identidades distintas a lo largo de la sesión; sin `ErrVehicleHistoryOverflow`). Skip D-04.
- [ ] **F0.7** `internal/app/telemetry_core_runtime_watchdog_test.go`: `TestFrozenPipelineStopsReportingFresh` (sin frames 1 s con reloj inyectado → status publicado deja de ser `fresh`), `TestStatusErrorReachesSubscribersBeforeHubsClose`. Skip D-06.
- [ ] **F0.8** `frontend/src/telemetry-transport/store.freshness.test.ts` ("degrada a stale cuando `capturedAt` supera el umbral") y caso en `store.test.ts` ("acepta una revisión de status mayor no contigua"). `test.skip` D-06/D-07.
- [ ] **F0.9** Baseline versionada: `BenchmarkOverlayProjectionAndMarshal{1,20,44,104}` en `internal/telemetry/projection/overlay/bench_test.go` (+ `payload_bytes` como `b.ReportMetric`), ejecución `-count=5` guardada en `docs/telemetry-core/evidence/baseline-2026-08/` con `benchstat`; reutilizar fixtures de `docs/research/telemetry-architecture-2026/bench/` si encajan (sin build tag en el nuevo).
- [ ] **F0.10** `architecture_test.go`: añadir casos (no relajar) para que los tests nuevos respeten fronteras. Suite completa verde. Handoff actualizado con la lista de tests rojos por defecto.

Criterio de cierre F0: 13 tests nuevos existen, cada Skip cita su defecto, baseline publicada, CI verde.

## F1 — Fallo no terminal (issue #6; carril A; depende de ADR 0008 Accepted)

- [ ] **F1.1** Crear `internal/app/telemetry_core_runtime_failure_policy.go`: tipo `failureClass {programming, productOrPayload, consumer}` y función `classifyTelemetryError(err) failureClass` con tabla exhaustiva sobre los errores tipados de `core`, `telemetrytransport`, `projection`; test `TestFailureClassificationTable` (cada error → exactamente una clase; error desconocido → `programming` con log).
- [ ] **F1.2** Métricas: ampliar `TelemetryCoreMetrics` (`telemetry_core_runtime.go:70-83`) con `FramesDropped{reason}`, `PublishFailures{product}`, `ConsumerPanics{boundary}`, `FailStops`, `PayloadBytes{product}` (p50/p95/p99 vía histograma simple) y `LifecycleTransitions{from,to}`; exponer en el diagnóstico existente.
- [ ] **F1.3** `telemetry_core_runtime.go`: sustituir los `failStop` de `:652-668` (proyección/publicación) por `dropFrame(reason)` + status `degraded`; `failStop` solo para `programming`. Mover `publishProjections` **después** de que el sink haya terminado el commit (sin cambiar aún la orquestación interna; F3 la unifica). Activar `TestPublishFailureIsNotTerminal` y la parte "no muere" de `TestRuntimePublishes104VehiclesEndToEnd` (quitar Skip).
- [ ] **F1.4** `telemetrytransport/transport.go`: `PublishSnapshot` con payload > `MaxPayloadBytes` devuelve `ErrPayloadTooLarge` (sin cambio) pero el runtime lo clasifica `productOrPayload` → descartar + contar. `MaxPayloadBytes` se conserva como contrato; añadir test `TestPayloadLimitIsAContractNotAKillSwitch`.
- [ ] **F1.5** `recover()` por frontera de consumidor (Engineer `ConsumeObservation/ConsumeFact/ConsumeSourceStatus`, publicación Overlay/Strategy): wrapper `guardConsumer(boundary, fn)` que recupera, cuenta `ConsumerPanics{boundary}` y registra. Activar `TestConsumerPanicDoesNotKillProcess`.
- [ ] **F1.6** Publicar status `error` **antes** de cerrar hubs en `failStop` (`:846-865`); activar `TestStatusErrorReachesSubscribersBeforeHubsClose` (parte backend). `core/driver_manager.go:296-298`: `setTerminal` solo para `programming`.
- [ ] **F1.7** Flag `telemetryFailurePolicyV2` (default on en rama; off reproduce política anterior) en la configuración de diagnóstico; test `TestFailurePolicyFlagRestoresLegacyBehaviour`.
- [ ] **F1.8** `TestRuntimeRestartsAfterTransientFailure` (un `Start` posterior no devuelve `ErrClosed` tras un fallo `productOrPayload`), `TestDroppedFrameIncrementsCounterAndPublishesDegraded`.
- [ ] **F1.9** Sesión real LMU ≥ 60 min con 104 slots (o fixture de 104 si no hay grid): `telemetry_fail_stop_total` = 0; evidencia en `docs/telemetry-core/evidence/isa-XXX-f1-failure-policy.md`. Goldens v1 sin cambio. Handoff y Linear actualizados. (**Gate Isaac** para promover a nightly.)

## F2 — Watchdog y stale honesto (issue #7; carril A backend / carril C frontend)

- [ ] **F2.1** Backend: watchdog monotónico en `telemetry_core_runtime.go` ("sin frames desde hace X", X configurable, default 1 s) que degrada el status publicado a `stale`/`degraded`; métrica `telemetry_last_frame_age_ms` y `telemetry_watchdog_degradations_total`; activar `TestFrozenPipelineStopsReportingFresh`; añadir `TestWatchdogDegradesWithinOneSecond` con reloj inyectado.
- [ ] **F2.2** `telemetrytransport/transport.go:333`: `statusRevision` deja de exigir contigüidad (informativo); test de transporte que acepta revisión mayor no contigua.
- [ ] **F2.3** Frontend: `frontend/src/telemetry-transport/freshness-watchdog.ts` (+ `freshness-watchdog.test.ts`): edad local `capturedAt` vs `Date.now()` con reloj inyectable; `store.ts:91-114` acepta revisión mayor no contigua y no descarta snapshot; activar `store.freshness.test.ts` y el caso D-07 de `store.test.ts`; `store.watchdog.test.ts` ("un frame de hace 3 s se pinta como stale aunque el backend calle").
- [ ] **F2.4** `frontend/src/overlay/core/telemetry-adapter.ts`: propagar la degradación a los ViewModels sin inventar valores; diagnóstico `snapshot-stale-watchdog` análogo a `snapshot-resync` (`store.ts:176-183`).
- [ ] **F2.5** `TestReconnectRecoversWithoutRestart` (escenarios 1, 2, 13, 20 de 09 §6). Flag `telemetryWatchdogEnabled`. Playwright de widgets de estado sin regresión. Handoff.

## F3 — Transacción única `TelemetryEngine.Apply` (issue #8; carril A; 5–8 PRs; flag `telemetryEngineApply`)

- [ ] **F3.1** PR fachada: crear `internal/telemetry/engine/{engine.go,commit.go}` con `TelemetryEngine.Apply(ctx, frame) (EngineResult, error)` que **envuelve** la orquestación actual (mapper → reducer → coordinator → derive) sin cambiar semántica; `EngineResult{State, Facts, Cursor}`; runtime llama a `Apply` tras el flag; reglas de import en `architecture_test.go` para `engine/`. Goldens y replay parity idénticos.
- [ ] **F3.2** Prepare/commit en `drivers/lmu/batch_mapper.go:146-149`: el mapper produce un candidato y **no avanza cursor**; `Apply` confirma el cursor en el commit. `IsUnmappableFrame` migrado literalmente. Activar `TestPostReducerStageFailureKeepsCursorsAligned`; añadir `TestApplyIsAllOrNothing` (error inyectado en cada uno de los 5 pasos × cada error tipado → estado, cursor y facts **exactamente** como antes) y `TestApplyRetryDoesNotDivergeCursors`.
- [ ] **F3.3** `core/reducer.go:137,228-230`: deja de commitear por su cuenta (commit lo hace `Apply`); conserva los 9 errores tipados y los clones defensivos; borrar `Reducer.Run` (`:183-186`, sin llamador) con test previo que lo documente.
- [ ] **F3.4** `core/session_coordinator.go`: su lógica entra en `Apply` sin cursor ni commit propios; se conservan high-water de vueltas y presencia continua; crear `internal/telemetry/identity/eviction.go` (LRU acotada de identidades no vistas, default 512) y sustituir el tope 104 (`:27-29,296-304`); activar `TestVehicleHistoryDoesNotOverflowInLongSession`; añadir `TestIdentityEvictionKeepsBoundedMemory` (500 identidades).
- [ ] **F3.5** `internal/telemetry/identity/slot.go`: generaciones de slot con **ventana de gracia** (default 30 frames, configurable) y verificación `(sourceKey, driverName, class)` al reabrir; integrarlo en `batch_mapper.go:186-212`; "cambio de player" deja de forzar epoch (`pipeline.go:269-271`, `delta.go:146-149`); activar `TestSlotMissingOneFrameKeepsVehicleIdentity` y `TestSlotReusedByAnotherCarGetsNewIdentity`; añadir `TestGraceWindowExpiryReleasesSlot`, `TestPlayerReappearanceKeepsControlsHistoryAndDeltaReference`. Nota de changelog (cambio observable documentado).
- [ ] **F3.6** `schema/identity/stint.go` (`StintID`) + `batch_mapper.go:299-333` rellena `RunIdentity.Driver/Team` desde `DriverName`; `session_coordinator` emite `FactDriverChanged` y abre stint; `TestDriverChangeEmitsFactAndOpensNewStint` (D-05). Activar `TestSessionSignatureStaleDoesNotMergeSessions` con política declarada para firma no fresca.
- [ ] **F3.7** `derive/pipeline.go` y `derive/delta.go` dentro de `Apply` sobre candidatos; `AlgorithmVersion` conservado; `cloneSelfDeltaTracker` (`delta.go:95-105`) a copy-on-write; retirar el registro DAG que no ejecuta nada (`pipeline.go:82-113`) con test previo; benchmark `Apply` p99 < 1 ms @104.
- [ ] **F3.8** Shadow Go muestreado: `internal/app/telemetry_shadow.go` compara `Apply` (nuevo) contra la orquestación anterior sobre la **misma** observación (1 de cada N frames; sin efectos externos; auto-disable si > presupuesto); comparador semántico con tolerancias; métricas `telemetry_shadow_mismatches_total{field}`; test `TestShadowHasNoExternalEffects`.
- [ ] **F3.9** Métricas `telemetry_engine_sequence`, `telemetry_frames_rejected_total{stage,reason}`, `telemetry_slot_grace_reopen_total`, `telemetry_slot_generation_bumps_total`, `telemetry_identity_evicted_total`, `telemetry_apply_duration_us`. Replay parity por digest en dos pacings verde (`canonical_integration_test.go`); 60 min reales sin `ErrStaleBatch`; evidencia; handoff; changelog. (**Gate Isaac** para promover.)

## F4 — Borrado de lo desconectado y guard de wiring (issue #9; carril B)

- [ ] **F4.1** `internal/telemetry/wiring_guard_test.go`: `TestExportedSymbolsHaveProductionCaller` (AST de `internal/telemetry/**`: cada símbolo exportado referenciado al menos una vez fuera de `_test.go`; allow-list comentada: replay, diagnostics, contratos `Deprecated`). Debe fallar listando `NewFanout`, `NewAnalysisFull`, `NewEngineerFact`, `ServeWailsFacts`, `SSEFactsHandler`, `recording.NewCoordinator` (este último va a allow-list hasta F12 con comentario y fecha).
- [ ] **F4.2** Rescatar `FactResyncRequiredError` y la retención acotada de facts de `core/fanout.go` → `projection/engineer/fact_resync.go`; `TestFactResyncRequiredErrorPortedToEngineerPort`. Documentar el patrón cap-1+drop (`fanout.go:210-217`) en un comentario del puerto.
- [ ] **F4.3** Borrar `core/fanout.go` + `fanout_test.go` y `derive/fanout_integration_test.go` (o reescribirlo contra el engine). Guard verde.
- [ ] **F4.4** Borrar RFC 7396: `telemetrytransport/merge_patch.go` (+ test), `frontend/src/telemetry-transport/merge-patch.ts` (+ test) y la rama `delta` de `store.ts:163-172`; `PublishSnapshot(full, delta)` → `PublishSnapshot(full)`; ampliar `legacy-retirement.test.ts` (`mergePatch`, `applyMergePatch`).
- [ ] **F4.5** Borrar seal SHA-256 (`transport.go:85,99,113,752-785`) y el escáner de claves prohibidas del hot path si existe; sustituir por invariantes de construcción + goldens; benchmark de marshal antes/después.
- [ ] **F4.6** `projection/analysis`: retirar `NewAnalysisFull` del transporte; paquete marcado `// Deprecated: referencia para F12.b` o borrado si guard lo permite. Borrar `frontend/src/overlay/core/telemetry-store.ts` si cero importadores (ampliar `legacy-retirement.test.ts` con `telemetry-store`).
- [ ] **F4.7** Docs (PR separado): `docs/telemetry-core/README.md:12` (wiring productivo existe desde TC-07C), `docs/telemetry-core/runtime-fanout.md` → nota de retirada. `go build`, `go vet`, suite completa verdes. Handoff.

## F5 — Contrato TS generado (issue #10; carril B)

- [ ] **F5.1** `tools/telemetry-contract-gen/main.go`: genera TS (interfaces readonly, enums, constantes) desde structs con tags JSON de `internal/telemetry/projection/**` y `schema/envelope`; determinista; cabecera `// DO NOT EDIT — generated by tools/telemetry-contract-gen`.
- [ ] **F5.2** `TestGeneratedContractMatchesHandwritten`: el generador sobre los tipos v1 actuales produce estructuralmente lo que hoy declara `frontend/src/telemetry-transport/contracts.ts` (28 campos); toda divergencia se documenta como hallazgo antes de continuar.
- [ ] **F5.3** `frontend/src/generated/telemetry.ts` + `telemetry.generated.test.ts` (compila y satisface `projection-golden.test.ts`); `contracts.ts` reexporta del generado.
- [ ] **F5.4** `Taskfile.yml`: `telemetry:contract` (regenera) y `telemetry:contract:check` (regenera + `git diff --exit-code`); gate en CI; `legacy-retirement.test.ts` prohíbe edición manual de `frontend/src/generated/**` (regla por ruta). Handoff.

## F6–F13 — Se descomponen en TASKS al arrancar cada fase (tras gate de F6 para F8+)

Referencia de alcance: PLAN §Fases y `12-migration-plan.md` §3. Antes de abrir F6: F3 y F5 verdes, ADR 0008 Accepted, medición manual WebView2/OBS preparada (procedimiento en `docs/manual-verification.md`).
