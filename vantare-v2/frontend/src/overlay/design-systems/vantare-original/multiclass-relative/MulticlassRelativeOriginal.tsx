import type { WidgetRendererProps } from "../../../core/design-system-definition";
import type { MulticlassRelativeViewModel } from "../../../widget-types/multiclass-relative/multiclass-relative-view-model";
export function MulticlassRelativeOriginal({ model }: WidgetRendererProps<MulticlassRelativeViewModel>) { return <section data-widget-system="vantare-original" data-widget-renderer="multiclass-relative" data-status={model.status} className="vo-multiclass-relative"><header>RELATIVE <small>MULTICLASS</small></header>{model.rows.map((row) => <div className={row.isPlayer ? "is-player" : ""} data-class={row.classId} key={`${row.place}-${row.number}`}><b>{row.place}</b><i style={{ background: row.classColor }} /><span>{row.number}</span><strong>{row.name}</strong><em>{row.gap === undefined ? "—" : row.gap.toFixed(3)}</em></div>)}</section>; }
