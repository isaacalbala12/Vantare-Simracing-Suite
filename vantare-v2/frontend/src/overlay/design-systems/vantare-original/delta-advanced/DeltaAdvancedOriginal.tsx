import type { WidgetRendererProps } from "../../../core/design-system-definition";
import { deltaAdvancedReferenceLabel, type DeltaAdvancedViewModel } from "../../../widget-types/delta-advanced/delta-advanced-view-model";
function value(value: number | undefined): string { return value === undefined ? "—" : `${value >= 0 ? "+" : ""}${value.toFixed(3)}`; }
export function DeltaAdvancedOriginal({ model }: WidgetRendererProps<DeltaAdvancedViewModel>) { return <section data-widget-system="vantare-original" data-widget-renderer="delta-advanced" data-status={model.status} className="vo-delta-advanced"><header>DELTA ADVANCED</header><strong>{value(model.best)}</strong><div><span>{deltaAdvancedReferenceLabel(model.reference)}</span>{model.showUnavailableFields || model.sector !== undefined ? <span>SECTOR {value(model.sector)}</span> : null}{model.showUnavailableFields || model.theoretical !== undefined ? <span>THEORETICAL {value(model.theoretical)}</span> : null}{model.showUnavailableFields || model.last !== undefined ? <span>LAST {value(model.last)}</span> : null}</div></section>; }
