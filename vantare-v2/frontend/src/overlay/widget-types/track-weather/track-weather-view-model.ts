import type {TelemetrySnapshot} from "../../core/telemetry-snapshot";import type {WidgetViewModelBase} from "../../core/widget-definition";import type {TrackWeatherContent} from "./track-weather-definition";export type TrackWeatherViewModel=WidgetViewModelBase&{type:"track-weather";ambientC?:number;trackC?:number;rainPercent?:number;wetnessPercent?:number;windKph?:number;windDirection?:string;pressureHpa?:number;content:TrackWeatherContent};export function buildTrackWeatherViewModel(snapshot:TelemetrySnapshot,content:TrackWeatherContent):TrackWeatherViewModel{const environment=snapshot.environment;const hasData=environment&&Object.values(environment).some(value=>value!==undefined);return{type:"track-weather",status:hasData?(snapshot.status==="stale"?"stale":"ready"):"missing",...environment,content};}
