# Evidencia ISA-102 / TC-06B

Artefactos reproducibles del RecordingSink SQLite:

- `benchmark.csv`: cinco repeticiones con 64 vehículos.
- `packaging.csv`: probes CGO-free y hashes SHA-256.
- `dependencies-licenses.csv`: inventario exacto de runtime.
- `fault-matrix.csv`: límites de fallo observados.

Los ejecutables son artefactos locales y no se versionan. Los CSV registran
sus tamaños y digests. No contienen rutas personales, telemetría real ni PII.
