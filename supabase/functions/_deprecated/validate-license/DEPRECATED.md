# validate-license (archived)

Legacy implementation retained only for audit history. It is outside the deployable Functions surface and must never be deployed. The desktop app uses authenticated device-claim and entitlement-read RPCs instead.
