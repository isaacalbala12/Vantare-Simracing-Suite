import type { WidgetRendererProps } from "../../../core/design-system-definition";
import type { BroadcastTowerViewModel } from "../../../widget-types/broadcast-tower/broadcast-tower-view-model";
export function BroadcastTowerOriginal({ model }: WidgetRendererProps<BroadcastTowerViewModel>) { return <section data-widget-system="vantare-original" data-widget-renderer="broadcast-tower" data-status={model.status} data-driver-count={model.rows.length} data-visible-rows={Math.max(1, model.rows.length)} className="vo-broadcast-tower"><header><strong>STANDINGS</strong><span>{model.sessionLabel}</span></header><div className="vo-broadcast-tower-rows">{model.rows.map((row) => <div className={row.isPlayer ? "is-player" : ""} key={`${row.place}-${row.number}`}><b>{row.place}</b><b>{row.number}</b><span>{row.name}</span><small>{row.className}</small><em>{row.gap === undefined ? "—" : row.gap.toFixed(3)}</em></div>)}{model.rows.length === 0 ? <div className="vo-broadcast-empty-row" aria-label="No drivers"><b>—</b><b>—</b><span>—</span><small>—</small><em>—</em></div> : null}</div><footer>{model.lap === undefined ? "LAP —" : `LAP ${model.lap}`}</footer></section>; }
