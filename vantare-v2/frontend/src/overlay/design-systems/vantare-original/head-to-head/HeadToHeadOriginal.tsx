import type { WidgetRendererProps } from "../../../core/design-system-definition";
import type { HeadToHeadViewModel } from "../../../widget-types/head-to-head/head-to-head-view-model";
export function HeadToHeadOriginal({ model }: WidgetRendererProps<HeadToHeadViewModel>) { return <section data-widget-system="vantare-original" data-widget-renderer="head-to-head" data-status={model.status} className="vo-head-to-head"><header>HEAD TO HEAD <small>{model.target.toUpperCase()}</small></header>{model.player && model.opponent ? <div className="vo-head-to-head-entries"><div><b>{model.player.number}</b><span>{model.player.name}</span></div><strong>{model.gapSeconds === undefined ? "—" : `${model.gapSeconds.toFixed(3)}s`}</strong><div><b>{model.opponent.number}</b><span>{model.opponent.name}</span></div></div> : <p role="status">{model.statusMessage ?? "No rival available"}</p>}</section>; }
