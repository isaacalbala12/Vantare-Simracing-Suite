import type {WidgetRendererProps} from "../../../core/design-system-definition";import type {RaceScheduleViewModel} from "../../../widget-types/race-schedule/race-schedule-view-model";export function RaceScheduleCrystal({model}:WidgetRendererProps<RaceScheduleViewModel>){return <section data-widget-system="vantare-crystal" data-widget-renderer="race-schedule" data-status={model.status} className="vc-race-schedule"><header><b>RACE SCHEDULE</b><span>UPCOMING EVENTS</span></header><div className="vc-race-events">{model.events.length?model.events.map((event,index)=><article key={event.id} data-tier={index}><small>{event.license??"OFFICIAL"}</small><h3>{event.title}</h3><strong>{event.track}</strong><time>{event.startAt}</time><footer>{event.classes.join(" · ")} · {event.durationMinutes} MIN</footer></article>):<p>CALENDAR UNAVAILABLE</p>}</div></section>;}
