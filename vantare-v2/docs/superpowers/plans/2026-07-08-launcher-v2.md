# Launcher v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir el launcher actual (preliminar) en un launcher de perfiles profesional con discovery robusto, telemetría, feedback en vivo, auto-launch, hotkeys y diseño Vantare Crystal, sin romper la base existente.

**Architecture:** 9 cortes incrementales sobre el spec validado `docs/superpowers/specs/2026-07-08-launcher-v2-design.md`. Cada corte compila, pasa sus tests, y se verifica manualmente antes del siguiente. Backend Go con Wails v3 alpha.98. Frontend React 19 + TypeScript + Tailwind v4 + Motion. Store externo con `useSyncExternalStore` para feedback de cadena. Save atómico con `.bak` rotativo y sidecar `.failed`. Code review multi-revisor al final.

**Tech Stack:** Go 1.25+ · Wails v3 alpha.98 · React 19 + TypeScript + Tailwind v4 · Motion (Framer) v11+ · `golang.org/x/sys/windows/registry` (ya en go.mod) · Vitest · `prefers-reduced-motion` con fallback `matchMedia`.

**Spec de referencia:** `docs/superpowers/specs/2026-07-08-launcher-v2-design.md`. **Este plan NO duplica el spec; lo implementa.**

---

## Mapeo de archivos (resumen)

### Backend Go

| Archivo | Responsabilidad | Acción |
|---|---|---|
| `internal/app/settings_service.go` | Persistencia atómica, SchemaVersion, mutex, RecordProfileAttempt/Success | Modificar |
| `internal/app/settings_service_test.go` | Tests de migración, atomicidad, sidecar, mutex, telemetría | Modificar |
| `internal/app/launcher/chain.go` | ChainRunner con liveness probe, PIDs, active map, RecordProfileAttempt/Success | Modificar |
| `internal/app/launcher/chain_test.go` | Tests del chain runner | Modificar |
| `internal/app/launcher/discovery_windows.go` | Parser Steam correcto | Modificar |
| `internal/app/launcher/registry_windows.go` | Listado del registro para AddNonSteamGame | Crear |
| `internal/app/launcher/registry_windows_test.go` | Tests del registro | Crear |
| `internal/app/launcher/discovery_windows_test.go` | Tests del parser Steam | Modificar |
| `internal/app/launcher/autostart_windows.go` | HKCU\...\Run + parseo --launch= | Crear |
| `internal/app/launcher/autostart_windows_test.go` | Tests | Crear |
| `internal/app/launcher/hotkey_windows.go` | RegisterHotKey + WM_POWERBROADCAST + whitelist | Crear |
| `internal/app/launcher/hotkey_windows_test.go` | Tests | Crear |
| `cmd/vantare/main.go` | Handlers, eventos, hook de cierre de ventana | Modificar |
| `cmd/vantare/main_test.go` | Tests de handlers | Modificar |
| `internal/app/diagnostics_service.go` | Sanitización de nuevos campos | Modificar |
| `internal/app/diagnostics_service_test.go` | Tests de sanitización | Modificar |

### Frontend

| Archivo | Responsabilidad | Acción |
|---|---|---|
| `frontend/src/hub/launcher/launcher-state.ts` | Tipos, helpers (estimateChainDuration) | Modificar |
| `frontend/src/hub/launcher/launcher-state.test.ts` | Tests | Modificar |
| `frontend/src/hub/launcher/chain-store.ts` | Store externo con useSyncExternalStore | Crear |
| `frontend/src/hub/launcher/chain-store.test.ts` | Tests del store | Crear |
| `frontend/src/hub/launcher/chain-store.subscribe.test.ts` | Tests de suscripción selectiva | Crear |
| `frontend/src/hub/launcher/ProfileCard.tsx` | Se transforma con early return; UI telemetría | Reescribir |
| `frontend/src/hub/launcher/ProfileCard.test.tsx` | Tests | Modificar |
| `frontend/src/hub/launcher/ProfileCard.timeline.tsx` | Mini-timeline con Motion | Crear |
| `frontend/src/hub/launcher/ProfileCard.timeline.test.tsx` | Tests | Crear |
| `frontend/src/hub/launcher/ProfileCard.timeline.a11y.test.tsx` | Tests de a11y | Crear |
| `frontend/src/hub/launcher/ProfileEditor.tsx` | Side-panel con notes, hotkey, autostart | Reescribir |
| `frontend/src/hub/launcher/ProfileEditor.test.tsx` | Tests | Modificar |
| `frontend/src/hub/launcher/AppsPanel.tsx` | Modal single-select, panel de detalles con args | Modificar |
| `frontend/src/hub/launcher/AppsPanel.test.tsx` | Tests | Modificar |
| `frontend/src/hub/launcher/AddNonSteamGameModal.tsx` | Single-select, pre-lowercase, cap 200 | Crear |
| `frontend/src/hub/launcher/AddNonSteamGameModal.test.tsx` | Tests | Crear |
| `frontend/src/hub/components/LauncherDock.tsx` | useSyncExternalStore, lastResult badge, tooltip con count | Reescribir |
| `frontend/src/hub/components/LauncherDock.test.tsx` | Tests | Modificar |
| `frontend/src/hub/HubShell.tsx` | Monta el store/provider global | Modificar |
| `frontend/src/i18n/locales/{es,en,pt,it}.ts` | Nuevas keys | Modificar |
| `frontend/src/hub/lib/wails-runtime-mock.ts` | Mockea nuevos eventos | Modificar |
| `frontend/scripts/launcher-visual-compare.mjs` | Capturas Playwright | Crear |
| `frontend/package.json` | Añade `motion` | Modificar |

---

## Corte 0a — Structs + migración aditiva

### Task 0a.1: Structs Go con nuevos campos y SchemaVersion

**Files:**
- Modify: `internal/app/settings_service.go`
- Test: `internal/app/settings_service_test.go`

- [ ] **Step 1: Write the failing test for SchemaVersion**

En `internal/app/settings_service_test.go`, añadir al final del archivo:

```go
func TestDefaultAppSettingsHasSchemaVersion1(t *testing.T) {
    s := app.DefaultAppSettings()
    if s.SchemaVersion != 1 {
        t.Fatalf("expected SchemaVersion=1, got %d", s.SchemaVersion)
    }
}

func TestLoadMigratesLegacySettings(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    // v1 legacy: sin SchemaVersion ni campos nuevos.
    legacy := `{
        "deltaMode": "self",
        "cpuSampling": true,
        "hotkeys": {},
        "launchers": {}
    }`
    if err := os.WriteFile(path, []byte(legacy), 0o644); err != nil {
        t.Fatalf("write: %v", err)
    }
    svc := app.NewSettingsService(path, nil)
    if err := svc.Load(); err != nil {
        t.Fatalf("load: %v", err)
    }
    // SchemaVersion se setea a 1 en el Load si era 0.
    if svc.Settings().SchemaVersion != 1 {
        t.Errorf("expected SchemaVersion=1 after migration, got %d", svc.Settings().SchemaVersion)
    }
    // launcherApps y launcherProfiles se inicializan con defaults si no existían.
    if svc.Settings().LauncherApps == nil {
        t.Error("LauncherApps should be initialized")
    }
    if svc.Settings().LauncherProfiles == nil {
        t.Error("LauncherProfiles should be initialized")
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `go test ./internal/app/... -run "TestDefaultAppSettingsHasSchemaVersion1|TestLoadMigratesLegacySettings" -v`
Expected: FAIL with "SchemaVersion undefined" o similar.

- [ ] **Step 3: Add SchemaVersion to AppSettings and new fields**

En `internal/app/settings_service.go`:

1. Añadir `SchemaVersion int` al struct `AppSettings`:

```go
type AppSettings struct {
    SchemaVersion    int                              `json:"schemaVersion"`
    DeltaMode        string                           `json:"deltaMode"`
    CpuSampling      bool                             `json:"cpuSampling"`
    Hotkeys          map[string]string                `json:"hotkeys"`
    LauncherApps     map[string]LauncherAppEntry      `json:"launcherApps,omitempty"`
    LauncherProfiles []LaunchProfile                  `json:"launcherProfiles,omitempty"`
}
```

2. Modificar `LauncherAppEntry` para añadir `IsFavorite`:

```go
type LauncherAppEntry struct {
    ID             string              `json:"id"`
    DisplayName    string              `json:"displayName"`
    Abbreviation   string              `json:"abbreviation"`
    Category       LauncherAppCategory `json:"category"`
    LaunchMethod   string              `json:"launchMethod"`
    SteamAppID     uint32              `json:"steamAppId,omitempty"`
    ExecutablePath string              `json:"executablePath,omitempty"`
    Args           string              `json:"args,omitempty"`
    Detected       bool                `json:"detected"`
    GradientFrom   string              `json:"gradientFrom"`
    GradientTo     string              `json:"gradientTo"`
    IsFavorite     bool                `json:"isFavorite,omitempty"`
}
```

3. Modificar `LaunchProfile` para añadir los 7 campos nuevos:

```go
type LaunchProfile struct {
    ID                    string       `json:"id"`
    Name                  string       `json:"name"`
    Description           string       `json:"description,omitempty"`
    Steps                 []LaunchStep `json:"steps"`
    IsFavorite            bool         `json:"isFavorite,omitempty"`
    Notes                 string       `json:"notes,omitempty"`
    LaunchCount           int          `json:"launchCount,omitempty"`
    LastLaunchedAt        *time.Time   `json:"lastLaunchedAt,omitempty"`
    AvgChainDurationMs    int64        `json:"avgChainDurationMs,omitempty"`
    LaunchOnWindowsStartup bool        `json:"launchOnWindowsStartup,omitempty"`
    Hotkey                string       `json:"hotkey,omitempty"`
}
```

4. Modificar `DefaultAppSettings` para incluir `SchemaVersion: 1`:

```go
func DefaultAppSettings() *AppSettings {
    return &AppSettings{
        SchemaVersion:    1,
        DeltaMode:        "self",
        CpuSampling:      true,
        Hotkeys:          map[string]string{...},
        LauncherApps:     defaultLauncherApps(),
        LauncherProfiles: defaultLauncherProfiles(),
    }
}
```

5. Añadir `migrateSettings` y llamarla desde `Load`:

```go
func (s *SettingsService) migrateSettings(settings *AppSettings) {
    if settings.SchemaVersion == 0 {
        // v1 → v2: inicializar campos nuevos si están vacíos.
        settings.SchemaVersion = 1
        if settings.LauncherApps == nil {
            settings.LauncherApps = defaultLauncherApps()
        }
        if settings.LauncherProfiles == nil {
            settings.LauncherProfiles = defaultLauncherProfiles()
        }
    }
    // SchemaVersion > 1: en el futuro, switch por versión.
}
```

En `Load`, después del merge y antes del return, llamar a `s.migrateSettings(merged)`.

- [ ] **Step 4: Run the test to verify it passes**

Run: `go test ./internal/app/... -run "TestDefaultAppSettingsHasSchemaVersion1|TestLoadMigratesLegacySettings" -v`
Expected: PASS.

- [ ] **Step 5: Verify all existing tests still pass**

Run: `go test ./internal/app/... -v`
Expected: PASS (los tests existentes con fields antiguos deben seguir pasando).

- [ ] **Step 6: Commit**

```bash
git add internal/app/settings_service.go internal/app/settings_service_test.go
git commit -m "feat(launcher): add SchemaVersion and new model fields (cut 0a)"
```

### Task 0a.2: Tipos TS con nuevos campos y helpers

**Files:**
- Modify: `frontend/src/hub/launcher/launcher-state.ts`
- Test: `frontend/src/hub/launcher/launcher-state.test.ts`

- [ ] **Step 1: Write the failing test for estimateChainDuration**

En `frontend/src/hub/launcher/launcher-state.test.ts`, añadir:

```ts
import { estimateChainDuration } from "./launcher-state";

describe("estimateChainDuration", () => {
  it("returns 0 for empty profile", () => {
    const profile = { id: "p", name: "P", steps: [] };
    expect(estimateChainDuration(profile, [])).toBe(0);
  });

  it("sums delays + 2s per executable + 1s per steam-uri", () => {
    const profile = {
      id: "p",
      name: "P",
      steps: [
        { appId: "lmu", delay: 0 },
        { appId: "obs", delay: 2 },
      ],
    };
    const apps = [
      { id: "lmu", launchMethod: "steam-uri" as const, detected: true, displayName: "LMU", abbreviation: "LMU", category: "simulator" as const, gradientFrom: "", gradientTo: "" },
      { id: "obs", launchMethod: "executable" as const, detected: true, displayName: "OBS", abbreviation: "OBS", category: "streaming" as const, gradientFrom: "", gradientTo: "" },
    ];
    // 0 (lmu delay) + 1 (steam-uri) + 2 (obs delay) + 2 (executable) = 5
    expect(estimateChainDuration(profile, apps)).toBe(5000);
  });

  it("prefers avgChainDurationMs over estimate when present", () => {
    const profile = {
      id: "p",
      name: "P",
      steps: [{ appId: "lmu", delay: 0 }],
      avgChainDurationMs: 12345,
    };
    expect(estimateChainDuration(profile, [])).toBe(12345);
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm --dir frontend test launcher-state.test -v`
Expected: FAIL with "estimateChainDuration is not a function".

- [ ] **Step 3: Add new types and helper to launcher-state.ts**

En `frontend/src/hub/launcher/launcher-state.ts`, añadir los nuevos campos y el helper:

```ts
// Añadir al LauncherAppEntry:
isFavorite?: boolean;

// Añadir al LaunchProfile:
isFavorite?: boolean;
notes?: string;
launchCount?: number;
lastLaunchedAt?: string | null;
avgChainDurationMs?: number;
launchOnWindowsStartup?: boolean;
hotkey?: string;

// Nuevo helper al final del archivo:
export function estimateChainDuration(
  profile: LaunchProfile,
  apps: LauncherAppEntry[]
): number {
  if (profile.avgChainDurationMs) return profile.avgChainDurationMs;
  if (profile.steps.length === 0) return 0;
  let totalMs = 0;
  for (const step of profile.steps) {
    const app = apps.find((a) => a.id === step.appId);
    const launchOverheadMs = app?.launchMethod === "steam-uri" ? 1000 : 2000;
    totalMs += step.delay * 1000 + launchOverheadMs;
  }
  return totalMs;
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `pnpm --dir frontend test launcher-state.test -v`
Expected: PASS.

- [ ] **Step 5: Verify all existing tests still pass**

Run: `pnpm --dir frontend test`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/hub/launcher/launcher-state.ts frontend/src/hub/launcher/launcher-state.test.ts
git commit -m "feat(launcher): add new TS model fields and estimateChainDuration (cut 0a)"
```

---

## Corte 0b — Save atómico aislado

### Task 0b.1: Save atómico con rename + backup rotativo

**Files:**
- Modify: `internal/app/settings_service.go`
- Test: `internal/app/settings_service_test.go`

- [ ] **Step 1: Write the failing test for atomic save**

En `internal/app/settings_service_test.go`, añadir:

```go
func TestSaveIsAtomic(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    svc := app.NewSettingsService(path, nil)
    _ = svc.Load()
    settings := app.DefaultAppSettings()
    settings.SchemaVersion = 1
    if err := svc.Save(settings); err != nil {
        t.Fatalf("save: %v", err)
    }
    // El archivo principal existe.
    if _, err := os.Stat(path); err != nil {
        t.Errorf("main file should exist: %v", err)
    }
    // El .bak existe (rotación desde el save anterior, en este caso el primer save no tiene .bak previo).
    // Para forzar la rotación, hacer un segundo save.
    settings.DeltaMode = "relative"
    if err := svc.Save(settings); err != nil {
        t.Fatalf("second save: %v", err)
    }
    bakPath := path + ".bak"
    if _, err := os.Stat(bakPath); err != nil {
        t.Errorf(".bak should exist after second save: %v", err)
    }
    // El main no es un .tmp huérfano.
    tmpPath := path + ".tmp"
    if _, err := os.Stat(tmpPath); !os.IsNotExist(err) {
        t.Errorf(".tmp should not exist after successful save")
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `go test ./internal/app/... -run "TestSaveIsAtomic" -v`
Expected: FAIL (no hay .bak después del segundo save).

- [ ] **Step 3: Implement atomic save with backup rotation**

En `internal/app/settings_service.go`, modificar `Save`:

```go
func (s *SettingsService) Save(settings *AppSettings) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    return s.saveLocked(settings)
}

func (s *SettingsService) saveLocked(settings *AppSettings) error {
    if s.path == "" {
        return ErrSettingsPathEmpty
    }
    data, err := json.MarshalIndent(settings, "", "  ")
    if err != nil {
        return fmt.Errorf("marshal: %w", err)
    }
    tmpPath := s.path + ".tmp"
    bakPath := s.path + ".bak"

    // 1. Escribir a .tmp.
    if err := os.WriteFile(tmpPath, data, 0o644); err != nil {
        return fmt.Errorf("write tmp: %w", err)
    }

    // 2. Rotar .bak desde el destino actual (si existe).
    if _, err := os.Stat(s.path); err == nil {
        // Renombrar el destino a .bak (atómico en Windows si el destino no está bloqueado).
        _ = os.Rename(s.path, bakPath)
    }

    // 3. Renombrar .tmp al destino.
    if err := os.Rename(tmpPath, s.path); err != nil {
        return fmt.Errorf("rename tmp: %w", err)
    }
    s.settings = settings
    return nil
}
```

Añadir `sync.Mutex` al struct `SettingsService` y el import `sync`:

```go
type SettingsService struct {
    path     string
    mu       sync.Mutex
    settings *AppSettings
    // ... otros campos
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `go test ./internal/app/... -run "TestSaveIsAtomic" -v`
Expected: PASS.

- [ ] **Step 5: Verify all existing tests still pass**

Run: `go test ./internal/app/... -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/app/settings_service.go internal/app/settings_service_test.go
git commit -m "feat(launcher): atomic save with backup rotation (cut 0b)"
```

### Task 0b.2: Load tolerante a JSON corrupto con fallback a .bak

**Files:**
- Modify: `internal/app/settings_service.go`
- Test: `internal/app/settings_service_test.go`

- [ ] **Step 1: Write the failing tests**

En `internal/app/settings_service_test.go`, añadir:

```go
func TestLoadToleratesCorruptedJSON(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    // Archivo corrupto (truncado).
    if err := os.WriteFile(path, []byte("{ \"deltaMode\":"), 0o644); err != nil {
        t.Fatalf("write: %v", err)
    }
    // Backup válido.
    bakPath := path + ".bak"
    if err := os.WriteFile(bakPath, []byte(`{"schemaVersion": 1, "deltaMode": "self", "cpuSampling": true, "hotkeys": {}, "launcherApps": {}, "launcherProfiles": []}`), 0o644); err != nil {
        t.Fatalf("write bak: %v", err)
    }
    svc := app.NewSettingsService(path, nil)
    if err := svc.Load(); err != nil {
        t.Fatalf("load should not error on corruption, got: %v", err)
    }
    // El Load debe haber cargado del .bak.
    if svc.Settings().DeltaMode != "self" {
        t.Errorf("expected DeltaMode=self from .bak, got %s", svc.Settings().DeltaMode)
    }
}

func TestLoadFallsBackToDefaultsOnTotalCorruption(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    bakPath := path + ".bak"
    if err := os.WriteFile(path, []byte("garbage"), 0o644); err != nil {
        t.Fatalf("write: %v", err)
    }
    if err := os.WriteFile(bakPath, []byte("also garbage"), 0o644); err != nil {
        t.Fatalf("write bak: %v", err)
    }
    svc := app.NewSettingsService(path, nil)
    if err := svc.Load(); err != nil {
        t.Fatalf("load should not panic on total corruption, got: %v", err)
    }
    // Defaults completos.
    if svc.Settings().SchemaVersion != 1 {
        t.Errorf("expected SchemaVersion=1 from defaults, got %d", svc.Settings().SchemaVersion)
    }
    if svc.Settings().LauncherProfiles == nil {
        t.Error("expected default launcher profiles")
    }
}
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `go test ./internal/app/... -run "TestLoadToleratesCorruptedJSON|TestLoadFallsBackToDefaultsOnTotalCorruption" -v`
Expected: FAIL (Load actual devuelve error en JSON corrupto).

- [ ] **Step 3: Implement tolerant Load with .bak fallback**

En `internal/app/settings_service.go`, modificar `Load` para que sea tolerante:

```go
func (s *SettingsService) Load() error {
    s.mu.Lock()
    defer s.mu.Unlock()
    return s.loadLocked()
}

func (s *SettingsService) loadLocked() error {
    data, err := os.ReadFile(s.path)
    if err != nil {
        if os.IsNotExist(err) {
            // Archivo no existe: defaults.
            s.settings = DefaultAppSettings()
            s.migrateSettings(s.settings)
            return nil
        }
        return fmt.Errorf("read: %w", err)
    }
    var loaded AppSettings
    if err := json.Unmarshal(data, &loaded); err != nil {
        // Intentar .bak.
        bakData, bakErr := os.ReadFile(s.path + ".bak")
        if bakErr == nil {
            if err := json.Unmarshal(bakData, &loaded); err == nil {
                s.applyLoaded(&loaded)
                return nil
            }
        }
        // Total corruption: defaults.
        s.settings = DefaultAppSettings()
        s.migrateSettings(s.settings)
        return nil // no propagar el error de parseo (el Hub no debe crashear).
    }
    s.applyLoaded(&loaded)
    return nil
}

func (s *SettingsService) applyLoaded(loaded *AppSettings) {
    merged := DefaultAppSettings()
    // ... merge existente (línea ~128 del archivo actual) ...
    s.migrateSettings(merged)
    s.settings = merged
}
```

- [ ] **Step 4: Run the tests to verify they pass**

Run: `go test ./internal/app/... -run "TestLoadToleratesCorruptedJSON|TestLoadFallsBackToDefaultsOnTotalCorruption" -v`
Expected: PASS.

- [ ] **Step 5: Verify all existing tests still pass**

Run: `go test ./internal/app/... -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/app/settings_service.go internal/app/settings_service_test.go
git commit -m "feat(launcher): tolerant Load with .bak fallback (cut 0b)"
```

### Task 0b.3: Sidecar .failed con retry en próximo arranque

**Files:**
- Modify: `internal/app/settings_service.go`
- Test: `internal/app/settings_service_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestSaveFailedSidecarCreated(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    // Simular que el destino está bloqueado: crear un directorio con el mismo nombre.
    if err := os.Mkdir(path, 0o755); err != nil {
        t.Fatalf("mkdir: %v", err)
    }
    svc := app.NewSettingsService(path, nil)
    _ = svc.Load()
    settings := app.DefaultAppSettings()
    // Save fallará porque el destino es un directorio.
    _ = svc.Save(settings) // no debe panic, debe escribir sidecar.
    sidecarPath := path + ".failed"
    if _, err := os.Stat(sidecarPath); err != nil {
        t.Errorf(".failed sidecar should exist after save error, got: %v", err)
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `go test ./internal/app/... -run "TestSaveFailedSidecarCreated" -v`
Expected: FAIL.

- [ ] **Step 3: Implement sidecar on save failure**

En `saveLocked`, envolver el bloque crítico en una función que escribe sidecar si falla:

```go
func (s *SettingsService) saveLocked(settings *AppSettings) error {
    if s.path == "" {
        return ErrSettingsPathEmpty
    }
    data, err := json.MarshalIndent(settings, "", "  ")
    if err != nil {
        return fmt.Errorf("marshal: %w", err)
    }
    tmpPath := s.path + ".tmp"
    bakPath := s.path + ".bak"
    sidecarPath := s.path + ".failed"

    // Escribir sidecar ANTES del rename, para no perder el payload si el rename falla.
    if err := os.WriteFile(sidecarPath, data, 0o644); err != nil {
        return fmt.Errorf("write sidecar: %w", err)
    }

    if err := os.WriteFile(tmpPath, data, 0o644); err != nil {
        return fmt.Errorf("write tmp: %w", err)
    }
    if _, err := os.Stat(s.path); err == nil {
        _ = os.Rename(s.path, bakPath)
    }
    if err := os.Rename(tmpPath, s.path); err != nil {
        return fmt.Errorf("rename tmp: %w", err)
    }
    // Save exitoso: borrar el sidecar.
    _ = os.Remove(sidecarPath)
    s.settings = settings
    return nil
}
```

En `loadLocked`, después de aplicar el load, intentar aplicar el sidecar si existe (solo si el load fue exitoso):

```go
func (s *SettingsService) loadLocked() error {
    // ... (load existente) ...
    if s.settings != nil {
        // Si hay un sidecar .failed, intentar aplicarlo (sobrescribe con el último payload conocido).
        if sidecarData, err := os.ReadFile(s.path + ".failed"); err == nil {
            var sidecarSettings AppSettings
            if err := json.Unmarshal(sidecarData, &sidecarSettings); err == nil {
                s.settings = &sidecarSettings
                // Intentar persistir el sidecar ya aplicado.
                _ = s.saveLocked(s.settings)
            }
        }
    }
    return nil
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `go test ./internal/app/... -run "TestSaveFailedSidecarCreated" -v`
Expected: PASS.

- [ ] **Step 5: Verify all existing tests still pass**

Run: `go test ./internal/app/... -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/app/settings_service.go internal/app/settings_service_test.go
git commit -m "feat(launcher): sidecar .failed for save resilience (cut 0b)"
```

### Task 0b.4: ts/lint/build final del corte 0b

- [ ] **Step 1: Verify frontend still builds**

Run: `pnpm --dir frontend build`
Expected: success.

- [ ] **Step 2: Verify frontend lint passes**

Run: `pnpm --dir frontend lint`
Expected: 0 errors.

- [ ] **Step 3: Final verification**

Run: `go test ./...`
Expected: PASS.

Run: `pnpm --dir frontend test`
Expected: PASS.

---

## Corte 1 — Discovery fix + parser Steam

### Task 1.1: Refactor readUninstallEntries a helper compartido

**Files:**
- Modify: `internal/app/launcher/discovery_windows.go`
- Create: `internal/app/launcher/registry_common.go` (helper compartido)

- [ ] **Step 1: Extract readUninstallEntries to registry_common.go**

Crear `internal/app/launcher/registry_common.go`:

```go
package launcher

import "golang.org/x/sys/windows/registry"

// readUninstallEntries lee las entradas Uninstall de Windows (HKLM + WOW6432Node + HKCU).
// Devuelve una lista de candidatos para matching con el catálogo KnownApps.
// Esta función es compartida entre discovery (auto-detección) y registry (modal Add Non-Steam Game).
func readUninstallEntries() []discoveredCandidate {
    var out []discoveredCandidate
    roots := []struct {
        root registry.Key
        path string
    }{
        {registry.LOCAL_MACHINE, `SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall`},
        {registry.LOCAL_MACHINE, `SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall`},
        {registry.CURRENT_USER, `SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall`},
    }
    for _, r := range roots {
        k, err := registry.OpenKey(r.root, r.path, registry.READ|registry.WOW64_64KEY)
        if err != nil {
            continue
        }
        names, _ := k.ReadSubKeyNames(-1)
        k.Close()
        for _, name := range names {
            sub, err := registry.OpenKey(r.root, r.path+`\`+name, registry.READ|registry.WOW64_64KEY)
            if err != nil {
                continue
            }
            dn, _, _ := sub.GetStringValue("DisplayName")
            loc, _, _ := sub.GetStringValue("InstallLocation")
            sub.Close()
            if dn == "" {
                continue
            }
            out = append(out, discoveredCandidate{DisplayName: dn, InstallLocation: loc})
        }
    }
    return out
}
```

- [ ] **Step 2: Refactor discovery_windows.go to use the helper**

Reemplazar el cuerpo de `readUninstallRegistry` con una llamada al helper. Eliminar la función `readUninstallRegistry` local.

- [ ] **Step 3: Verify compile**

Run: `go build ./internal/app/launcher/...`
Expected: success.

- [ ] **Step 4: Commit**

```bash
git add internal/app/launcher/discovery_windows.go internal/app/launcher/registry_common.go
git commit -m "refactor(launcher): extract readUninstallEntries to shared helper"
```

### Task 1.2: Implementar parser de libraryfolders.vdf

**Files:**
- Modify: `internal/app/launcher/discovery_windows.go`
- Test: `internal/app/launcher/discovery_windows_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestParseLibraryFolders(t *testing.T) {
    vdf := `"libraryfolders"
    {
        "0"
        {
            "path"        "C:\\Program Files (x86)\\Steam"
            "label"       ""
            "contentid"   "123"
        }
        "1"
        {
            "path"        "D:\\SteamLibrary"
            "label"       ""
            "contentid"   "456"
        }
    }`
    paths := parseLibraryFoldersVDF(vdf)
    if len(paths) != 2 {
        t.Fatalf("expected 2 paths, got %d", len(paths))
    }
    if paths[0] != `C:\Program Files (x86)\Steam` {
        t.Errorf("path 0 wrong: %s", paths[0])
    }
    if paths[1] != `D:\SteamLibrary` {
        t.Errorf("path 1 wrong: %s", paths[1])
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `go test ./internal/app/launcher/... -run "TestParseLibraryFolders" -v`
Expected: FAIL.

- [ ] **Step 3: Implement parseLibraryFoldersVDF**

En `internal/app/launcher/discovery_windows.go`:

```go
import (
    "bufio"
    "regexp"
    "strings"
)

var vdfPathRe = regexp.MustCompile(`"path"\s+"([^"]+)"`)

func parseLibraryFoldersVDF(content string) []string {
    var paths []string
    scanner := bufio.NewScanner(strings.NewReader(content))
    for scanner.Scan() {
        line := scanner.Text()
        m := vdfPathRe.FindStringSubmatch(line)
        if m != nil {
            // Limpiar escapes de Steam: \\ -> \.
            p := strings.ReplaceAll(m[1], `\\\\`, `\`)
            paths = append(paths, p)
        }
    }
    return paths
}
```

Modificar `readSteamLibraryFolders` para usar el parser:

```go
func readSteamLibraryFolders() []string {
    steamPath := os.Getenv("steamPath")
    if steamPath == "" {
        steamPath = filepath.Join(os.Getenv("ProgramFiles(x86)"), "Steam")
    }
    vdfPath := filepath.Join(steamPath, "steamapps", "libraryfolders.vdf")
    data, err := os.ReadFile(vdfPath)
    if err != nil {
        return nil
    }
    return parseLibraryFoldersVDF(string(data))
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `go test ./internal/app/launcher/... -run "TestParseLibraryFolders" -v`
Expected: PASS.

- [ ] **Step 5: Verify all launcher tests pass**

Run: `go test ./internal/app/launcher/... -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/app/launcher/discovery_windows.go internal/app/launcher/discovery_windows_test.go
git commit -m "feat(launcher): implement libraryfolders.vdf parser (cut 1)"
```

### Task 1.3: Test de regresión: discovery encuentra OBS

**Files:**
- Test: `internal/app/launcher/discovery_windows_test.go`

- [ ] **Step 1: Write the test with mocked readUninstallEntries**

```go
func TestDiscoverFindsOBSWithMockedRegistry(t *testing.T) {
    // Sobrescribir readUninstallEntries vía un var de paquete para test.
    original := readUninstallEntries
    readUninstallEntries = func() []discoveredCandidate {
        return []discoveredCandidate{
            {DisplayName: "OBS Studio 30.0.0", InstallLocation: `C:\Program Files\obs-studio\bin\64bit`},
        }
    }
    defer func() { readUninstallEntries = original }()

    // Crear un fake obs64.exe en un temp dir.
    dir := t.TempDir()
    exePath := filepath.Join(dir, "obs64.exe")
    if err := os.WriteFile(exePath, []byte("fake"), 0o644); err != nil {
        t.Fatalf("write: %v", err)
    }

    // Llamar matchKnownApps directamente.
    candidates := readUninstallEntries()
    found := matchKnownApps(candidates)
    if _, ok := found["obs"]; !ok {
        t.Fatal("OBS should be detected")
    }
    // No verificamos ExecutablePath exacto porque findFirstExisting lee del InstallLocation real.
    // Lo que sí verificamos es que la entry existe.
}
```

- [ ] **Step 2: Convertir readUninstallEntries a var para inyección en tests**

En `registry_common.go`, cambiar la función a un `var`:

```go
var readUninstallEntries = func() []discoveredCandidate {
    var out []discoveredCandidate
    // ... cuerpo actual ...
    return out
}
```

- [ ] **Step 3: Run the test to verify it passes**

Run: `go test ./internal/app/launcher/... -run "TestDiscoverFindsOBSWithMockedRegistry" -v`
Expected: PASS (con el mock de readUninstallEntries).

- [ ] **Step 4: Commit**

```bash
git add internal/app/launcher/discovery_windows_test.go internal/app/launcher/registry_common.go
git commit -m "test(launcher): add OBS discovery regression test (cut 1)"
```

---

## Corte 2 — Menú Add Non-Steam Game (single-select)

### Task 2.1: Backend registry_windows.go

**Files:**
- Create: `internal/app/launcher/registry_windows.go`
- Test: `internal/app/launcher/registry_windows_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestRegistryListReturnsApps(t *testing.T) {
    original := readUninstallEntries
    readUninstallEntries = func() []discoveredCandidate {
        return []discoveredCandidate{
            {DisplayName: "Spotify", InstallLocation: "C:\\Users\\Test\\AppData\\Roaming\\Spotify"},
            {DisplayName: "Notepad", InstallLocation: ""}, // sin path
        }
    }
    defer func() { readUninstallEntries = original }()

    apps := listRegistryApps()
    var spotify *RegistryApp
    for i := range apps {
        if apps[i].DisplayName == "Spotify" {
            spotify = &apps[i]
        }
    }
    if spotify == nil {
        t.Fatal("Spotify should be in the list")
    }
    // Notepad sin path: se incluye igualmente (el frontend lo filtra si quiere).
    foundNotepad := false
    for _, a := range apps {
        if a.DisplayName == "Notepad" {
            foundNotepad = true
        }
    }
    if !foundNotepad {
        t.Error("Notepad should be included even without path")
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `go test ./internal/app/launcher/... -run "TestRegistryListReturnsApps" -v`
Expected: FAIL.

- [ ] **Step 3: Implement RegistryApp type and listRegistryApps**

Crear `internal/app/launcher/registry_windows.go`:

```go
package launcher

// RegistryApp es una entrada del registro de Windows (no catalogada en KnownApps).
type RegistryApp struct {
    ID             string `json:"id"`
    DisplayName    string `json:"displayName"`
    ExecutablePath string `json:"executablePath"`
}

// listRegistryApps lee el registro de Windows y devuelve apps instaladas (sin filtrar por KnownApps).
// Es la fuente del modal "Add Non-Steam Game".
func listRegistryApps() []RegistryApp {
    candidates := readUninstallEntries()
    out := make([]RegistryApp, 0, len(candidates))
    for i, c := range candidates {
        out = append(out, RegistryApp{
            ID:             fmt.Sprintf("registry-%d", i),
            DisplayName:    c.DisplayName,
            ExecutablePath: c.InstallLocation,
        })
    }
    return out
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `go test ./internal/app/launcher/... -run "TestRegistryListReturnsApps" -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/app/launcher/registry_windows.go internal/app/launcher/registry_windows_test.go
git commit -m "feat(launcher): registry_windows.go for AddNonSteamGame modal (cut 2)"
```

### Task 2.2: Handler de launcher:registry:list en main.go

**Files:**
- Modify: `cmd/vantare/main.go`
- Test: `cmd/vantare/main_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestHandleRegistryList(t *testing.T) {
    // El test verificará que el handler emite "launcher:registry:listed" con apps.
    // Setup mínimo: usar el harness de main_test.go existente.
    // ...
}
```

- [ ] **Step 2: Implementar el handler**

En `cmd/vantare/main.go`, añadir (siguiendo el patrón de los handlers existentes como `handleLauncherStatusGet`):

```go
func (a *App) handleRegistryList(event wailsEvent) {
    apps := launcher.ListRegistryApps()
    a.emit("launcher:registry:listed", map[string]any{"apps": apps})
}
```

Registrar el handler en el wiring (donde están los otros `launcher:*`):

```go
case "launcher:registry:list":
    a.handleRegistryList(event)
```

- [ ] **Step 3: Run the test to verify it passes**

Run: `go test ./cmd/vantare/... -run "TestHandleRegistryList" -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add cmd/vantare/main.go cmd/vantare/main_test.go
git commit -m "feat(launcher): handleRegistryList wiring (cut 2)"
```

### Task 2.3: AddNonSteamGameModal con single-select y cap 200

**Files:**
- Create: `frontend/src/hub/launcher/AddNonSteamGameModal.tsx`
- Create: `frontend/src/hub/launcher/AddNonSteamGameModal.test.tsx`

- [ ] **Step 1: Install motion**

Run: `pnpm --dir frontend add motion`
Expected: `motion` añadido a `package.json`.

- [ ] **Step 2: Write the failing tests**

```tsx
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { AddNonSteamGameModal } from "./AddNonSteamGameModal";

const mockApps = Array.from({ length: 250 }, (_, i) => ({
  id: `r-${i}`,
  displayName: `App ${i}`,
  executablePath: `C:\\Program Files\\App${i}\\app.exe`,
}));

describe("AddNonSteamGameModal", () => {
  it("renders cap of 200 matches when search is empty", async () => {
    // Mockear Events.On para devolver mockApps en launcher:registry:listed.
    (globalThis as any).window = {
      runtime: {
        Events: {
          Emit: jest.fn(),
          On: (_name: string, cb: (e: any) => void) => {
            setTimeout(() => cb({ data: { apps: mockApps } }), 0);
            return () => {};
          },
        },
      },
    };
    // ... usar la abstracción real de wails-runtime-mock.
  });
});
```

(Nota: el mock exacto del runtime Wails se ajusta a `frontend/src/hub/lib/wails-runtime-mock.ts`.)

- [ ] **Step 3: Implementar AddNonSteamGameModal**

```tsx
import { useEffect, useMemo, useState } from "react";
import { Events } from "@wailsio/runtime";

type RegistryApp = { id: string; displayName: string; executablePath: string };

type Props = {
  open: boolean;
  onClose: () => void;
  onAdd: (entry: { displayName: string; executablePath: string }) => void;
};

const CAP = 200;

export function AddNonSteamGameModal({ open, onClose, onAdd }: Props) {
  const [apps, setApps] = useState<RegistryApp[]>([]);
  const [loading, setLoading] = useState(false);
  const [search, setSearch] = useState("");
  const [selectedId, setSelectedId] = useState<string | null>(null);

  useEffect(() => {
    if (!open) return;
    setLoading(true);
    Events.Emit("launcher:registry:list");
    const off = Events.On("launcher:registry:listed", (e: any) => {
      setApps(e.data?.apps ?? []);
      setLoading(false);
    });
    return () => { off(); };
  }, [open]);

  // Pre-lowercase una vez.
  const lowercased = useMemo(
    () => apps.map((a) => ({ ...a, _lower: a.displayName.toLowerCase() + "|" + a.executablePath.toLowerCase() })),
    [apps]
  );

  const filtered = useMemo(() => {
    const q = search.toLowerCase().trim();
    if (!q) return lowercased;
    return lowercased.filter((a) => a._lower.includes(q));
  }, [lowercased, search]);

  const visible = filtered.slice(0, CAP);
  const moreCount = filtered.length - visible.length;

  const handleAdd = () => {
    const selected = apps.find((a) => a.id === selectedId);
    if (selected) {
      onAdd({ displayName: selected.displayName, executablePath: selected.executablePath });
      onClose();
    }
  };

  if (!open) return null;
  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm" data-testid="add-non-steam-modal">
      <div className="card-sleek rounded-xl w-[640px] max-h-[80vh] flex flex-col">
        <header className="p-4 border-b border-white/10">
          <h2 className="text-lg font-bold text-white">Añadir programa</h2>
        </header>
        <div className="p-3">
          <input
            type="search"
            value={search}
            onChange={(e) => setSearch(e.target.value)}
            placeholder="Buscar..."
            className="w-full rounded-md bg-black/40 border border-white/10 px-2 py-1 text-sm text-white"
            data-testid="add-non-steam-search"
          />
        </div>
        <div className="flex-1 overflow-y-auto" data-testid="add-non-steam-list">
          {loading ? (
            <div className="p-4 text-xs text-vantare-textDim">Cargando...</div>
          ) : (
            <table className="w-full text-sm">
              <tbody>
                {visible.map((a) => (
                  <tr
                    key={a.id}
                    onClick={() => setSelectedId(a.id)}
                    className={selectedId === a.id ? "bg-accent/20" : "hover:bg-white/5 cursor-pointer"}
                    data-testid={`add-non-steam-row-${a.id}`}
                  >
                    <td className="p-2">{a.displayName}</td>
                    <td className="p-2 text-xs text-vantare-textDim">{a.executablePath}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          )}
          {moreCount > 0 && (
            <div className="p-3 text-xs text-vantare-textMuted border-t border-white/5">
              Refina la búsqueda para ver más ({moreCount} resultados más).
            </div>
          )}
        </div>
        <footer className="p-3 border-t border-white/10 flex justify-end gap-2">
          <button onClick={onClose} data-testid="add-non-steam-cancel" className="px-3 py-1.5 rounded-lg text-[10px] uppercase tracking-[.18em] text-vantare-textDim hover:text-white">
            Cancelar
          </button>
          <button onClick={handleAdd} disabled={!selectedId} data-testid="add-non-steam-add" className="px-3 py-1.5 rounded-lg bg-accent text-[10px] uppercase tracking-[.18em] font-bold text-black hover:opacity-90 disabled:opacity-40">
            Añadir
          </button>
        </footer>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `pnpm --dir frontend test AddNonSteamGameModal -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/AddNonSteamGameModal.tsx frontend/src/hub/launcher/AddNonSteamGameModal.test.tsx frontend/package.json
git commit -m "feat(launcher): AddNonSteamGameModal single-select + cap 200 (cut 2)"
```

### Task 2.4: Integrar modal en AppsPanel y añadir panel de detalles con args editable

**Files:**
- Modify: `frontend/src/hub/launcher/AppsPanel.tsx`
- Test: `frontend/src/hub/launcher/AppsPanel.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
it("opens AddNonSteamGameModal on add button click", () => {
  // ...
});
```

- [ ] **Step 2: Integrate the modal**

En `AppsPanel.tsx`, reemplazar el `<input type="file">` por la apertura del modal:

```tsx
import { AddNonSteamGameModal } from "./AddNonSteamGameModal";

// Estado:
const [showAddModal, setShowAddModal] = useState(false);
const [detailsAppId, setDetailsAppId] = useState<string | null>(null);

// Botón:
<button onClick={() => setShowAddModal(true)} data-testid="apps-add-manual">
  + {t("launcher.apps.addManual")}
</button>

// Modal:
<AddNonSteamGameModal
  open={showAddModal}
  onClose={() => setShowAddModal(false)}
  onAdd={(entry) => {
    // Crear una LauncherAppEntry con detected: false y emitir add.
    const newEntry: LauncherAppEntry = {
      id: newProfileId("app"),
      displayName: entry.displayName,
      abbreviation: entry.displayName.slice(0, 3).toUpperCase(),
      category: "utility",
      launchMethod: "executable",
      executablePath: entry.executablePath,
      detected: false,
      gradientFrom: "#6b7280",
      gradientTo: "#1f2937",
    };
    Events.Emit("launcher:app:add", { entry: newEntry });
  }}
/>

// Panel de detalles (toggle):
{detailsAppId && (() => {
  const app = apps.find((a) => a.id === detailsAppId);
  if (!app) return null;
  return (
    <div className="mt-2 p-3 bg-black/30 rounded-md text-xs">
      <div>Path: {app.executablePath}</div>
      <input
        type="text"
        value={app.args ?? ""}
        onChange={(e) => {
          // Emitir update event (nuevo: launcher:app:update).
          Events.Emit("launcher:app:update", { id: app.id, args: e.target.value });
        }}
        placeholder="Args..."
        data-testid="app-args-input"
      />
    </div>
  );
})()}
```

- [ ] **Step 3: Implementar handler de update en main.go**

En `cmd/vantare/main.go`, añadir:

```go
func (a *App) handleAppUpdate(event wailsEvent) {
    id, _ := event.Payload["id"].(string)
    args, _ := event.Payload["args"].(string)
    if err := a.settings.UpdateLauncherAppArgs(id, args); err != nil {
        a.logError("update app args", err)
        return
    }
    apps := a.settings.GetLauncherApps()
    a.emit("launcher:apps:updated", map[string]any{"apps": apps})
}
```

En `settings_service.go`, añadir:

```go
func (s *SettingsService) UpdateLauncherAppArgs(id, args string) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    if s.settings == nil { return ErrSettingsNotLoaded }
    app, ok := s.settings.LauncherApps[id]
    if !ok { return ErrAppNotFound }
    app.Args = args
    s.settings.LauncherApps[id] = app
    return s.saveLocked(s.settings)
}
```

- [ ] **Step 4: Run tests**

Run: `pnpm --dir frontend test AppsPanel -v && go test ./internal/app/... ./cmd/vantare/... -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/AppsPanel.tsx frontend/src/hub/launcher/AppsPanel.test.tsx cmd/vantare/main.go internal/app/settings_service.go
git commit -m "feat(launcher): integrate AddNonSteamGameModal + args editable (cut 2)"
```

### Task 2.5: Visual compare del modal

- [ ] **Step 1: Create launcher-visual-compare.mjs script**

Crear `frontend/scripts/launcher-visual-compare.mjs` siguiendo el patrón de `widget-studio-visual-compare.mjs`. Captura: modal abierto, modal con búsqueda, modal con selección.

- [ ] **Step 2: Run visual compare**

Run: `cd frontend && node scripts/launcher-visual-compare.mjs`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add frontend/scripts/launcher-visual-compare.mjs
git commit -m "test(launcher): visual compare for AddNonSteamGameModal (cut 2)"
```

---

## Corte 3 — Favoritos y notas

### Task 3.1: Toggle favorita en AppBadge y AppRow

**Files:**
- Modify: `frontend/src/hub/components/AppBadge.tsx` (o crear si no existe)
- Test: `frontend/src/hub/components/AppBadge.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
it("renders favorite toggle that emits update event on click", () => {
  // ...
});
```

- [ ] **Step 2: Implement the toggle**

En `AppBadge.tsx` (o donde esté el badge de la app), añadir una estrella clickable:

```tsx
<button
  onClick={() => Events.Emit("launcher:app:favorite", { id: app.id, isFavorite: !app.isFavorite })}
  data-testid={`app-favorite-${app.id}`}
  aria-label={app.isFavorite ? "Quitar de favoritas" : "Marcar como favorita"}
>
  {app.isFavorite ? "★" : "☆"}
</button>
```

- [ ] **Step 3: Handler en main.go**

```go
func (a *App) handleAppFavorite(event wailsEvent) {
    id, _ := event.Payload["id"].(string)
    fav, _ := event.Payload["isFavorite"].(bool)
    if err := a.settings.SetLauncherAppFavorite(id, fav); err != nil { ... }
    a.emit("launcher:apps:updated", map[string]any{"apps": a.settings.GetLauncherApps()})
}
```

En `settings_service.go`:

```go
func (s *SettingsService) SetLauncherAppFavorite(id string, fav bool) error { ... }
```

- [ ] **Step 4: Run tests**

Run: `pnpm --dir frontend test AppBadge -v && go test ./... -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/components/AppBadge.tsx cmd/vantare/main.go internal/app/settings_service.go
git commit -m "feat(launcher): favorite toggle on app badge (cut 3)"
```

### Task 3.2: ProfileCard con badge favorita y orden en grilla

**Files:**
- Modify: `frontend/src/hub/launcher/ProfileCard.tsx`
- Test: `frontend/src/hub/launcher/ProfileCard.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
it("renders favorite badge and isFavorite prop affects rendering", () => {
  // ...
});
```

- [ ] **Step 2: Implement favorite badge in ProfileCard**

En `ProfileCard.tsx`, añadir al header (cuando `profile.isFavorite`):

```tsx
{profile.isFavorite && (
  <span className="text-amber-400" data-testid={`profile-favorite-badge-${profile.id}`} aria-label="Favorita">★</span>
)}
```

- [ ] **Step 3: Ordenar ProfilesPanel con favoritas primero**

En `ProfilesPanel.tsx`:

```tsx
const orderedProfiles = useMemo(
  () => [...profiles].sort((a, b) => {
    if (a.isFavorite && !b.isFavorite) return -1;
    if (!a.isFavorite && b.isFavorite) return 1;
    return a.name.localeCompare(b.name);
  }),
  [profiles]
);
```

(Sin sección separada, según decisión 11.)

- [ ] **Step 4: Run tests**

Run: `pnpm --dir frontend test ProfileCard ProfilesPanel -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/ProfileCard.tsx frontend/src/hub/launcher/ProfileCard.test.tsx frontend/src/hub/launcher/ProfilesPanel.tsx
git commit -m "feat(launcher): favorite badge + order in grid (no separate section) (cut 3)"
```

### Task 3.3: Notes en ProfileEditor (side-panel)

**Files:**
- Create: `frontend/src/hub/launcher/ProfileEditor.tsx` (reescribir como side-panel)
- Test: `frontend/src/hub/launcher/ProfileEditor.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
it("renders notes textarea in side panel", () => {
  // ...
});
```

- [ ] **Step 2: Implement side-panel con notes**

```tsx
// Side-panel que se desliza desde la derecha.
import { motion, AnimatePresence } from "motion/react";

type Props = {
  profile: LaunchProfile | null;
  open: boolean;
  onClose: () => void;
  onSave: (p: LaunchProfile) => void;
};

export function ProfileEditor({ profile, open, onClose, onSave }: Props) {
  const [draft, setDraft] = useState<LaunchProfile | null>(null);
  useEffect(() => { setDraft(profile); }, [profile]);

  if (!draft) return null;

  return (
    <AnimatePresence>
      {open && (
        <>
          <motion.div
            className="fixed inset-0 z-40 bg-black/40"
            initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}
            onClick={onClose}
          />
          <motion.aside
            className="fixed right-0 top-0 bottom-0 w-[480px] z-50 card-sleek p-5 overflow-y-auto"
            initial={{ x: "100%" }} animate={{ x: 0 }} exit={{ x: "100%" }}
            transition={{ duration: 0.25, ease: [0.16, 1, 0.3, 1] }}
            data-testid="profile-editor-panel"
            role="dialog" aria-label="Editar perfil"
          >
            <h2>Editar perfil</h2>
            <input value={draft.name} onChange={(e) => setDraft({ ...draft, name: e.target.value })} data-testid="profile-editor-name" />
            <textarea value={draft.notes ?? ""} onChange={(e) => setDraft({ ...draft, notes: e.target.value })} data-testid="profile-editor-notes" placeholder="Notas..." />
            {/* ... hotkey, autostart, steps, args ... */}
            <button onClick={() => onSave(draft)} data-testid="profile-editor-save">Guardar</button>
          </motion.aside>
        </>
      )}
    </AnimatePresence>
  );
}
```

- [ ] **Step 3: Run tests**

Run: `pnpm --dir frontend test ProfileEditor -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/launcher/ProfileEditor.tsx frontend/src/hub/launcher/ProfileEditor.test.tsx
git commit -m "feat(launcher): ProfileEditor as side-panel with notes (cut 3)"
```

### Task 3.4: Visual compare del estado con favoritas

- [ ] **Step 1: Extend launcher-visual-compare.mjs to capture favorite state**

- [ ] **Step 2: Run and commit**

```bash
git add frontend/scripts/launcher-visual-compare.mjs
git commit -m "test(launcher): visual compare for favorites + notes (cut 3)"
```

---

## Corte 4 — Telemetría (backend-only)

### Task 4.1: RecordProfileAttempt y RecordProfileSuccess

**Files:**
- Modify: `internal/app/launcher/profiles.go` (o crear `telemetry.go`)
- Test: `internal/app/launcher/profiles_test.go`

- [ ] **Step 1: Write the failing tests**

```go
func TestRecordProfileAttemptAlwaysIncrements(t *testing.T) {
    backend := &fakeProfilesBackend{
        apps:     map[string]LauncherAppEntry{"lmu": {}},
        profiles: []LaunchProfile{{ID: "p1", Name: "P1", Steps: []LaunchStep{{AppID: "lmu", Delay: 0}}}},
    }
    if err := RecordProfileAttempt(backend, "p1"); err != nil {
        t.Fatalf("attempt: %v", err)
    }
    if backend.profiles[0].LaunchCount != 1 {
        t.Errorf("count should be 1, got %d", backend.profiles[0].LaunchCount)
    }
    if backend.profiles[0].LastLaunchedAt == nil {
        t.Error("LastLaunchedAt should be set")
    }
}

func TestRecordProfileSuccessUpdatesEMA(t *testing.T) {
    backend := &fakeProfilesBackend{
        apps:     map[string]LauncherAppEntry{"lmu": {}},
        profiles: []LaunchProfile{{ID: "p1", Name: "P1", Steps: []LaunchStep{{AppID: "lmu", Delay: 0}}}},
    }
    if err := RecordProfileSuccess(backend, "p1", 10000); err != nil {
        t.Fatalf("success: %v", err)
    }
    if backend.profiles[0].AvgChainDurationMs != 10000 {
        t.Errorf("expected avg=10000 first time, got %d", backend.profiles[0].AvgChainDurationMs)
    }
    if err := RecordProfileSuccess(backend, "p1", 20000); err != nil {
        t.Fatalf("success 2: %v", err)
    }
    // EMA: 0.3*20000 + 0.7*10000 = 6000 + 7000 = 13000
    if backend.profiles[0].AvgChainDurationMs != 13000 {
        t.Errorf("expected EMA=13000, got %d", backend.profiles[0].AvgChainDurationMs)
    }
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `go test ./internal/app/launcher/... -run "TestRecordProfileAttempt|TestRecordProfileSuccess" -v`
Expected: FAIL.

- [ ] **Step 3: Implement RecordProfileAttempt and RecordProfileSuccess**

En `internal/app/launcher/profiles.go` (o `telemetry.go`):

```go
const emaAlpha = 0.3

func RecordProfileAttempt(backend ProfilesBackend, profileID string) error {
    profiles := backend.GetLauncherProfiles()
    idx := -1
    for i := range profiles {
        if profiles[i].ID == profileID { idx = i; break }
    }
    if idx == -1 { return ErrProfileNotFound }
    profiles[idx].LaunchCount++
    now := time.Now().UTC()
    profiles[idx].LastLaunchedAt = &now
    return backend.SetLauncherProfiles(profiles)
}

func RecordProfileSuccess(backend ProfilesBackend, profileID string, durationMs int64) error {
    profiles := backend.GetLauncherProfiles()
    idx := -1
    for i := range profiles {
        if profiles[i].ID == profileID { idx = i; break }
    }
    if idx == -1 { return ErrProfileNotFound }
    p := &profiles[idx]
    if p.AvgChainDurationMs == 0 {
        p.AvgChainDurationMs = durationMs
    } else {
        p.AvgChainDurationMs = int64(emaAlpha*float64(durationMs) + (1-emaAlpha)*float64(p.AvgChainDurationMs))
    }
    return backend.SetLauncherProfiles(profiles)
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `go test ./internal/app/launcher/... -run "TestRecordProfileAttempt|TestRecordProfileSuccess" -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/app/launcher/profiles.go internal/app/launcher/profiles_test.go
git commit -m "feat(launcher): RecordProfileAttempt + RecordProfileSuccess with EMA (cut 4)"
```

### Task 4.2: ChainRunner llama a las funciones de telemetría

**Files:**
- Modify: `internal/app/launcher/chain.go`
- Test: `internal/app/launcher/chain_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestChainRunnerCallsRecordAttemptOnFailure(t *testing.T) {
    // Stub exec que falla, verificar que RecordProfileAttempt se llama pero no RecordProfileSuccess.
    // ...
}
```

- [ ] **Step 2: Wire the calls**

En `chain.go`, después de cerrar la cadena:

```go
// SIEMPRE: attempt.
_ = RecordProfileAttempt(backend, profileID)
// Solo en éxito: success.
if success {
    _ = RecordProfileSuccess(backend, profileID, durationMs)
}
```

- [ ] **Step 3: Run tests**

Run: `go test ./internal/app/launcher/... -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/app/launcher/chain.go internal/app/launcher/chain_test.go
git commit -m "feat(launcher): chain runner calls telemetry functions (cut 4)"
```

---

## Corte 5 — Cadena en vivo (UI + store + telemetry UI + liveness)

### Task 5.1: Store externo con useSyncExternalStore

**Files:**
- Create: `frontend/src/hub/launcher/chain-store.ts`
- Create: `frontend/src/hub/launcher/chain-store.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
import { createChainStore, ChainStepStatus } from "./chain-store";

describe("chain-store", () => {
  it("updates on step event", () => {
    const store = createChainStore();
    store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "launching", startedAt: 1000 });
    expect(store.getChain("p1")?.steps[0].status).toBe("launching");
  });

  it("is tolerant to out-of-order: done before launching", () => {
    const store = createChainStore();
    store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "done", finishedAt: 2000 });
    expect(store.getChain("p1")?.steps[0].status).toBe("done");
  });

  it("is idempotent: second done for same step is ignored", () => {
    const store = createChainStore();
    store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "done", finishedAt: 2000 });
    store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "done", finishedAt: 3000 });
    expect(store.getChain("p1")?.steps[0].finishedAt).toBe(2000);
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm --dir frontend test chain-store.test -v`
Expected: FAIL.

- [ ] **Step 3: Implement the store with reducer**

```ts
import { useSyncExternalStore } from "react";

export type ChainStepStatus = "pending" | "launching" | "done" | "failed";
export type ChainStepState = {
  appId: string;
  status: ChainStepStatus;
  startedAt?: number;
  finishedAt?: number;
  message?: string;
  pid?: number;
};
export type ChainState = {
  profileId: string;
  startedAt: number;
  steps: ChainStepState[];
  currentStepIndex: number;
  overallStatus: "running" | "done" | "error";
};
export type ChainStepEvent = {
  profileId: string;
  stepIndex: number;
  appId: string;
  status: ChainStepStatus;
  startedAt?: number;
  finishedAt?: number;
  message?: string;
  pid?: number;
};

const VALID_TRANSITIONS: Record<ChainStepStatus, ChainStepStatus[]> = {
  pending: ["launching", "done", "failed"],
  launching: ["done", "failed"],
  done: [],
  failed: [],
};

function applyStep(state: ChainStepState | undefined, ev: ChainStepEvent): ChainStepState {
  if (!state) state = { appId: ev.appId, status: "pending" };
  // Idempotencia: si el estado actual es terminal, ignorar.
  if (state.status === "done" || state.status === "failed") return state;
  // Validar transición.
  if (!VALID_TRANSITIONS[state.status].includes(ev.status)) {
    // Permitir eventos desordenados: si llega "done" antes de "launching", aceptar.
    if (state.status === "pending" && (ev.status === "done" || ev.status === "failed")) {
      return { ...state, appId: ev.appId, status: ev.status, startedAt: ev.startedAt, finishedAt: ev.finishedAt, message: ev.message, pid: ev.pid };
    }
    return state;
  }
  return { ...state, status: ev.status, startedAt: ev.startedAt ?? state.startedAt, finishedAt: ev.finishedAt, message: ev.message, pid: ev.pid ?? state.pid };
}

export function createChainStore() {
  let chains = new Map<string, ChainState>();
  const timeouts = new Map<string, ReturnType<typeof setTimeout>>();
  const watchers = new Map<string, Set<() => void>>(); // profileId -> watchers
  const globalWatchers = new Set<() => void>();
  let watchdogInterval: ReturnType<typeof setInterval> | null = null;

  function notify(profileId: string) {
    watchers.get(profileId)?.forEach((fn) => fn());
    globalWatchers.forEach((fn) => fn());
  }

  function getChain(profileId: string): ChainState | undefined {
    return chains.get(profileId);
  }

  function handleStep(ev: ChainStepEvent) {
    // Cancelar el timeout pendiente de cleanup para este perfil.
    const pending = timeouts.get(ev.profileId);
    if (pending) { clearTimeout(pending); timeouts.delete(ev.profileId); }

    const existing = chains.get(ev.profileId);
    const steps = existing ? [...existing.steps] : [];
    steps[ev.stepIndex] = applyStep(steps[ev.stepIndex], ev);
    const newChain: ChainState = existing
      ? { ...existing, steps, currentStepIndex: ev.stepIndex, overallStatus: "running" }
      : { profileId: ev.profileId, startedAt: ev.startedAt ?? Date.now(), steps, currentStepIndex: ev.stepIndex, overallStatus: "running" };
    chains.set(ev.profileId, newChain);
    notify(ev.profileId);
  }

  function handleDone(profileId: string, success: boolean) {
    const existing = chains.get(profileId);
    if (!existing) return;
    chains.set(profileId, { ...existing, overallStatus: success ? "done" : "error" });
    scheduleCleanup(profileId);
    notify(profileId);
  }

  function scheduleCleanup(profileId: string) {
    const pending = timeouts.get(profileId);
    if (pending) clearTimeout(pending);
    const t = setTimeout(() => {
      chains.delete(profileId);
      timeouts.delete(profileId);
      notify(profileId);
    }, 3000);
    timeouts.set(profileId, t);
  }

  // Watchdog: cada 5s, comprobar chains en running sin eventos recientes.
  function startWatchdog() {
    if (watchdogInterval) return;
    watchdogInterval = setInterval(() => {
      const now = Date.now();
      const stale: string[] = [];
      for (const [id, chain] of chains) {
        if (chain.overallStatus === "running" && now - chain.startedAt > 30000) {
          chains.set(id, { ...chain, overallStatus: "error" });
          scheduleCleanup(id);
          stale.push(id);
        }
      }
      stale.forEach(notify);
    }, 5000);
  }
  function stopWatchdog() {
    if (watchdogInterval) { clearInterval(watchdogInterval); watchdogInterval = null; }
  }

  function shutdown() {
    timeouts.forEach((t) => clearTimeout(t));
    timeouts.clear();
    stopWatchdog();
  }

  // useSyncExternalStore-style subscription.
  function subscribe(profileId: string | null, cb: () => void) {
    if (profileId === null) {
      globalWatchers.add(cb);
      return () => globalWatchers.delete(cb);
    }
    if (!watchers.has(profileId)) watchers.set(profileId, new Set());
    watchers.get(profileId)!.add(cb);
    return () => { watchers.get(profileId)?.delete(cb); };
  }

  startWatchdog();
  return { getChain, handleStep, handleDone, subscribe, shutdown, scheduleCleanup };
}

// React hook: suscribe a un profileId específico.
export function useChainState(profileId: string) {
  // El store se crea una vez y se inyecta vía Context (definido en otro archivo).
  return useSyncExternalStore(
    (cb) => storeRef.subscribe(profileId, cb),
    () => storeRef.getChain(profileId),
    () => undefined
  );
}

// Singleton (en producción se inyecta vía provider; aquí simplificado).
let storeRef = createChainStore();
export function getChainStore() { return storeRef; }
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `pnpm --dir frontend test chain-store.test -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/chain-store.ts frontend/src/hub/launcher/chain-store.test.ts
git commit -m "feat(launcher): external chain store with useSyncExternalStore (cut 5)"
```

### Task 5.2: Liveness probe en ChainRunner

**Files:**
- Modify: `internal/app/launcher/chain.go`
- Test: `internal/app/launcher/chain_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestChainRunnerLivenessProbeCatchesCrash(t *testing.T) {
    // exec stub que arranca y muere en 1s con código 1.
    // Verificar que el step se marca failed.
}
```

- [ ] **Step 2: Implementar liveness probe**

En `chain.go`, tras `Start()`, lanzar una goroutine que espera el proceso con timeout:

```go
cmd := exec.Command(...)
if err := cmd.Start(); err != nil { ... }
pid := cmd.Process.Pid
emit step con pid.
// Goroutine de liveness probe (no bloqueante).
go func() {
    // Wait con timeout.
    done := make(chan error, 1)
    go func() { done <- cmd.Wait() }()
    select {
    case err := <-done:
        finishedAt := time.Now()
        if err != nil {
            // Proceso murió con código !=0 o error.
            emit step failed.
        } else {
            emit step done.
        }
    case <-time.After(3 * time.Second):
        // Sigue vivo a los 3s, asumimos done.
        emit step done.
    }
}()
```

- [ ] **Step 3: Run tests**

Run: `go test ./internal/app/launcher/... -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/app/launcher/chain.go internal/app/launcher/chain_test.go
git commit -m "feat(launcher): liveness probe for .exe post-Start (cut 5)"
```

### Task 5.3: ProfileCard se transforma con mini-timeline

**Files:**
- Create: `frontend/src/hub/launcher/ProfileCard.timeline.tsx`
- Create: `frontend/src/hub/launcher/ProfileCard.timeline.test.tsx`
- Modify: `frontend/src/hub/launcher/ProfileCard.tsx`

- [ ] **Step 1: Write the failing test for ProfileCardTimeline**

```tsx
it("renders one block per step with launching pulse", () => {
  // ...
});
```

- [ ] **Step 2: Implement ProfileCard.timeline.tsx**

```tsx
import { motion } from "motion/react";
import type { ChainState, LauncherAppEntry } from "./chain-store";

const CATEGORY_COLORS: Record<string, string> = {
  simulator: "#ff3b3b",
  streaming: "#302e31",
  audio: "#06b6d4",  // cian (decisión post-review).
  telemetry: "#f59e0b",
  utility: "#3b82f6",
};

type Props = {
  chain: ChainState;
  apps: LauncherAppEntry[];
  onCancel: () => void;
};

export function ProfileCardTimeline({ chain, apps, onCancel }: Props) {
  return (
    <div className="card-sleek rounded-xl p-5" data-testid="profile-timeline">
      <button onClick={onCancel} data-testid="profile-cancel" className="float-right px-3 py-1.5 rounded-lg border border-amber-500/40 text-[10px] uppercase tracking-[.18em] text-amber-300">Cancelar</button>
      <h3 className="font-display font-bold text-lg text-white mb-3">Lanzando...</h3>
      <div className="flex flex-wrap gap-2">
        {chain.steps.map((step, i) => {
          const app = apps.find((a) => a.id === step.appId);
          const color = app ? CATEGORY_COLORS[app.category] : "#6b7280";
          return (
            <motion.div
              key={`${step.appId}-${i}`}  // key estable.
              data-testid={`timeline-step-${i}`}
              className="rounded-md p-2 min-w-[80px]"
              style={{
                background: step.status === "launching" ? `${color}33` : step.status === "done" ? "#10b98126" : step.status === "failed" ? "#ef444426" : "rgba(255,255,255,0.05)",
                border: `1px solid ${step.status === "launching" ? color : step.status === "done" ? "#10b98166" : "rgba(255,255,255,0.1)"}`,
              }}
              animate={step.status === "launching" ? { opacity: [0.7, 1, 0.7] } : {}}
              transition={step.status === "launching" ? { duration: 1.5, repeat: Infinity, ease: "easeInOut" } : {}}
            >
              <div className="text-xs font-bold text-white">{app?.abbreviation ?? step.appId}</div>
              <div className="text-[10px] text-vantare-textMuted">{step.status}</div>
            </motion.div>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: ProfileCard se transforma con early return**

```tsx
import { useChainState } from "./chain-store";
import { ProfileCardTimeline } from "./ProfileCard.timeline";

export function ProfileCard({ profile, apps, onLaunch, onCancel }: Props) {
  const chain = useChainState(profile.id);
  if (chain) {
    return <ProfileCardTimeline chain={chain} apps={apps} onCancel={() => onCancel(profile.id)} />;
  }
  return (
    <article /* card normal */>
      {/* ... incluye telemetría visible: lastLaunchedAt, avgChainDurationMs, count en tooltip ... */}
    </article>
  );
}
```

- [ ] **Step 4: Run tests**

Run: `pnpm --dir frontend test ProfileCard -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/ProfileCard.timeline.tsx frontend/src/hub/launcher/ProfileCard.timeline.test.tsx frontend/src/hub/launcher/ProfileCard.tsx
git commit -m "feat(launcher): ProfileCard transforms to mini-timeline (cut 5)"
```

### Task 5.4: a11y tests + visual compare del timeline

- [ ] **Step 1: a11y tests**

```tsx
it("timeline has role=status and aria-live=polite", () => { ... });
it("cancel button has aria-label", () => { ... });
```

- [ ] **Step 2: Visual compare**

Extender `launcher-visual-compare.mjs` con captura del estado launching.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/hub/launcher/ProfileCard.timeline.a11y.test.tsx frontend/scripts/launcher-visual-compare.mjs
git commit -m "test(launcher): a11y + visual compare for timeline (cut 5)"
```

---

## Corte 6 — Dock sincronizado con lastResult y tooltip

**Files:**
- Modify: `frontend/src/hub/components/LauncherDock.tsx`
- Modify: `frontend/src/hub/components/LauncherDock.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
it("renders lastResult badge on dock button when no chain active", () => { ... });
it("count appears in tooltip, not on the face", () => { ... });
```

- [ ] **Step 2: Implement dock with useSyncExternalStore + lastResult + tooltip**

```tsx
import { useChainState, getChainStore } from "../launcher/chain-store";

export function LauncherDock({ onNavigate }: Props) {
  const [profiles, setProfiles] = useState<LaunchProfile[]>([]);
  // ... existing load profiles ...

  const orderedProfiles = useMemo(
    () => [...profiles].sort((a, b) => {
      if (a.isFavorite && !b.isFavorite) return -1;
      if (!a.isFavorite && b.isFavorite) return 1;
      return a.name.localeCompare(b.name);
    }),
    [profiles]
  );

  return (
    <aside className="v52-dock hidden lg:flex flex-col">
      <button onClick={() => onNavigate("launcher") /* icon */} aria-label="Ir a Launcher" />
      <div className="overflow-y-auto flex flex-col gap-1">
        {orderedProfiles.map((p) => (
          <DockProfileButton
            key={p.id}
            profile={p}
            onLaunch={() => Events.Emit("launcher:profile:launch", { id: p.id })}
            onCancel={() => Events.Emit("launcher:profile:cancel", { id: p.id })}
          />
        ))}
      </div>
    </aside>
  );
}

function DockProfileButton({ profile, onLaunch, onCancel }: Props) {
  const chain = useChainState(profile.id);
  const lastResult = useLastResult(profile.id); // sub-hook del store.
  // ... render con anillo SVG durante chain, badge sutil de lastResult, tooltip con count + lastLaunchedAt ...
}
```

- [ ] **Step 3: Run tests**

Run: `pnpm --dir frontend test LauncherDock -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/components/LauncherDock.tsx frontend/src/hub/components/LauncherDock.test.tsx
git commit -m "feat(launcher): dock with lastResult + count in tooltip (cut 6)"
```

---

## Corte 7 — Auto-launch + hotkeys + cierre robusto

### Task 7.1: autostart_windows.go

**Files:**
- Create: `internal/app/launcher/autostart_windows.go`
- Create: `internal/app/launcher/autostart_windows_test.go`

- [ ] **Step 1: Write the failing tests**

```go
func TestRegisterUnregisterAutostart(t *testing.T) {
    // ...
}

func TestAutostartWithUnknownProfileID(t *testing.T) {
    // Llama a ParseLaunchFlag con un profileID inexistente.
    // Verifica que devuelve ok=false sin panic.
}
```

- [ ] **Step 2: Implement autostart_windows.go**

```go
package launcher

import (
    "fmt"
    "os"
    "golang.org/x/sys/windows/registry"
)

const autostartKeyPath = `Software\Microsoft\Windows\CurrentVersion\Run`

func RegisterAutostart(profileID string) error {
    exe, err := os.Executable()
    if err != nil { return err }
    value := fmt.Sprintf(`"%s" --launch=%s`, exe, profileID)
    k, _, err := registry.CreateKey(registry.CURRENT_USER, autostartKeyPath, registry.SET_VALUE)
    if err != nil { return err }
    defer k.Close()
    return k.SetStringValue(fmt.Sprintf("Vantare.%s", profileID), value)
}

func UnregisterAutostart(profileID string) error {
    k, err := registry.OpenKey(registry.CURRENT_USER, autostartKeyPath, registry.SET_VALUE)
    if err != nil { return err }
    defer k.Close()
    return k.DeleteValue(fmt.Sprintf("Vantare.%s", profileID))
}

// ParseLaunchFlag extrae el profileID de os.Args[1:] buscando --launch=<id>.
// Devuelve el ID y ok=true si se encontró y es válido; ok=false en caso contrario (sin panic).
func ParseLaunchFlag(args []string) (string, bool) {
    for _, a := range args {
        if len(a) > 9 && a[:9] == "--launch=" {
            id := a[9:]
            if id != "" {
                return id, true
            }
        }
    }
    return "", false
}
```

- [ ] **Step 3: Run tests**

Run: `go test ./internal/app/launcher/... -run "TestAutostart" -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/app/launcher/autostart_windows.go internal/app/launcher/autostart_windows_test.go
git commit -m "feat(launcher): autostart_windows.go + ParseLaunchFlag (cut 7)"
```

### Task 7.2: hotkey_windows.go con RegisterHotKey + WM_POWERBROADCAST

**Files:**
- Create: `internal/app/launcher/hotkey_windows.go`
- Create: `internal/app/launcher/hotkey_windows_test.go`

- [ ] **Step 1: Write the failing tests**

```go
func TestParseHotkeyString(t *testing.T) {
    mods, vk, err := ParseHotkeyString("ctrl+shift+1")
    if err != nil { t.Fatal(err) }
    if mods&MOD_CONTROL == 0 { t.Error("expected ctrl") }
    if mods&MOD_SHIFT == 0 { t.Error("expected shift") }
    if vk != 0x31 { t.Errorf("expected VK_1=0x31, got %x", vk) }
}

func TestHotkeyWhitelistRejectsReserved(t *testing.T) {
    if IsHotkeyAllowed("ctrl+c") { t.Error("ctrl+c should be rejected") }
    if !IsHotkeyAllowed("ctrl+shift+1") { t.Error("ctrl+shift+1 should be allowed") }
}
```

- [ ] **Step 2: Implement hotkey_windows.go**

```go
package launcher

import (
    "strings"
)

func ParseHotkeyString(s string) (modifiers uint32, vk uint32, err error) {
    parts := strings.Split(strings.ToLower(s), "+")
    for _, p := range parts[:len(parts)-1] {
        switch p {
        case "ctrl": modifiers |= MOD_CONTROL
        case "shift": modifiers |= MOD_SHIFT
        case "alt": modifiers |= MOD_ALT
        case "win": modifiers |= MOD_WIN
        }
    }
    last := parts[len(parts)-1]
    if len(last) == 1 {
        vk = uint32(last[0]) // 'a'-'z', '0'-'9'.
    } else {
        return 0, 0, fmt.Errorf("invalid key: %s", last)
    }
    return modifiers, vk, nil
}

var reservedCombos = map[string]bool{
    "ctrl+c": true, "ctrl+v": true, "ctrl+x": true,
    "ctrl+alt+delete": true, "win+l": true, "alt+f4": true,
    "alt+tab": true,
}

func IsHotkeyAllowed(combo string) bool {
    if reservedCombos[strings.ToLower(combo)] { return false }
    return true
}
```

- [ ] **Step 3: Run tests**

Run: `go test ./internal/app/launcher/... -run "TestParseHotkey|TestHotkeyWhitelist" -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/app/launcher/hotkey_windows.go internal/app/launcher/hotkey_windows_test.go
git commit -m "feat(launcher): hotkey_windows.go with whitelist (cut 7)"
```

### Task 7.3: Hook de cierre de ventana cancela cadenas

**Files:**
- Modify: `cmd/vantare/main.go`
- Test: `cmd/vantare/main_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestHandleWindowCloseCancelsChains(t *testing.T) {
    // Verifica que onClose cancela las cadenas activas.
}
```

- [ ] **Step 2: Wire the hook**

En `main.go`, en el setup de la ventana Wails:

```go
wails.OnShutdown(func() {
    a.chainRunner.CancelAll()
})
```

Añadir `CancelAll` a `ChainRunner`:

```go
func (r *ChainRunner) CancelAll() {
    r.mu.Lock()
    defer r.mu.Unlock()
    for _, cancel := range r.active {
        cancel()
    }
    r.active = map[string]context.CancelFunc{}
}
```

- [ ] **Step 3: Run tests**

Run: `go test ./... -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add cmd/vantare/main.go internal/app/launcher/chain.go cmd/vantare/main_test.go
git commit -m "feat(launcher): window close hook cancels active chains (cut 7)"
```

### Task 7.4: i18n + final verification

- [ ] **Step 1: Add new i18n keys**

En `frontend/src/i18n/locales/{es,en,pt,it}.ts`, añadir las nuevas keys:
- `launcher.favorite`, `launcher.notes`, `launcher.hotkey`, `launcher.autostart`
- `launcher.chain.states.{pending,launching,done,failed,cancelled}`
- `launcher.modal.addNonSteam.{title,search,add,cancel,browse}`
- `launcher.appDetails.{path,args}`
- etc.

- [ ] **Step 2: Run final verification**

```bash
pnpm --dir frontend test
pnpm --dir frontend build
pnpm --dir frontend lint
go test ./...
go build ./...
```

Expected: all pass.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/i18n/
git commit -m "feat(launcher): i18n keys for new features (cut 7)"
```

---

## Code Review Multi-Revisor (al final de los 9 cortes)

Una vez los 9 cortes están commiteados y los tests pasan, se ejecuta el code review con 3 subagentes `oracle` en paralelo, sobre el código de los cortes 0b-7 (no sobre el spec).

### Review Task 1: Lanzar 3 revisores oracle en paralelo

```bash
# Crear el job board (manual, fuera del plan de tasks).
# Disparar 3 subagentes en background con task IDs reutilizables.
```

Cada revisor recibe:
- El spec validado (`docs/superpowers/specs/2026-07-08-launcher-v2-design.md`).
- El rango de commits (todos los de la iteración: `git log --oneline 626b66d..HEAD`).
- La instrucción: revisar el código commiteado desde la lente asignada (rendimiento, fiabilidad, mantenibilidad), y emitir un reporte con:
  - Issues encontrados (con `file:line` y sugerencia de fix).
  - Decisiones validadas con justificación de 1 línea.
  - Veredicto: ¿se puede mergear? Si no, ¿qué falta?

### Review Task 2: Reconciliar los reportes

Yo (orchestrator) consolido los 3 reportes en un único "Code Review Report" y, si hay issues que se reabren, los paso a un worker `fixer` con un mini-plan de fixes.

### Review Task 3: Re-verificar después de fixes

```bash
pnpm --dir frontend test
pnpm --dir frontend build
go test ./...
go build ./...
```

Si pasa, el launcher v2 está listo para commit final + tag (decisión del usuario, no automática).

---

## Resumen de entregables

- **Backend Go**: 8 archivos modificados/creados, todos los tests pasan.
- **Frontend**: 14 archivos modificados/creados, todos los tests pasan, build OK, lint OK.
- **i18n**: paridad de las nuevas keys en es/en/pt/it.
- **Visual compare**: script captura los estados clave, exit 0.
- **Code review**: 3 revisores aprueban (o los issues se reabren y se arreglan).
- **Sin commit, sin tag, sin release** (decisión del usuario, no automática).
