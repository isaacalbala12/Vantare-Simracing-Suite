# Launcher v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir el launcher actual (preliminar) en un launcher de perfiles profesional con discovery robusto, telemetría, feedback en vivo, auto-launch, hotkeys, toasts nativos y diseño Vantare Crystal, sin romper la base existente.

**Architecture:** 9 cortes incrementales sobre el spec validado `docs/superpowers/specs/2026-07-08-launcher-v2-design.md` y revisado adversarialmente. Cada corte compila, pasa sus tests, y se verifica manualmente antes del siguiente. Backend Go con Wails v3 alpha.98. Frontend React 19 + TypeScript + Tailwind v4 + Motion (Framer v11+). Store externo con `useSyncExternalStore` para feedback de cadena. Save atómico con `.bak` rotativo, sidecar `.failed` y retry con backoff. Code review multi-revisor al final con regla de quórum explícita.

**Tech Stack:** Go 1.25+ · Wails v3 alpha.98 · React 19 + TypeScript · Tailwind v4 · Motion (Framer) v11+ · `golang.org/x/sys/windows/registry` (ya en go.mod) · `golang.org/x/sys/windows` (RegisterHotKey, WM_POWERBROADCAST) · Vitest · `prefers-reduced-motion` con fallback `matchMedia`.

**Spec de referencia:** `docs/superpowers/specs/2026-07-08-launcher-v2-design.md`. **Este plan NO duplica el spec; lo implementa.**

---

## Mapeo de archivos (corregido tras revisión adversarial)

### Backend Go

| Archivo | Responsabilidad | Acción |
|---|---|---|
| `internal/app/settings_service.go` | Persistencia atómica con retry+backoff, SchemaVersion, mutex, RecordProfileAttempt/Success | Modificar |
| `internal/app/settings_service_test.go` | Tests de migración, atomicidad, sidecar, mutex, telemetría | Modificar |
| `internal/app/launcher/chain.go` | ChainRunner con backend, liveness probe, PIDs, active map con rechazo, RecordProfileAttempt/Success, CancelAll, eventos con schema reconciliado | Reescribir |
| `internal/app/launcher/chain_test.go` | Tests completos del chain runner (7 tests) | Modificar |
| `internal/app/launcher/discovery_windows.go` | Parser Steam correcto | Modificar |
| `internal/app/launcher/registry_common.go` | Helper compartido readUninstallEntries (var para tests) | Crear |
| `internal/app/launcher/registry_windows.go` | ListRegistryApps (exportado) para el modal | Crear |
| `internal/app/launcher/registry_windows_test.go` | Tests del listado | Crear |
| `internal/app/launcher/discovery_windows_test.go` | Tests del parser Steam + 7 apps | Modificar |
| `internal/app/launcher/autostart_windows.go` | HKCU\...\Run + ParseLaunchFlag | Crear |
| `internal/app/launcher/autostart_windows_test.go` | Tests | Crear |
| `internal/app/launcher/hotkey_windows.go` | ParseHotkeyString, IsHotkeyAllowed, RegisterProfileHotkey, UnregisterProfileHotkey, ReRegisterAll, hotkeyIdPool, parseHotkeyString | Reescribir |
| `internal/app/launcher/hotkey_windows_test.go` | Tests de parsing, whitelist, re-registro idempotente | Modificar |
| `internal/app/launcher/toast_windows.go` | ShowToast (nativo WinRT) + NotificationsEnabled check + fallback al Hub | Crear |
| `internal/app/launcher/toast_windows_test.go` | Tests | Crear |
| `internal/app/diagnostics_service.go` | Sanitización de nuevos campos (isFavorite, notes redactado, launchCount, lastLaunchedAt, avgChainDurationMs, launchOnWindowsStartup, hotkey) | Modificar |
| `internal/app/diagnostics_service_test.go` | Tests de sanitización | Modificar |
| `cmd/vantare/main.go` | Handlers: registry:list, profile:cancel, profile:retry:failed, profile:stats:save, profile:hotkey:set, autostart:toggle, app:favorite, app:update, registry:list. Hook Wails OnShutdown cancela cadenas. Hooks de Windows message pump (WM_POWERBROADCAST). | Modificar |
| `cmd/vantare/main_test.go` | TestHandleRegistryList, TestHandleProfileCancel, TestHandleProfileRetryFailed, TestHandleProfileStatsSave, TestHandleProfileHotkeySet, TestHandleAutostartToggle, TestHandleWindowCloseCancelsChains, TestHandleAppFavorite, TestHandleAppUpdate, TestHandleLaunchFlag | Modificar |

### Frontend

| Archivo | Responsabilidad | Acción |
|---|---|---|
| `frontend/src/hub/launcher/launcher-state.ts` | Tipos, helpers (estimateChainDuration) | Modificar |
| `frontend/src/hub/launcher/launcher-state.test.ts` | Tests | Modificar |
| `frontend/src/hub/launcher/chain-store.ts` | Store externo con useSyncExternalStore, lastResultByProfileId, lastEventAt, useLastResult, cleanup con clearTimeout, watchdog, reducer tolerante | Crear |
| `frontend/src/hub/launcher/chain-store.test.ts` | Tests del store (reducer, cleanup, watchdog) | Crear |
| `frontend/src/hub/launcher/chain-store.subscribe.test.ts` | Test de suscripción selectiva | Crear |
| `frontend/src/hub/launcher/ProfileCard.tsx` | Se transforma con early return; UI telemetría (lastLaunchedAt, avg, count en tooltip); JSX completo de card normal | Reescribir |
| `frontend/src/hub/launcher/ProfileCard.test.tsx` | Tests con todos los campos visibles | Modificar |
| `frontend/src/hub/launcher/ProfileCard.timeline.tsx` | Mini-timeline con Motion + cian para audio | Crear |
| `frontend/src/hub/launcher/ProfileCard.timeline.test.tsx` | Tests | Crear |
| `frontend/src/hub/launcher/ProfileCard.timeline.a11y.test.tsx` | Tests de a11y | Crear |
| `frontend/src/hub/launcher/ProfileEditor.tsx` | Side-panel completo: name, notes, hotkey, autostart, steps, args | Reescribir |
| `frontend/src/hub/launcher/ProfileEditor.test.tsx` | Tests de todos los campos | Modificar |
| `frontend/src/hub/launcher/AppsPanel.tsx` | Modal single-select, panel de detalles con args editable, toggle favorita | Modificar |
| `frontend/src/hub/launcher/AppsPanel.test.tsx` | Tests | Modificar |
| `frontend/src/hub/launcher/AddNonSteamGameModal.tsx` | Single-select, pre-lowercase, cap 200 | Crear |
| `frontend/src/hub/launcher/AddNonSteamGameModal.test.tsx` | Tests con assertions reales | Crear |
| `frontend/src/hub/launcher/HubToast.tsx` | Componente toast del Hub (fallback) | Crear |
| `frontend/src/hub/launcher/HubToast.test.tsx` | Tests | Crear |
| `frontend/src/hub/components/LauncherDock.tsx` | useChainState + useLastResult, anillo SVG, lastResult badge, tooltip con count + lastLaunchedAt | Reescribir |
| `frontend/src/hub/components/LauncherDock.test.tsx` | Tests | Modificar |
| `frontend/src/hub/HubApp.tsx` | Provider ChainRunnerProvider global | Modificar |
| `frontend/src/i18n/locales/{es,en,pt,it}.ts` | Nuevas keys | Modificar |
| `frontend/src/i18n/i18n.test.ts` | Paridad de las nuevas keys en los 4 diccionarios | Modificar |
| `frontend/src/lib/wails-runtime-mock.ts` | Mockea nuevos eventos | Modificar |
| `frontend/scripts/launcher-visual-compare.mjs` | Capturas Playwright (incluye stale chain y lastResult dock) | Crear |
| `frontend/package.json` | Añade `motion` | Modificar |

---

## Corte 0a — Structs + migración aditiva + sanitización

### Task 0a.1: Structs Go con nuevos campos y SchemaVersion

**Files:**
- Modify: `internal/app/settings_service.go`
- Test: `internal/app/settings_service_test.go`

- [ ] **Step 1: Write the failing test for SchemaVersion**

En `internal/app/settings_service_test.go`, añadir al final:

```go
func TestDefaultAppSettingsHasSchemaVersion1(t *testing.T) {
    s := app.DefaultAppSettings()
    if s.SchemaVersion != 1 {
        t.Fatalf("expected SchemaVersion=1, got %d", s.SchemaVersion)
    }
}

func TestLoadMigratesLegacySettings(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    legacy := `{
        "deltaMode": "self",
        "cpuSampling": true,
        "hotkeys": {}
    }`
    if err := os.WriteFile(path, []byte(legacy), 0o644); err != nil {
        t.Fatalf("write: %v", err)
    }
    svc := app.NewSettingsService(path, nil)
    if err := svc.Load(); err != nil {
        t.Fatalf("load: %v", err)
    }
    if svc.Settings().SchemaVersion != 1 {
        t.Errorf("expected SchemaVersion=1 after migration, got %d", svc.Settings().SchemaVersion)
    }
    if svc.Settings().LauncherApps == nil {
        t.Error("LauncherApps should be initialized")
    }
    if svc.Settings().LauncherProfiles == nil {
        t.Error("LauncherProfiles should be initialized")
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `go test ./internal/app/... -run "TestDefaultAppSettingsHasSchemaVersion1|TestLoadMigratesLegacySettings" -v`
Expected: FAIL.

- [ ] **Step 3: Add SchemaVersion and new fields**

En `internal/app/settings_service.go`:

```go
type AppSettings struct {
    SchemaVersion    int                              `json:"schemaVersion"`
    DeltaMode        string                           `json:"deltaMode"`
    CpuSampling      bool                             `json:"cpuSampling"`
    Hotkeys          map[string]string                `json:"hotkeys"`
    LauncherApps     map[string]LauncherAppEntry      `json:"launcherApps,omitempty"`
    LauncherProfiles []LaunchProfile                  `json:"launcherProfiles,omitempty"`
}

type LauncherAppEntry struct {
    ID             string              `json:"id"`
    DisplayName    string              `json:"displayName"`
    Abbreviation   string              `json:"abbreviation"`
    Category       LauncherAppCategory `json:"category"`
    LaunchMethod   string              `json:"launchMethod"`
    SteamAppID     uint32              `json:"steamAppId,omitempty"`
    ExecutablePath string              `json:"executablePath,omitempty"`
    Args           string              `json:"args,omitempty"`
    Detected       bool                `json:"detected"`
    GradientFrom   string              `json:"gradientFrom"`
    GradientTo     string              `json:"gradientTo"`
    IsFavorite     bool                `json:"isFavorite,omitempty"`
}

type LaunchProfile struct {
    ID                     string       `json:"id"`
    Name                   string       `json:"name"`
    Description            string       `json:"description,omitempty"`
    Steps                  []LaunchStep `json:"steps"`
    IsFavorite             bool         `json:"isFavorite,omitempty"`
    Notes                  string       `json:"notes,omitempty"`
    LaunchCount            int          `json:"launchCount,omitempty"`
    LastLaunchedAt         *time.Time   `json:"lastLaunchedAt,omitempty"`
    AvgChainDurationMs     int64        `json:"avgChainDurationMs,omitempty"`
    LaunchOnWindowsStartup bool         `json:"launchOnWindowsStartup,omitempty"`
    Hotkey                 string       `json:"hotkey,omitempty"`
}
```

- [ ] **Step 4: Update DefaultAppSettings and migrateSettings**

```go
func DefaultAppSettings() *AppSettings {
    return &AppSettings{
        SchemaVersion:    1,
        DeltaMode:        "self",
        CpuSampling:      true,
        Hotkeys:          map[string]string{ /* existente */ },
        LauncherApps:     defaultLauncherApps(),
        LauncherProfiles: defaultLauncherProfiles(),
    }
}

func (s *SettingsService) migrateSettings(settings *AppSettings) {
    if settings.SchemaVersion == 0 {
        settings.SchemaVersion = 1
        if settings.LauncherApps == nil {
            settings.LauncherApps = defaultLauncherApps()
        }
        if settings.LauncherProfiles == nil {
            settings.LauncherProfiles = defaultLauncherProfiles()
        }
    }
}
```

En `Load`, después del merge, llamar a `s.migrateSettings(merged)`.

- [ ] **Step 5: Run the test to verify it passes**

Run: `go test ./internal/app/... -run "TestDefaultAppSettingsHasSchemaVersion1|TestLoadMigratesLegacySettings" -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/app/settings_service.go internal/app/settings_service_test.go
git commit -m "feat(launcher): add SchemaVersion and new model fields (cut 0a)"
```

### Task 0a.2: Tipos TS y estimateChainDuration

**Files:**
- Modify: `frontend/src/hub/launcher/launcher-state.ts`
- Test: `frontend/src/hub/launcher/launcher-state.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
import { estimateChainDuration } from "./launcher-state";

describe("estimateChainDuration", () => {
  it("returns 0 for empty profile", () => {
    const profile = { id: "p", name: "P", steps: [] };
    expect(estimateChainDuration(profile, [])).toBe(0);
  });

  it("sums delays + 2s per executable + 1s per steam-uri", () => {
    const profile = {
      id: "p", name: "P",
      steps: [
        { appId: "lmu", delay: 0 },
        { appId: "obs", delay: 2 },
      ],
    };
    const apps = [
      { id: "lmu", launchMethod: "steam-uri" as const, detected: true, displayName: "LMU", abbreviation: "LMU", category: "simulator" as const, gradientFrom: "", gradientTo: "" },
      { id: "obs", launchMethod: "executable" as const, detected: true, displayName: "OBS", abbreviation: "OBS", category: "streaming" as const, gradientFrom: "", gradientTo: "" },
    ];
    // 0 (lmu delay) + 1 (steam-uri) + 2 (obs delay) + 2 (executable) = 5
    expect(estimateChainDuration(profile, apps)).toBe(5000);
  });

  it("prefers avgChainDurationMs over estimate when present", () => {
    const profile = {
      id: "p", name: "P",
      steps: [{ appId: "lmu", delay: 0 }],
      avgChainDurationMs: 12345,
    };
    expect(estimateChainDuration(profile, [])).toBe(12345);
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm --dir frontend test launcher-state.test -v`
Expected: FAIL.

- [ ] **Step 3: Add types and helper**

En `frontend/src/hub/launcher/launcher-state.ts`:

```ts
export type LauncherAppEntry = {
  // ... campos existentes
  isFavorite?: boolean;
};

export type LaunchProfile = {
  // ... campos existentes
  isFavorite?: boolean;
  notes?: string;
  launchCount?: number;
  lastLaunchedAt?: string | null;
  avgChainDurationMs?: number;
  launchOnWindowsStartup?: boolean;
  hotkey?: string;
};

export function estimateChainDuration(
  profile: LaunchProfile,
  apps: LauncherAppEntry[]
): number {
  if (profile.avgChainDurationMs) return profile.avgChainDurationMs;
  if (profile.steps.length === 0) return 0;
  let totalMs = 0;
  for (const step of profile.steps) {
    const app = apps.find((a) => a.id === step.appId);
    const launchOverheadMs = app?.launchMethod === "steam-uri" ? 1000 : 2000;
    totalMs += step.delay * 1000 + launchOverheadMs;
  }
  return totalMs;
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `pnpm --dir frontend test launcher-state.test -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/launcher-state.ts frontend/src/hub/launcher/launcher-state.test.ts
git commit -m "feat(launcher): add new TS model fields and estimateChainDuration (cut 0a)"
```

### Task 0a.3: Sanitización de diagnostics para los nuevos campos

**Files:**
- Modify: `internal/app/diagnostics_service.go`
- Modify: `internal/app/diagnostics_service_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestSanitizeLauncherProfileRedactsNotes(t *testing.T) {
    s := app.NewDiagnosticsService()
    profiles := []app.SanitizedLauncherProfile{
        {ID: "p1", Name: "Creator", Steps: 3, Notes: "C:\\Users\\me\\file.exe with --flag"},
    }
    out := s.SanitizeLauncherProfiles(profiles)
    if !strings.Contains(out[0].Notes, "[redacted]") {
        t.Errorf("expected notes path to be redacted, got: %s", out[0].Notes)
    }
}

func TestSanitizeLauncherAppRedactsArgs(t *testing.T) {
    s := app.NewDiagnosticsService()
    apps := []app.SanitizedLauncherApp{
        {ID: "obs", DisplayName: "OBS", Category: "streaming", LaunchMethod: "executable",
         Detected: true, ExecutablePath: "C:\\Program Files\\OBS\\obs64.exe --profile x"},
    }
    out := s.SanitizeLauncherApps(apps)
    if strings.Contains(out[0].ExecutablePath, "OBS\\obs64.exe") {
        t.Errorf("expected exe path to be redacted, got: %s", out[0].ExecutablePath)
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `go test ./internal/app/... -run "TestSanitizeLauncherProfileRedactsNotes|TestSanitizeLauncherAppRedactsArgs" -v`
Expected: FAIL.

- [ ] **Step 3: Update SanitizedLauncherProfile and SanitizedLauncherApp**

En `internal/app/diagnostics_service.go`:

```go
type SanitizedLauncherProfile struct {
    ID    string `json:"id"`
    Name  string `json:"name"`
    Notes string `json:"notes,omitempty"` // paths redactados
    Steps int    `json:"steps"`
}

type SanitizedLauncherApp struct {
    ID             string `json:"id"`
    DisplayName    string `json:"displayName"`
    Category       string `json:"category"`
    LaunchMethod   string `json:"launchMethod"`
    Detected       bool   `json:"detected"`
    ExecutablePath string `json:"executablePath,omitempty"` // redacted
    Args           string `json:"args,omitempty"`            // redacted
    IsFavorite     bool   `json:"isFavorite"`
}
```

- [ ] **Step 4: Implement redactor for free text**

```go
var pathInTextRe = regexp.MustCompile(`[A-Za-z]:\\[^\s"']+`)

func redactPaths(s string) string {
    return pathInTextRe.ReplaceAllString(s, "[redacted]")
}
```

- [ ] **Step 5: Run the test to verify it passes**

Run: `go test ./internal/app/... -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/app/diagnostics_service.go internal/app/diagnostics_service_test.go
git commit -m "feat(launcher): sanitize new launcher fields and redact paths in notes/args (cut 0a)"
```

### Task 0a.4: Verify final del corte

- [ ] **Step 1: Run all Go tests**

Run: `go test ./internal/app/...`
Expected: PASS.

- [ ] **Step 2: Run frontend tests and lint**

Run: `pnpm --dir frontend test && pnpm --dir frontend lint && pnpm --dir frontend build`
Expected: PASS.

---

## Corte 0b — Save atómico aislado (con retry, sidecar, mutex, .bak)

### Task 0b.1: Save atómico con retry+backoff y rotación de .bak

**Files:**
- Modify: `internal/app/settings_service.go`
- Test: `internal/app/settings_service_test.go`

- [ ] **Step 1: Write the failing tests**

```go
func TestSaveIsAtomic(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    svc := app.NewSettingsService(path, nil)
    _ = svc.Load()
    s := app.DefaultAppSettings()
    if err := svc.Save(s); err != nil { t.Fatalf("save: %v", err) }
    if _, err := os.Stat(path); err != nil { t.Errorf("main should exist: %v", err) }
    s.DeltaMode = "relative"
    if err := svc.Save(s); err != nil { t.Fatalf("save 2: %v", err) }
    if _, err := os.Stat(path + ".bak"); err != nil { t.Errorf(".bak should exist after second save: %v", err) }
    if _, err := os.Stat(path + ".tmp"); !os.IsNotExist(err) { t.Errorf(".tmp should not exist after successful save") }
    if _, err := os.Stat(path + ".failed"); !os.IsNotExist(err) { t.Errorf(".failed should not exist on success") }
}

func TestSaveRetriesOnLock(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    svc := app.NewSettingsService(path, nil)
    _ = svc.Load()
    // Bloquear el rename: crear un directorio con el nombre del .tmp.
    // ... (uso de un seam en el save para inyectar el fallo; ver Step 3).
}
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `go test ./internal/app/... -run "TestSaveIsAtomic|TestSaveRetriesOnLock" -v`
Expected: FAIL.

- [ ] **Step 3: Implement saveLocked with retry+backoff and .bak rotation**

En `internal/app/settings_service.go`:

```go
type SettingsService struct {
    path     string
    mu       sync.Mutex
    settings *AppSettings
}

func (s *SettingsService) Save(settings *AppSettings) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    return s.saveLocked(settings, 0)
}

var saveBackoffs = []time.Duration{0, 100 * time.Millisecond, 500 * time.Millisecond, 1 * time.Second}

func (s *SettingsService) saveLocked(settings *AppSettings, attempt int) error {
    if s.path == "" { return ErrSettingsPathEmpty }
    data, err := json.MarshalIndent(settings, "", "  ")
    if err != nil { return fmt.Errorf("marshal: %w", err) }
    tmpPath := s.path + ".tmp"
    bakPath := s.path + ".bak"
    sidecarPath := s.path + ".failed"

    if err := os.WriteFile(tmpPath, data, 0o644); err != nil {
        return s.retryOrSidecar(settings, attempt, data, sidecarPath, fmt.Errorf("write tmp: %w", err))
    }
    if _, err := os.Stat(s.path); err == nil {
        _ = os.Rename(s.path, bakPath)
    }
    if err := os.Rename(tmpPath, s.path); err != nil {
        return s.retryOrSidecar(settings, attempt, data, sidecarPath, fmt.Errorf("rename: %w", err))
    }
    // Save exitoso: borrar sidecar si quedó.
    _ = os.Remove(sidecarPath)
    s.settings = settings
    return nil
}

func (s *SettingsService) retryOrSidecar(settings *AppSettings, attempt int, data []byte, sidecarPath string, lastErr error) error {
    if attempt+1 < len(saveBackoffs) {
        time.Sleep(saveBackoffs[attempt+1])
        return s.saveLocked(settings, attempt+1)
    }
    // Agotado: escribir sidecar con el payload (solo en fallo).
    _ = os.WriteFile(sidecarPath, data, 0o644)
    return fmt.Errorf("save failed after retries: %w", lastErr)
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `go test ./internal/app/... -run "TestSaveIsAtomic" -v`
Expected: PASS.

- [ ] **Step 5: Verify all existing tests pass**

Run: `go test ./internal/app/...`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/app/settings_service.go internal/app/settings_service_test.go
git commit -m "feat(launcher): atomic save with retry+backoff and .bak rotation (cut 0b)"
```

### Task 0b.2: Load tolerante a JSON corrupto con .bak + sidecar aplicado al arranque

**Files:**
- Modify: `internal/app/settings_service.go`
- Test: `internal/app/settings_service_test.go`

- [ ] **Step 1: Write the failing tests**

```go
func TestLoadToleratesCorruptedJSONFallsBackToBak(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    bakPath := path + ".bak"
    if err := os.WriteFile(path, []byte("{ \"deltaMode\":"), 0o644); err != nil { t.Fatal(err) }
    if err := os.WriteFile(bakPath, []byte(`{"schemaVersion": 1, "deltaMode": "self", "cpuSampling": true, "hotkeys": {}, "launcherApps": {}, "launcherProfiles": []}`), 0o644); err != nil { t.Fatal(err) }
    svc := app.NewSettingsService(path, nil)
    if err := svc.Load(); err != nil { t.Fatalf("load: %v", err) }
    if svc.Settings().DeltaMode != "self" {
        t.Errorf("expected DeltaMode=self from .bak, got %s", svc.Settings().DeltaMode)
    }
}

func TestLoadFallsBackToDefaultsOnTotalCorruption(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    bakPath := path + ".bak"
    os.WriteFile(path, []byte("garbage"), 0o644)
    os.WriteFile(bakPath, []byte("also garbage"), 0o644)
    svc := app.NewSettingsService(path, nil)
    if err := svc.Load(); err != nil { t.Fatalf("load should not panic: %v", err) }
    if svc.Settings().SchemaVersion != 1 { t.Errorf("expected defaults with SchemaVersion=1") }
    if svc.Settings().LauncherProfiles == nil { t.Error("expected default profiles") }
}

func TestSidecarAppliedOnStartup(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    sidecarPath := path + ".failed"
    if err := os.WriteFile(sidecarPath, []byte(`{"schemaVersion": 1, "deltaMode": "absolute", "cpuSampling": true, "hotkeys": {}, "launcherApps": {}, "launcherProfiles": [{"id":"x","name":"X","steps":[]}]}`), 0o644); err != nil { t.Fatal(err) }
    svc := app.NewSettingsService(path, nil)
    if err := svc.Load(); err != nil { t.Fatalf("load: %v", err) }
    if svc.Settings().DeltaMode != "absolute" { t.Errorf("expected sidecar applied, got %s", svc.Settings().DeltaMode) }
    if len(svc.Settings().LauncherProfiles) != 1 { t.Errorf("expected 1 profile from sidecar") }
    if _, err := os.Stat(sidecarPath); !os.IsNotExist(err) { t.Errorf("sidecar should be removed after applied") }
}
```

- [ ] **Step 2: Implement loadLocked with .bak and sidecar**

```go
func (s *SettingsService) Load() error {
    s.mu.Lock()
    defer s.mu.Unlock()
    return s.loadLocked()
}

func (s *SettingsService) loadLocked() error {
    s.settings = DefaultAppSettings()
    s.migrateSettings(s.settings)
    if s.path == "" { return nil }

    // 1. Sidecar tiene prioridad: si existe, aplicarlo y borrar.
    if sidecarData, err := os.ReadFile(s.path + ".failed"); err == nil {
        var sc AppSettings
        if err := json.Unmarshal(sidecarData, &sc); err == nil {
            s.settings = &sc
            s.migrateSettings(s.settings)
            _ = os.Remove(s.path + ".failed")
            // Persistir el sidecar aplicado (sin re-grabar en loadLocked, vía un write directo).
            return s.persistSidecarApplied()
        }
    }

    // 2. Intentar principal.
    data, err := os.ReadFile(s.path)
    if err != nil {
        if os.IsNotExist(err) { return nil } // defaults ya cargados.
        return fmt.Errorf("read: %w", err)
    }
    var loaded AppSettings
    if err := json.Unmarshal(data, &loaded); err != nil {
        // 3. Fallback a .bak.
        if bakData, bakErr := os.ReadFile(s.path + ".bak"); bakErr == nil {
            if err := json.Unmarshal(bakData, &loaded); err == nil {
                s.applyLoaded(&loaded)
                return nil
            }
        }
        // 4. Defaults.
        return nil
    }
    s.applyLoaded(&loaded)
    return nil
}

func (s *SettingsService) applyLoaded(loaded *AppSettings) {
    merged := DefaultAppSettings()
    // Merge explícito (copiar del archivo actual, línea ~128, no resumir).
    if loaded.DeltaMode != "" { merged.DeltaMode = loaded.DeltaMode }
    merged.CpuSampling = loaded.CpuSampling
    if loaded.Hotkeys != nil {
        merged.Hotkeys = make(map[string]string, len(loaded.Hotkeys))
        for k, v := range loaded.Hotkeys { merged.Hotkeys[k] = v }
    }
    if loaded.LauncherApps != nil {
        merged.LauncherApps = make(map[string]LauncherAppEntry, len(loaded.LauncherApps))
        for k, v := range loaded.LauncherApps { merged.LauncherApps[k] = v }
    }
    if loaded.LauncherProfiles != nil {
        merged.LauncherProfiles = make([]LaunchProfile, len(loaded.LauncherProfiles))
        copy(merged.LauncherProfiles, loaded.LauncherProfiles)
    }
    s.migrateSettings(merged)
    s.settings = merged
}

func (s *SettingsService) persistSidecarApplied() error {
    if s.settings == nil { return nil }
    data, err := json.MarshalIndent(s.settings, "", "  ")
    if err != nil { return err }
    tmpPath := s.path + ".tmp"
    bakPath := s.path + ".bak"
    if err := os.WriteFile(tmpPath, data, 0o644); err != nil { return err }
    if _, err := os.Stat(s.path); err == nil { _ = os.Rename(s.path, bakPath) }
    if err := os.Rename(tmpPath, s.path); err != nil { return err }
    return nil
}
```

- [ ] **Step 3: Run the tests to verify they pass**

Run: `go test ./internal/app/... -run "TestLoadTolerates|TestLoadFallsBack|TestSidecarApplied" -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/app/settings_service.go internal/app/settings_service_test.go
git commit -m "feat(launcher): tolerant Load with .bak and sidecar applied on startup (cut 0b)"
```

### Task 0b.3: Mutex de escritura (test de concurrencia)

**Files:**
- Modify: `internal/app/settings_service.go` (mutex ya añadido en 0b.1)
- Test: `internal/app/settings_service_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestSettingsWriteMutexSerializesConcurrentWrites(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "app-settings.json")
    svc := app.NewSettingsService(path, nil)
    _ = svc.Load()

    const N = 20
    var wg sync.WaitGroup
    errs := make(chan error, N)
    for i := 0; i < N; i++ {
        wg.Add(1)
        go func(i int) {
            defer wg.Done()
            s := app.DefaultAppSettings()
            s.LauncherApps = map[string]app.LauncherAppEntry{
                "k": {ID: "k", DisplayName: "K" + string(rune('a'+i%26)), Abbreviation: "K", Category: app.AppCategoryUtility, LaunchMethod: "executable", Detected: true, GradientFrom: "#000", GradientTo: "#fff"},
            }
            errs <- svc.Save(s)
        }(i)
    }
    wg.Wait()
    close(errs)
    for err := range errs {
        if err != nil { t.Errorf("concurrent save error: %v", err) }
    }
    // El archivo final debe ser JSON válido.
    data, err := os.ReadFile(path)
    if err != nil { t.Fatal(err) }
    var s app.AppSettings
    if err := json.Unmarshal(data, &s); err != nil { t.Errorf("final file must be valid JSON: %v", err) }
}
```

- [ ] **Step 2: Run the test to verify it passes (mutex ya está implementado)**

Run: `go test ./internal/app/... -run "TestSettingsWriteMutexSerializesConcurrentWrites" -v`
Expected: PASS (con el `sync.Mutex` añadido en 0b.1).

- [ ] **Step 3: Commit**

```bash
git add internal/app/settings_service_test.go
git commit -m "test(launcher): write mutex serializes concurrent saves (cut 0b)"
```

### Task 0b.4: Verify final del corte 0b

- [ ] **Step 1: Run all tests**

Run: `go test ./...`
Expected: PASS.

- [ ] **Step 2: Run frontend tests and build**

Run: `pnpm --dir frontend test && pnpm --dir frontend build && pnpm --dir frontend lint`
Expected: PASS.

---

## Corte 1 — Discovery fix + parser Steam + 7 apps catalogadas

### Task 1.1: Refactor readUninstallEntries a var compartido (seam primero)

**Files:**
- Create: `internal/app/launcher/registry_common.go`
- Modify: `internal/app/launcher/discovery_windows.go`

- [ ] **Step 1: Verificar tests existentes en verde**

Run: `go test ./internal/app/launcher/... -v`
Expected: PASS (baseline).

- [ ] **Step 2: Crear el helper como var (seam) en registry_common.go**

Crear `internal/app/launcher/registry_common.go`:

```go
package launcher

import "golang.org/x/sys/windows/registry"

// readUninstallEntries es un var (seam para tests) que lee el registro de Windows.
// Devuelve las entradas Uninstall de HKLM + WOW6432Node + HKCU.
var readUninstallEntries = func() []discoveredCandidate {
    var out []discoveredCandidate
    roots := []struct {
        root registry.Key
        path string
    }{
        {registry.LOCAL_MACHINE, `SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall`},
        {registry.LOCAL_MACHINE, `SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall`},
        {registry.CURRENT_USER, `SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall`},
    }
    for _, r := range roots {
        k, err := registry.OpenKey(r.root, r.path, registry.READ|registry.WOW64_64KEY)
        if err != nil { continue }
        names, _ := k.ReadSubKeyNames(-1)
        k.Close()
        for _, name := range names {
            sub, err := registry.OpenKey(r.root, r.path+`\`+name, registry.READ|registry.WOW64_64KEY)
            if err != nil { continue }
            dn, _, _ := sub.GetStringValue("DisplayName")
            loc, _, _ := sub.GetStringValue("InstallLocation")
            sub.Close()
            if dn == "" { continue }
            out = append(out, discoveredCandidate{DisplayName: dn, InstallLocation: loc})
        }
    }
    return out
}
```

- [ ] **Step 3: Refactor discovery_windows.go para usar el seam**

Reemplazar el cuerpo de `readUninstallRegistry` por `return readUninstallEntries()`. Eliminar la función local.

- [ ] **Step 4: Verify compile y tests existentes**

Run: `go test ./internal/app/launcher/... -v`
Expected: PASS (sin cambios funcionales).

- [ ] **Step 5: Commit**

```bash
git add internal/app/launcher/registry_common.go internal/app/launcher/discovery_windows.go
git commit -m "refactor(launcher): extract readUninstallEntries as injectable var (seam)"
```

### Task 1.2: Parser de libraryfolders.vdf

**Files:**
- Modify: `internal/app/launcher/discovery_windows.go`
- Test: `internal/app/launcher/discovery_windows_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestParseLibraryFolders(t *testing.T) {
    vdf := `"libraryfolders"
    {
        "0"
        {
            "path"        "C:\\Program Files (x86)\\Steam"
        }
        "1"
        {
            "path"        "D:\\SteamLibrary"
        }
    }`
    paths := parseLibraryFoldersVDF(vdf)
    if len(paths) != 2 { t.Fatalf("expected 2 paths, got %d", len(paths)) }
    if paths[0] != `C:\Program Files (x86)\Steam` { t.Errorf("path 0: %s", paths[0]) }
    if paths[1] != `D:\SteamLibrary` { t.Errorf("path 1: %s", paths[1]) }
}
```

- [ ] **Step 2: Run test, verify fails**

Run: `go test ./internal/app/launcher/... -run "TestParseLibraryFolders" -v`
Expected: FAIL.

- [ ] **Step 3: Implement parseLibraryFoldersVDF**

En `internal/app/launcher/discovery_windows.go`:

```go
import (
    "bufio"
    "regexp"
    "strings"
)

var vdfPathRe = regexp.MustCompile(`"path"\s+"([^"]+)"`)

func parseLibraryFoldersVDF(content string) []string {
    var paths []string
    scanner := bufio.NewScanner(strings.NewReader(content))
    for scanner.Scan() {
        m := vdfPathRe.FindStringSubmatch(scanner.Text())
        if m != nil {
            paths = append(paths, strings.ReplaceAll(m[1], `\\\\`, `\`))
        }
    }
    return paths
}
```

Modificar `readSteamLibraryFolders` para usar el parser.

- [ ] **Step 4: Run test, verify passes**

Run: `go test ./internal/app/launcher/... -run "TestParseLibraryFolders" -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/app/launcher/discovery_windows.go internal/app/launcher/discovery_windows_test.go
git commit -m "feat(launcher): implement libraryfolders.vdf parser (cut 1)"
```

### Task 1.3: Tests de discovery de las 7 apps catalogadas

**Files:**
- Test: `internal/app/launcher/discovery_windows_test.go`

- [ ] **Step 1: Write the failing test (con el seam ya creado en 1.1)**

```go
func TestDiscoverFindsAll7CatalogedApps(t *testing.T) {
    original := readUninstallEntries
    defer func() { readUninstallEntries = original }()
    readUninstallEntries = func() []discoveredCandidate {
        return []discoveredCandidate{
            {DisplayName: "Le Mans Ultimate", InstallLocation: ""},
            {DisplayName: "OBS Studio 30.0.0", InstallLocation: "C:\\Program Files\\obs-studio\\bin\\64bit"},
            {DisplayName: "CrewChiefV4", InstallLocation: "C:\\Users\\Test\\AppData\\Local\\CrewChief"},
            {DisplayName: "Discord", InstallLocation: "C:\\Users\\Test\\AppData\\Local\\Discord"},
            {DisplayName: "Spotify", InstallLocation: "C:\\Users\\Test\\AppData\\Roaming\\Spotify"},
            {DisplayName: "MoTeC i2", InstallLocation: "C:\\Program Files\\MoTeC"},
            {DisplayName: "SimHub", InstallLocation: "C:\\Program Files\\SimHub"},
        }
    }
    found := matchKnownApps(readUninstallEntries())
    expectedIDs := []string{"lmu", "obs", "crewchief", "discord", "spotify", "motec", "simhub"}
    for _, id := range expectedIDs {
        if _, ok := found[id]; !ok { t.Errorf("expected %s in discovered apps", id) }
    }
}

func TestDiscoverAlwaysIncludesLMUEvenWhenNotDetected(t *testing.T) {
    original := readUninstallEntries
    defer func() { readUninstallEntries = original }()
    readUninstallEntries = func() []discoveredCandidate { return nil }
    found := Discover()
    if _, ok := found["lmu"]; !ok { t.Fatal("lmu must always be present") }
    if found["lmu"].Detected { t.Error("lmu should be marked Detected=false when not found") }
}

func TestReadUninstallEntriesFiltersEmpty(t *testing.T) {
    original := readUninstallEntries
    defer func() { readUninstallEntries = original }()
    readUninstallEntries = func() []discoveredCandidate {
        return []discoveredCandidate{
            {DisplayName: "", InstallLocation: "C:\\Whatever"},
            {DisplayName: "Valid App", InstallLocation: "C:\\Valid"},
        }
    }
    out := readUninstallEntries()
    if len(out) != 2 { t.Fatalf("expected 2 (no filter at this layer), got %d", len(out)) }
    // El filtro de displayName vacío vive en readUninstallEntries; verificar que lo tiene.
}
```

- [ ] **Step 2: Run test, verify passes**

Run: `go test ./internal/app/launcher/... -run "TestDiscoverFindsAll7|TestDiscoverAlwaysIncludes|TestReadUninstallEntriesFiltersEmpty" -v`
Expected: PASS (el seam de 1.1 permite la inyección).

- [ ] **Step 3: Commit**

```bash
git add internal/app/launcher/discovery_windows_test.go
git commit -m "test(launcher): discovery regression for 7 cataloged apps (cut 1)"
```

### Task 1.4: Verify final del corte 1

- [ ] **Step 1: Run all Go tests**

Run: `go test ./internal/app/launcher/...`
Expected: PASS.

---

## Corte 2 — Menú Add Non-Steam Game (single-select)

### Task 2.1: Backend registry_windows.go con ListRegistryApps (exportado)

**Files:**
- Create: `internal/app/launcher/registry_windows.go`
- Test: `internal/app/launcher/registry_windows_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestListRegistryAppsReturnsAllDetected(t *testing.T) {
    original := readUninstallEntries
    defer func() { readUninstallEntries = original }()
    readUninstallEntries = func() []discoveredCandidate {
        return []discoveredCandidate{
            {DisplayName: "Spotify", InstallLocation: `C:\Users\Test\AppData\Roaming\Spotify`},
            {DisplayName: "Notepad", InstallLocation: ""},
        }
    }
    apps := launcher.ListRegistryApps()
    if len(apps) != 2 { t.Fatalf("expected 2 apps, got %d", len(apps)) }
    var spotify *launcher.RegistryApp
    for i := range apps {
        if apps[i].DisplayName == "Spotify" { spotify = &apps[i] }
    }
    if spotify == nil { t.Fatal("Spotify should be in the list") }
    if spotify.ExecutablePath == "" { t.Error("Spotify should have a path") }
}
```

- [ ] **Step 2: Run, verify fails**

Run: `go test ./internal/app/launcher/... -run "TestListRegistryApps" -v`
Expected: FAIL.

- [ ] **Step 3: Implement ListRegistryApps (exportado)**

Crear `internal/app/launcher/registry_windows.go`:

```go
package launcher

import "fmt"

type RegistryApp struct {
    ID             string `json:"id"`
    DisplayName    string `json:"displayName"`
    ExecutablePath string `json:"executablePath"`
}

// ListRegistryApps devuelve todas las apps instaladas en el sistema (leídas del registro de Windows).
// Es la fuente del modal "Add Non-Steam Game".
func ListRegistryApps() []RegistryApp {
    candidates := readUninstallEntries()
    out := make([]RegistryApp, 0, len(candidates))
    for i, c := range candidates {
        out = append(out, RegistryApp{
            ID:             fmt.Sprintf("registry-%d", i),
            DisplayName:    c.DisplayName,
            ExecutablePath: c.InstallLocation,
        })
    }
    return out
}
```

- [ ] **Step 4: Run, verify passes**

Run: `go test ./internal/app/launcher/... -run "TestListRegistryApps" -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/app/launcher/registry_windows.go internal/app/launcher/registry_windows_test.go
git commit -m "feat(launcher): ListRegistryApps for AddNonSteamGame modal (cut 2)"
```

### Task 2.2: Handler de launcher:registry:list + UpdateLauncherAppArgs + tests de main_test

**Files:**
- Modify: `cmd/vantare/main.go`
- Modify: `internal/app/settings_service.go`
- Test: `cmd/vantare/main_test.go`

- [ ] **Step 1: Write the failing test**

```go
func TestHandleRegistryList(t *testing.T) {
    // Setup con harness existente de main_test.go.
    // Verificar que el handler emite "launcher:registry:listed" con la lista de apps.
}

func TestHandleAppUpdate(t *testing.T) {
    // Setup con harness.
    // Emitir "launcher:app:update" con id y args.
    // Verificar que SettingsService.UpdateLauncherAppArgs fue llamado.
}
```

- [ ] **Step 2: Implementar UpdateLauncherAppArgs en settings_service.go**

```go
func (s *SettingsService) UpdateLauncherAppArgs(id, args string) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    if s.settings == nil { return ErrSettingsNotLoaded }
    app, ok := s.settings.LauncherApps[id]
    if !ok { return ErrAppNotFound }
    app.Args = args
    s.settings.LauncherApps[id] = app
    return s.saveLocked(s.settings, 0)
}
```

- [ ] **Step 3: Implementar los handlers en main.go**

```go
func (a *App) handleRegistryList(event wailsEvent) {
    apps := launcher.ListRegistryApps()
    a.emit("launcher:registry:listed", map[string]any{"apps": apps})
}

func (a *App) handleAppUpdate(event wailsEvent) {
    id, _ := event.Payload["id"].(string)
    args, _ := event.Payload["args"].(string)
    if err := a.settings.UpdateLauncherAppArgs(id, args); err != nil {
        a.logError("update app args", err); return
    }
    a.emit("launcher:apps:updated", map[string]any{"apps": a.settings.GetLauncherApps()})
}
```

Registrar en el wiring:

```go
case "launcher:registry:list": a.handleRegistryList(event)
case "launcher:app:update": a.handleAppUpdate(event)
```

- [ ] **Step 4: Run tests, verify pass**

Run: `go test ./cmd/vantare/... -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add cmd/vantare/main.go internal/app/settings_service.go cmd/vantare/main_test.go
git commit -m "feat(launcher): registry:list + app:update handlers + UpdateLauncherAppArgs (cut 2)"
```

### Task 2.3: AddNonSteamGameModal frontend (single-select, pre-lowercase, cap 200)

**Files:**
- Modify: `frontend/package.json` (añade motion)
- Create: `frontend/src/hub/launcher/AddNonSteamGameModal.tsx`
- Create: `frontend/src/hub/launcher/AddNonSteamGameModal.test.tsx`

- [ ] **Step 1: Install motion**

Run: `pnpm --dir frontend add motion`
Expected: `motion` en `package.json`.

- [ ] **Step 2: Write the failing test con assertions reales**

```tsx
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { AddNonSteamGameModal } from "./AddNonSteamGameModal";
import { setupWailsMock, teardownWailsMock } from "../../lib/wails-runtime-test-utils";

describe("AddNonSteamGameModal", () => {
  beforeEach(() => setupWailsMock());
  afterEach(() => teardownWailsMock());

  it("renders cap of 200 matches when search is empty (250 registry apps)", async () => {
    const mockApps = Array.from({ length: 250 }, (_, i) => ({
      id: `r-${i}`, displayName: `App ${i}`, executablePath: `C:\\P\\A${i}.exe`,
    }));
    (globalThis as any).window.__WAILS_LISTENERS__["launcher:registry:listed"] = (cb: any) => {
      setTimeout(() => cb({ data: { apps: mockApps } }), 0);
      return () => {};
    };
    render(<AddNonSteamGameModal open={true} onClose={() => {}} onAdd={() => {}} />);
    await waitFor(() => {
      expect(screen.getAllByTestId(/^add-non-steam-row-/)).toHaveLength(200);
    });
    expect(screen.getByText(/Refina la búsqueda para ver más \(50 resultados más\)/)).toBeInTheDocument();
  });

  it("single-select: clicking a row highlights it; second click deselects", async () => {
    // ... (mock con 3 apps, click row 0, assert highlighted, click row 1, assert row 0 no highlighted, row 1 sí)
  });

  it("calls onAdd with the selected entry and closes", async () => {
    // ... (mock, click row, click Añadir, assert onAdd called with right entry)
  });

  it("Browse button is present", () => {
    // render, assert "Browse..." button exists
  });
});
```

(Nota: `setupWailsMock`/`teardownWailsMock` se crean en `frontend/src/lib/wails-runtime-test-utils.ts` para abstraer el mock; el worker los añade si no existen.)

- [ ] **Step 3: Implementar el modal**

```tsx
import { useEffect, useMemo, useState } from "react";
import { Events } from "@wailsio/runtime";

type RegistryApp = { id: string; displayName: string; executablePath: string };
type Props = { open: boolean; onClose: () => void; onAdd: (entry: { displayName: string; executablePath: string }) => void; };

const CAP = 200;

export function AddNonSteamGameModal({ open, onClose, onAdd }: Props) {
  const [apps, setApps] = useState<RegistryApp[]>([]);
  const [loading, setLoading] = useState(false);
  const [search, setSearch] = useState("");
  const [selectedId, setSelectedId] = useState<string | null>(null);

  useEffect(() => {
    if (!open) return;
    setLoading(true);
    Events.Emit("launcher:registry:list");
    const off = Events.On("launcher:registry:listed", (e: any) => {
      setApps(e.data?.apps ?? []);
      setLoading(false);
    });
    return () => { off(); };
  }, [open]);

  const lowercased = useMemo(
    () => apps.map((a) => ({ ...a, _lower: a.displayName.toLowerCase() + "|" + a.executablePath.toLowerCase() })),
    [apps]
  );

  const filtered = useMemo(() => {
    const q = search.toLowerCase().trim();
    return q ? lowercased.filter((a) => a._lower.includes(q)) : lowercased;
  }, [lowercased, search]);

  const visible = filtered.slice(0, CAP);
  const moreCount = filtered.length - visible.length;

  const handleAdd = () => {
    const selected = apps.find((a) => a.id === selectedId);
    if (selected) { onAdd({ displayName: selected.displayName, executablePath: selected.executablePath }); onClose(); }
  };

  if (!open) return null;
  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm" data-testid="add-non-steam-modal">
      <div className="card-sleek rounded-xl w-[640px] max-h-[80vh] flex flex-col">
        <header className="p-4 border-b border-white/10">
          <h2 className="text-lg font-bold text-white">Añadir programa</h2>
        </header>
        <div className="p-3">
          <input
            type="search"
            value={search}
            onChange={(e) => setSearch(e.target.value)}
            placeholder="Buscar..."
            className="w-full rounded-md bg-black/40 border border-white/10 px-2 py-1 text-sm text-white"
            data-testid="add-non-steam-search"
          />
        </div>
        <div className="flex-1 overflow-y-auto" data-testid="add-non-steam-list">
          {loading ? <div className="p-4 text-xs text-vantare-textDim">Cargando...</div> : (
            <table className="w-full text-sm">
              <tbody>
                {visible.map((a) => (
                  <tr key={a.id} onClick={() => setSelectedId(selectedId === a.id ? null : a.id)}
                      className={selectedId === a.id ? "bg-accent/20 cursor-pointer" : "hover:bg-white/5 cursor-pointer"}
                      data-testid={`add-non-steam-row-${a.id}`}>
                    <td className="p-2">{a.displayName}</td>
                    <td className="p-2 text-xs text-vantare-textDim">{a.executablePath}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          )}
          {moreCount > 0 && (
            <div className="p-3 text-xs text-vantare-textMuted border-t border-white/5">
              Refina la búsqueda para ver más ({moreCount} resultados más).
            </div>
          )}
        </div>
        <footer className="p-3 border-t border-white/10 flex justify-end gap-2">
          <button onClick={onBrowse} data-testid="add-non-steam-browse" className="mr-auto px-3 py-1.5 rounded-lg border border-white/10 text-[10px] uppercase tracking-[.18em] text-vantare-textMuted hover:text-white">Browse...</button>
          <button onClick={onClose} data-testid="add-non-steam-cancel" className="px-3 py-1.5 rounded-lg text-[10px] uppercase tracking-[.18em] text-vantare-textDim hover:text-white">Cancelar</button>
          <button onClick={handleAdd} disabled={!selectedId} data-testid="add-non-steam-add" className="px-3 py-1.5 rounded-lg bg-accent text-[10px] uppercase tracking-[.18em] font-bold text-black hover:opacity-90 disabled:opacity-40">Añadir</button>
        </footer>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Run, verify pass**

Run: `pnpm --dir frontend test AddNonSteamGameModal -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/AddNonSteamGameModal.tsx frontend/src/hub/launcher/AddNonSteamGameModal.test.tsx frontend/package.json
git commit -m "feat(launcher): AddNonSteamGameModal single-select + cap 200 (cut 2)"
```

### Task 2.4: Integrar modal en AppsPanel + panel de detalles con args editable

**Files:**
- Modify: `frontend/src/hub/launcher/AppsPanel.tsx`
- Test: `frontend/src/hub/launcher/AppsPanel.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { AppsPanel } from "./AppsPanel";

it("opens AddNonSteamGameModal on add button click", () => {
  render(<AppsPanel />);
  fireEvent.click(screen.getByTestId("apps-add-manual"));
  expect(screen.getByTestId("add-non-steam-modal")).toBeInTheDocument();
});

it("renders details panel with args input when clicking on an app", () => {
  // mock apps, click on one, assert path/args inputs appear
});
```

- [ ] **Step 2: Integrate the modal and details panel**

```tsx
import { AddNonSteamGameModal } from "./AddNonSteamGameModal";

const [showAdd, setShowAdd] = useState(false);
const [detailsAppId, setDetailsAppId] = useState<string | null>(null);

<button onClick={() => setShowAdd(true)} data-testid="apps-add-manual">+ {t("launcher.apps.addManual")}</button>

<AddNonSteamGameModal
  open={showAdd}
  onClose={() => setShowAdd(false)}
  onAdd={(entry) => {
    const newEntry: LauncherAppEntry = {
      id: newProfileId("app"),
      displayName: entry.displayName,
      abbreviation: entry.displayName.slice(0, 3).toUpperCase(),
      category: "utility",
      launchMethod: "executable",
      executablePath: entry.executablePath,
      detected: false,
      gradientFrom: "#6b7280", gradientTo: "#1f2937",
    };
    Events.Emit("launcher:app:add", { entry: newEntry });
  }}
/>

{detailsAppId && (() => {
  const app = apps.find((a) => a.id === detailsAppId);
  if (!app) return null;
  return (
    <div className="mt-2 p-3 bg-black/30 rounded-md text-xs" data-testid={`app-details-${app.id}`}>
      <div>Path: <code>{app.executablePath}</code></div>
      <label className="block mt-1">Args:
        <input type="text" value={app.args ?? ""}
          onChange={(e) => Events.Emit("launcher:app:update", { id: app.id, args: e.target.value })}
          data-testid={`app-args-input-${app.id}`}
          className="ml-2 rounded bg-black/40 border border-white/10 px-2 py-0.5 text-white" />
      </label>
    </div>
  );
})()}
```

- [ ] **Step 3: Run, verify pass**

Run: `pnpm --dir frontend test AppsPanel -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/launcher/AppsPanel.tsx frontend/src/hub/launcher/AppsPanel.test.tsx
git commit -m "feat(launcher): integrate AddNonSteamGameModal + args editable in details (cut 2)"
```

### Task 2.5: Visual compare del modal

- [ ] **Step 1: Create launcher-visual-compare.mjs**

Siguiendo el patrón de `widget-studio-visual-compare.mjs`. Captura: modal abierto, modal con búsqueda, modal con selección.

- [ ] **Step 2: Run**

Run: `cd frontend && node scripts/launcher-visual-compare.mjs`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add frontend/scripts/launcher-visual-compare.mjs
git commit -m "test(launcher): visual compare for AddNonSteamGameModal (cut 2)"
```

---

## Corte 3 — Favoritos y notas (con ProfileEditor side-panel completo)

### Task 3.1: Toggle favorita en AppBadge y AppRow

**Files:**
- Modify: `frontend/src/hub/components/AppBadge.tsx`
- Test: `frontend/src/hub/components/AppBadge.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { AppBadge } from "./AppBadge";

it("renders favorite toggle that emits update event on click", () => {
  const app = { id: "obs", displayName: "OBS", abbreviation: "OBS", category: "streaming" as const, launchMethod: "executable" as const, detected: true, gradientFrom: "#000", gradientTo: "#fff", isFavorite: false };
  const onFavorite = jest.fn();
  render(<AppBadge app={app} onFavorite={onFavorite} />);
  fireEvent.click(screen.getByTestId("app-favorite-obs"));
  expect(onFavorite).toHaveBeenCalledWith("obs", true);
});
```

- [ ] **Step 2: Implement favorite toggle**

```tsx
<button
  onClick={() => onFavorite(app.id, !app.isFavorite)}
  data-testid={`app-favorite-${app.id}`}
  aria-label={app.isFavorite ? "Quitar de favoritas" : "Marcar como favorita"}
>
  {app.isFavorite ? "★" : "☆"}
</button>
```

- [ ] **Step 3: Wire in AppsPanel**

```tsx
<button
  onClick={() => Events.Emit("launcher:app:favorite", { id: app.id, isFavorite: !app.isFavorite })}
  // ... (propagar al backend en Task 3.4)
/>
```

- [ ] **Step 4: Commit (parcial; el wiring de main.go va en Task 7.5)**

```bash
git add frontend/src/hub/components/AppBadge.tsx frontend/src/hub/components/AppBadge.test.tsx frontend/src/hub/launcher/AppsPanel.tsx
git commit -m "feat(launcher): favorite toggle on app badge (cut 3, frontend only)"
```

### Task 3.2: ProfileCard con badge favorita y orden en grilla

**Files:**
- Modify: `frontend/src/hub/launcher/ProfileCard.tsx`
- Modify: `frontend/src/hub/launcher/ProfilesPanel.tsx`
- Test: `frontend/src/hub/launcher/ProfileCard.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
it("renders favorite badge when profile.isFavorite is true", () => {
  const profile = { id: "creator", name: "Creator", steps: [], isFavorite: true };
  render(<ProfileCard profile={profile} apps={[]} />);
  expect(screen.getByTestId("profile-favorite-badge-creator")).toBeInTheDocument();
});
```

- [ ] **Step 2: Implement favorite badge in ProfileCard header**

```tsx
{profile.isFavorite && (
  <span className="text-amber-400 ml-2" data-testid={`profile-favorite-badge-${profile.id}`} aria-label="Favorita">★</span>
)}
```

- [ ] **Step 3: Implement sort in ProfilesPanel (sin sección separada)**

```tsx
const orderedProfiles = useMemo(
  () => [...profiles].sort((a, b) => {
    if (a.isFavorite && !b.isFavorite) return -1;
    if (!a.isFavorite && b.isFavorite) return 1;
    return a.name.localeCompare(b.name);
  }),
  [profiles]
);
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/launcher/ProfileCard.tsx frontend/src/hub/launcher/ProfilesPanel.tsx frontend/src/hub/launcher/ProfileCard.test.tsx
git commit -m "feat(launcher): favorite badge + sort in grid (no separate section) (cut 3)"
```

### Task 3.3a: ProfileEditor side-panel — campos base (name, description, notes)

**Files:**
- Modify: `frontend/src/hub/launcher/ProfileEditor.tsx`
- Test: `frontend/src/hub/launcher/ProfileEditor.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
it("renders side-panel with name, description, notes fields", () => {
  const profile = { id: "p1", name: "P1", description: "d", steps: [], notes: "n" };
  render(<ProfileEditor profile={profile} open={true} onClose={() => {}} onSave={() => {}} />);
  expect(screen.getByTestId("profile-editor-name")).toHaveValue("P1");
  expect(screen.getByTestId("profile-editor-description")).toHaveValue("d");
  expect(screen.getByTestId("profile-editor-notes")).toHaveValue("n");
});
```

- [ ] **Step 2: Implement side-panel with name/description/notes**

```tsx
import { motion, AnimatePresence } from "motion/react";

export function ProfileEditor({ profile, open, onClose, onSave }: Props) {
  const [draft, setDraft] = useState<LaunchProfile | null>(null);
  useEffect(() => { setDraft(profile); }, [profile]);
  if (!draft) return null;

  return (
    <AnimatePresence>
      {open && (
        <>
          <motion.div className="fixed inset-0 z-40 bg-black/40" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={onClose} />
          <motion.aside
            className="fixed right-0 top-0 bottom-0 w-[480px] z-50 card-sleek p-5 overflow-y-auto"
            initial={{ x: "100%" }} animate={{ x: 0 }} exit={{ x: "100%" }}
            transition={{ duration: 0.25, ease: [0.16, 1, 0.3, 1] }}
            data-testid="profile-editor-panel"
            role="dialog" aria-label="Editar perfil"
          >
            <h2 className="text-lg font-bold text-white">Editar perfil</h2>
            <label className="block mt-3">
              <span className="text-[10px] uppercase tracking-[.18em] text-vantare-textDim">Nombre</span>
              <input value={draft.name} onChange={(e) => setDraft({ ...draft, name: e.target.value })}
                data-testid="profile-editor-name"
                className="w-full rounded-md bg-black/40 border border-white/10 px-2 py-1 text-sm text-white" />
            </label>
            <label className="block mt-3">
              <span className="text-[10px] uppercase tracking-[.18em] text-vantare-textDim">Descripción</span>
              <input value={draft.description ?? ""} onChange={(e) => setDraft({ ...draft, description: e.target.value })}
                data-testid="profile-editor-description"
                className="w-full rounded-md bg-black/40 border border-white/10 px-2 py-1 text-sm text-white" />
            </label>
            <label className="block mt-3">
              <span className="text-[10px] uppercase tracking-[.18em] text-vantare-textDim">Notas</span>
              <textarea value={draft.notes ?? ""} onChange={(e) => setDraft({ ...draft, notes: e.target.value })}
                data-testid="profile-editor-notes" rows={3}
                className="w-full rounded-md bg-black/40 border border-white/10 px-2 py-1 text-sm text-white" />
            </label>
            {/* Steps, hotkey, autostart van en Task 3.3b */}
            <div className="mt-4 flex justify-end gap-2">
              <button onClick={onClose} data-testid="profile-editor-cancel" className="px-3 py-1.5 rounded-lg text-[10px] uppercase tracking-[.18em] text-vantare-textDim hover:text-white">Cancelar</button>
              <button onClick={() => onSave(draft)} data-testid="profile-editor-save" className="px-3 py-1.5 rounded-lg bg-accent text-[10px] uppercase tracking-[.18em] font-bold text-black hover:opacity-90">Guardar</button>
            </div>
          </motion.aside>
        </>
      )}
    </AnimatePresence>
  );
}
```

- [ ] **Step 3: Run, verify pass**

Run: `pnpm --dir frontend test ProfileEditor -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/launcher/ProfileEditor.tsx frontend/src/hub/launcher/ProfileEditor.test.tsx
git commit -m "feat(launcher): ProfileEditor side-panel base (name, description, notes) (cut 3)"
```

### Task 3.3b: ProfileEditor side-panel — steps, hotkey, autostart

**Files:**
- Modify: `frontend/src/hub/launcher/ProfileEditor.tsx`
- Test: `frontend/src/hub/launcher/ProfileEditor.test.tsx`

- [ ] **Step 1: Write the failing test**

```tsx
it("renders steps editor with add/remove/reorder", () => { /* ... */ });
it("renders hotkey input with validation against reserved combos", () => { /* ... */ });
it("renders autostart toggle, disabled when profile has no valid steps", () => { /* ... */ });
```

- [ ] **Step 2: Add steps editor + hotkey + autostart**

Añadir al side-panel:

```tsx
{/* Steps editor */}
<div className="mt-4">
  <span className="text-[10px] uppercase tracking-[.18em] text-vantare-textDim">Pasos</span>
  {draft.steps.map((step, i) => (
    <div key={i} className="flex items-center gap-2 mt-2" data-testid={`editor-step-${i}`}>
      <select value={step.appId} onChange={(e) => {
        const next = [...draft.steps]; next[i] = { ...step, appId: e.target.value }; setDraft({ ...draft, steps: next });
      }} data-testid={`editor-step-app-${i}`} className="flex-1 rounded-md bg-black/40 border border-white/10 px-2 py-1 text-sm text-white">
        <option value="">— Selecciona app —</option>
        {apps.map((a) => <option key={a.id} value={a.id}>{a.displayName}</option>)}
      </select>
      <input type="number" min={0} value={step.delay} onChange={(e) => {
        const next = [...draft.steps]; next[i] = { ...step, delay: Number(e.target.value) || 0 }; setDraft({ ...draft, steps: next });
      }} data-testid={`editor-step-delay-${i}`} className="w-16 rounded-md bg-black/40 border border-white/10 px-2 py-1 text-sm text-white" />
      <button onClick={() => { const next = draft.steps.filter((_, idx) => idx !== i); setDraft({ ...draft, steps: next }); }}
        data-testid={`editor-step-remove-${i}`} aria-label="Eliminar paso" className="px-1 text-vantare-textDim hover:text-vantare-red-400">✕</button>
    </div>
  ))}
  <button onClick={() => setDraft({ ...draft, steps: [...draft.steps, { appId: "", delay: 2 }] })}
    data-testid="editor-step-add" className="mt-2 px-3 py-1.5 rounded-lg border border-dashed border-white/10 text-[10px] uppercase tracking-[.18em] text-vantare-textMuted hover:border-accent/40">+ Añadir paso</button>
</div>

{/* Hotkey */}
<label className="block mt-4">
  <span className="text-[10px] uppercase tracking-[.18em] text-vantare-textDim">Hotkey global</span>
  <input type="text" value={draft.hotkey ?? ""}
    onChange={(e) => setDraft({ ...draft, hotkey: e.target.value || undefined })}
    data-testid="profile-editor-hotkey" placeholder="ctrl+shift+1 (vacío = sin hotkey)"
    className="w-full rounded-md bg-black/40 border border-white/10 px-2 py-1 text-sm text-white" />
  {draft.hotkey && !isHotkeyAllowed(draft.hotkey) && (
    <span className="text-xs text-vantare-red-400" data-testid="profile-editor-hotkey-error">Combinación reservada</span>
  )}
</label>

{/* Autostart */}
<label className="flex items-center gap-2 mt-4">
  <input type="checkbox" checked={draft.launchOnWindowsStartup ?? false}
    disabled={draft.steps.length === 0}
    onChange={(e) => setDraft({ ...draft, launchOnWindowsStartup: e.target.checked })}
    data-testid="profile-editor-autostart" />
  <span className="text-sm text-white">Iniciar con Windows</span>
</label>
```

Importar `isHotkeyAllowed` desde `launcher-state.ts` (helper añadido en Task 7.2).

- [ ] **Step 3: Commit**

```bash
git add frontend/src/hub/launcher/ProfileEditor.tsx frontend/src/hub/launcher/ProfileEditor.test.tsx
git commit -m "feat(launcher): ProfileEditor steps + hotkey + autostart (cut 3)"
```

### Task 3.4: i18n keys for favorites + notes (parcial; resto en Task 7.6)

- [ ] **Step 1: Add i18n keys**

En `frontend/src/i18n/locales/{es,en,pt,it}.ts`:
- `launcher.favorite`
- `launcher.notes`
- `launcher.modal.addNonSteam.title` (adelantado, se usa en Task 2.3)
- `launcher.modal.addNonSteam.search`
- `launcher.modal.addNonSteam.browse`
- `launcher.modal.addNonSteam.moreResults` (template: "Refina para ver más ({n} más)")

- [ ] **Step 2: Add parity test in i18n.test.ts**

```ts
it("new launcher keys are present in all 4 locales", () => {
  for (const locale of ["es", "en", "pt", "it"] as const) {
    const t = useI18nForTest(locale);
    expect(t("launcher.favorite")).toBeTruthy();
    expect(t("launcher.notes")).toBeTruthy();
    // ...
  }
});
```

- [ ] **Step 3: Run, verify pass**

Run: `pnpm --dir frontend test i18n -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/i18n/
git commit -m "feat(launcher): i18n keys for favorites + notes + modal (cut 3)"
```

### Task 3.5: Verify final del corte 3

- [ ] **Step 1: Run all tests**

Run: `pnpm --dir frontend test && go test ./...`
Expected: PASS.

---

## Corte 4 — Telemetría (backend-only) + ChainRunner con backend

### Task 4.1: ProfilesBackend interface y fakeProfilesBackend

**Files:**
- Modify: `internal/app/launcher/profiles.go` (o crear `internal/app/launcher/telemetry.go`)
- Test: `internal/app/launcher/profiles_test.go`

- [ ] **Step 1: Write the failing test que usa la interfaz (sin definirla explícitamente en el plan)**

El test usa `fakeProfilesBackend` que implementa la interfaz. Primero lo escribimos, vemos que falla, y luego definimos la interfaz.

```go
package launcher

import (
    "errors"
    "testing"
)

func TestRecordProfileAttemptAlwaysIncrementsEvenOnFailure(t *testing.T) {
    backend := &fakeProfilesBackend{
        apps: map[string]LauncherAppEntry{"lmu": {ID: "lmu", DisplayName: "LMU", Abbreviation: "LMU", Category: AppCategorySimulator, LaunchMethod: "steam-uri", Detected: true, GradientFrom: "#f00", GradientTo: "#000"}},
        profiles: []LaunchProfile{{ID: "p1", Name: "P1", Steps: []LaunchStep{{AppID: "lmu", Delay: 0}}}},
    }
    if err := RecordProfileAttempt(backend, "p1"); err != nil { t.Fatalf("attempt: %v", err) }
    if backend.profiles[0].LaunchCount != 1 { t.Errorf("count should be 1, got %d", backend.profiles[0].LaunchCount) }
    if backend.profiles[0].LastLaunchedAt == nil { t.Error("LastLaunchedAt should be set") }
    // Simular otro intento (que en producción representa una cadena fallida).
    if err := RecordProfileAttempt(backend, "p1"); err != nil { t.Fatalf("attempt 2: %v", err) }
    if backend.profiles[0].LaunchCount != 2 { t.Errorf("count should be 2 even on simulated failure path, got %d", backend.profiles[0].LaunchCount) }
}

func TestRecordProfileSuccessUpdatesEMAOnly(t *testing.T) {
    backend := &fakeProfilesBackend{
        apps: map[string]LauncherAppEntry{"lmu": {}},
        profiles: []LaunchProfile{{ID: "p1", Name: "P1", Steps: []LaunchStep{{AppID: "lmu", Delay: 0}}}},
    }
    if err := RecordProfileSuccess(backend, "p1", 10000); err != nil { t.Fatal(err) }
    if backend.profiles[0].AvgChainDurationMs != 10000 { t.Errorf("first success: expected 10000, got %d", backend.profiles[0].AvgChainDurationMs) }
    if err := RecordProfileSuccess(backend, "p1", 20000); err != nil { t.Fatal(err) }
    // EMA: 0.3*20000 + 0.7*10000 = 13000
    if backend.profiles[0].AvgChainDurationMs != 13000 { t.Errorf("EMA: expected 13000, got %d", backend.profiles[0].AvgChainDurationMs) }
}

func TestRecordProfileAttemptFailsOnUnknownProfile(t *testing.T) {
    backend := &fakeProfilesBackend{apps: map[string]LauncherAppEntry{}, profiles: nil}
    if err := RecordProfileAttempt(backend, "ghost"); !errors.Is(err, ErrProfileNotFound) {
        t.Errorf("expected ErrProfileNotFound, got %v", err)
    }
}
```

- [ ] **Step 2: Run, verify fail (interfaz no existe)**

Run: `go test ./internal/app/launcher/... -run "TestRecordProfile" -v`
Expected: FAIL.

- [ ] **Step 3: Implement ProfilesBackend interface + fakeProfilesBackend + RecordProfileAttempt/Success**

En `internal/app/launcher/telemetry.go` (nuevo):

```go
package launcher

import "time"

const emaAlpha = 0.3

// ProfilesBackend es el contrato que RecordProfileAttempt/Success necesitan de SettingsService.
// SettingsService ya implementa estos métodos (verificados en spec §3.3).
type ProfilesBackend interface {
    GetLauncherProfiles() []LaunchProfile
    SetLauncherProfiles([]LaunchProfile) error
    GetLauncherApps() map[string]LauncherAppEntry
}

// RecordProfileAttempt se llama SIEMPRE que el usuario lanza una cadena (éxito o fallo).
func RecordProfileAttempt(backend ProfilesBackend, profileID string) error {
    profiles := backend.GetLauncherProfiles()
    idx := -1
    for i := range profiles {
        if profiles[i].ID == profileID { idx = i; break }
    }
    if idx == -1 { return ErrProfileNotFound }
    profiles[idx].LaunchCount++
    now := time.Now().UTC()
    profiles[idx].LastLaunchedAt = &now
    return backend.SetLauncherProfiles(profiles)
}

// RecordProfileSuccess se llama SOLO si success=true.
func RecordProfileSuccess(backend ProfilesBackend, profileID string, durationMs int64) error {
    profiles := backend.GetLauncherProfiles()
    idx := -1
    for i := range profiles {
        if profiles[i].ID == profileID { idx = i; break }
    }
    if idx == -1 { return ErrProfileNotFound }
    p := &profiles[idx]
    if p.AvgChainDurationMs == 0 {
        p.AvgChainDurationMs = durationMs
    } else {
        p.AvgChainDurationMs = int64(emaAlpha*float64(durationMs) + (1-emaAlpha)*float64(p.AvgChainDurationMs))
    }
    return backend.SetLauncherProfiles(profiles)
}

// fakeProfilesBackend para tests.
type fakeProfilesBackend struct {
    apps     map[string]LauncherAppEntry
    profiles []LaunchProfile
}

func (f *fakeProfilesBackend) GetLauncherProfiles() []LaunchProfile { return f.profiles }
func (f *fakeProfilesBackend) SetLauncherProfiles(p []LaunchProfile) error {
    f.profiles = append([]LaunchProfile(nil), p...)
    return nil
}
func (f *fakeProfilesBackend) GetLauncherApps() map[string]LauncherAppEntry { return f.apps }
```

- [ ] **Step 4: Run, verify pass**

Run: `go test ./internal/app/launcher/... -run "TestRecordProfile" -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/app/launcher/telemetry.go internal/app/launcher/profiles_test.go
git commit -m "feat(launcher): RecordProfileAttempt/Success with EMA + ProfilesBackend (cut 4)"
```

### Task 4.2: ChainRunner con backend, liveness probe, PIDs, active map con rechazo

**Files:**
- Modify: `internal/app/launcher/chain.go`
- Test: `internal/app/launcher/chain_test.go`

- [ ] **Step 1: Write the failing tests (5 tests reales)**

```go
func TestChainRunnerMeasuresWallClock(t *testing.T) {
    // exec stub que duerme 100ms; verificar que startedAt/finishedAt reflejan wall-clock.
}

func TestChainRunnerCancellationStopsAtStepBoundary(t *testing.T) {
    // Lanzar con ctx cancelable; cancelar tras step 0; verificar que step 1 queda pending.
}

func TestChainRunnerRejectsDoubleLaunch(t *testing.T) {
    // Primer StartChain OK; segundo con mismo profileID devuelve error "perfil ya en curso".
}

func TestChainRunnerFailureDoesNotUpdateAvgButUpdatesCount(t *testing.T) {
    // exec stub que falla; verificar que RecordProfileAttempt se llama pero RecordProfileSuccess NO.
}

func TestChainRunnerLivenessProbeCatchesCrash(t *testing.T) {
    // exec stub que arranca y muere en 1s con código 1; verificar que el step se marca failed.
}
```

(Los tests usan el `fakeProfilesBackend` de 4.1 y un `fakeExecLauncher` que el worker define.)

- [ ] **Step 2: Run, verify fail**

Run: `go test ./internal/app/launcher/... -run "TestChainRunner" -v`
Expected: FAIL.

- [ ] **Step 3: Reescribir chain.go con todos los fixes**

El nuevo `chain.go` debe:
1. Aceptar `backend ProfilesBackend` en `NewChainRunner`.
2. Mantener `active map[profileID]context.CancelFunc` con rechazo si ya existe.
3. Por cada step:
   - sleep con `select` sobre `ctx.Done()`.
   - `time.Now()` → `startedAt`.
   - `cmd := exec.Command(...)`, `Start()`, registrar `pid := cmd.Process.Pid`.
   - Emitir `ChainProgress` con `Status: "launching"`, `StartedAt`, `Pid`.
   - Liveness probe en goroutine: `cmd.Wait()` con `select` sobre timeout 3s. Si exit code ≠0 o error → emitir `Status: "failed"`. Si timeout → emitir `Status: "done"`.
4. Cerrar la cadena:
   - `RecordProfileAttempt(backend, profileID)` SIEMPRE.
   - `RecordProfileSuccess(backend, profileID, durationMs)` SOLO si success.
   - Emitir `chain:done` con `success`.
5. Reconciliar el schema de evento con el store frontend: `ChainProgress` con `Status: "pending"|"launching"|"done"|"failed"`, `StartedAt int64`, `FinishedAt int64`, `Pid int`, `Message string`.
6. `CancelAll()` para el hook de cierre de Wails.

```go
type ChainProgress struct {
    ProfileID  string `json:"profileId"`
    StepIndex  int    `json:"stepIndex"`
    AppID      string `json:"appId"`
    Status     string `json:"status"` // "pending"|"launching"|"done"|"failed"
    StartedAt  int64  `json:"startedAt,omitempty"`
    FinishedAt int64  `json:"finishedAt,omitempty"`
    Pid        int    `json:"pid,omitempty"`
    Message    string `json:"message,omitempty"`
}
```

- [ ] **Step 4: Run, verify pass**

Run: `go test ./internal/app/launcher/... -run "TestChainRunner" -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/app/launcher/chain.go internal/app/launcher/chain_test.go
git commit -m "feat(launcher): chain runner with backend, liveness probe, PIDs, double-launch rejection (cut 4)"
```

### Task 4.3: Verify final del corte 4

- [ ] **Step 1: Run all Go tests**

Run: `go test ./internal/app/launcher/...`
Expected: PASS.

- [ ] **Step 2: Run frontend tests**

Run: `pnpm --dir frontend test`
Expected: PASS (frontend sin cambios en este corte, pero la suite debe seguir verde).

---

## Corte 5 — Cadena en vivo (store, profile card, liveness UI, toast, provider)

### Task 5.1a: Store externo — tipos + applyStep + handleStep/handleDone

**Files:**
- Create: `frontend/src/hub/launcher/chain-store.ts`
- Create: `frontend/src/hub/launcher/chain-store.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
import { createChainStore } from "./chain-store";

describe("chain-store reducer", () => {
  it("updates on step event", () => {
    const store = createChainStore();
    store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "launching", startedAt: 1000 });
    expect(store.getChain("p1")?.steps[0].status).toBe("launching");
  });

  it("is tolerant to out-of-order: done before launching accepted from pending", () => {
    const store = createChainStore();
    store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "done", finishedAt: 2000 });
    expect(store.getChain("p1")?.steps[0].status).toBe("done");
  });

  it("is idempotent: second done for same step is ignored", () => {
    const store = createChainStore();
    store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "done", finishedAt: 2000 });
    store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "done", finishedAt: 3000 });
    expect(store.getChain("p1")?.steps[0].finishedAt).toBe(2000);
  });
});
```

- [ ] **Step 2: Run, verify fail**

Run: `pnpm --dir frontend test chain-store.test -v`
Expected: FAIL.

- [ ] **Step 3: Implement tipos + reducer + handleStep/handleDone**

En `frontend/src/hub/launcher/chain-store.ts`:

```ts
export type ChainStepStatus = "pending" | "launching" | "done" | "failed";
export type ChainStepState = {
  appId: string;
  status: ChainStepStatus;
  startedAt?: number;
  finishedAt?: number;
  message?: string;
  pid?: number;
};
export type ChainState = {
  profileId: string;
  startedAt: number;
  lastEventAt: number; // para el watchdog.
  steps: ChainStepState[];
  currentStepIndex: number;
  overallStatus: "running" | "done" | "error";
};
export type ChainStepEvent = {
  profileId: string;
  stepIndex: number;
  appId: string;
  status: ChainStepStatus;
  startedAt?: number;
  finishedAt?: number;
  message?: string;
  pid?: number;
};
export type LastResult = "success" | "partial" | "error";

const VALID_TRANSITIONS: Record<ChainStepStatus, ChainStepStatus[]> = {
  pending: ["launching", "done", "failed"],
  launching: ["done", "failed"],
  done: [],
  failed: [],
};

function applyStep(state: ChainStepState | undefined, ev: ChainStepEvent): ChainStepState {
  if (!state) state = { appId: ev.appId, status: "pending" };
  if (state.status === "done" || state.status === "failed") return state; // idempotente.
  if (!VALID_TRANSITIONS[state.status].includes(ev.status)) {
    // Permitir out-of-order: pending → done/failed directo.
    if (state.status === "pending" && (ev.status === "done" || ev.status === "failed")) {
      return { ...state, appId: ev.appId, status: ev.status, startedAt: ev.startedAt, finishedAt: ev.finishedAt, message: ev.message, pid: ev.pid };
    }
    return state;
  }
  return { ...state, status: ev.status, startedAt: ev.startedAt ?? state.startedAt, finishedAt: ev.finishedAt, message: ev.message, pid: ev.pid ?? state.pid };
}

export function createChainStore() {
  let chains = new Map<string, ChainState>();
  let lastResults = new Map<string, LastResult>();

  return {
    getChain(profileId: string): ChainState | undefined { return chains.get(profileId); },
    getLastResult(profileId: string): LastResult | undefined { return lastResults.get(profileId); },

    handleStep(ev: ChainStepEvent) {
      const existing = chains.get(ev.profileId);
      const steps = existing ? [...existing.steps] : [];
      steps[ev.stepIndex] = applyStep(steps[ev.stepIndex], ev);
      const now = ev.startedAt ?? ev.finishedAt ?? Date.now();
      chains.set(ev.profileId, existing
        ? { ...existing, steps, currentStepIndex: ev.stepIndex, lastEventAt: now }
        : { profileId: ev.profileId, startedAt: now, lastEventAt: now, steps, currentStepIndex: ev.stepIndex, overallStatus: "running" });
    },

    handleDone(profileId: string, success: boolean) {
      const existing = chains.get(profileId);
      if (!existing) return;
      chains.set(profileId, { ...existing, overallStatus: success ? "done" : "error" });
      if (success) lastResults.set(profileId, "success");
      else lastResults.set(profileId, existing.steps.some((s) => s.status === "done") ? "partial" : "error");
    },

    handleError(profileId: string) {
      lastResults.set(profileId, "error");
    },

    clearChain(profileId: string) {
      chains.delete(profileId);
    },
  };
}
```

- [ ] **Step 4: Run, verify pass**

Run: `pnpm --dir frontend test chain-store.test -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/chain-store.ts frontend/src/hub/launcher/chain-store.test.ts
git commit -m "feat(launcher): chain-store types + reducer + handleStep/handleDone (cut 5.1a)"
```

### Task 5.1b: chain-store — scheduleCleanup con clearTimeout + watchdog con lastEventAt

**Files:**
- Modify: `frontend/src/hub/launcher/chain-store.ts`
- Modify: `frontend/src/hub/launcher/chain-store.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
it("clears chain after 3s of done/error", async () => {
  jest.useFakeTimers();
  const store = createChainStore();
  store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "done", finishedAt: 1000 });
  store.handleDone("p1", true);
  jest.advanceTimersByTime(2999);
  expect(store.getChain("p1")).toBeDefined();
  jest.advanceTimersByTime(1);
  expect(store.getChain("p1")).toBeUndefined();
});

it("clears pending timeout when new step arrives within 3s", () => {
  jest.useFakeTimers();
  const store = createChainStore();
  store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "done", finishedAt: 1000 });
  store.handleDone("p1", true);
  jest.advanceTimersByTime(2000);
  // Relanzar el mismo perfil dentro de los 3s.
  store.handleStep({ profileId: "p1", stepIndex: 0, appId: "lmu", status: "launching", startedAt: 5000 });
  jest.advanceTimersByTime(1500);
  // La cadena nueva NO debe estar borrada.
  expect(store.getChain("p1")?.currentStepIndex).toBe(0);
});
```

- [ ] **Step 2: Implement scheduleCleanup and watchdog**

```ts
export function createChainStore() {
  let chains = new Map<string, ChainState>();
  let lastResults = new Map<string, LastResult>();
  const timeouts = new Map<string, ReturnType<typeof setTimeout>>();
  let watchdogInterval: ReturnType<typeof setInterval> | null = null;
  const STALE_MS = 30000;
  const CLEANUP_MS = 3000;

  function scheduleCleanup(profileId: string) {
    const pending = timeouts.get(profileId);
    if (pending) clearTimeout(pending);
    const t = setTimeout(() => { chains.delete(profileId); timeouts.delete(profileId); }, CLEANUP_MS);
    timeouts.set(profileId, t);
  }

  function startWatchdog() {
    if (watchdogInterval) return;
    watchdogInterval = setInterval(() => {
      const now = Date.now();
      for (const [id, chain] of chains) {
        if (chain.overallStatus === "running" && now - chain.lastEventAt > STALE_MS) {
          chains.set(id, { ...chain, overallStatus: "error" });
          lastResults.set(id, "error");
          scheduleCleanup(id);
        }
      }
    }, 5000);
  }

  return {
    getChain: (id: string) => chains.get(id),
    getLastResult: (id: string) => lastResults.get(id),

    handleStep(ev: ChainStepEvent) {
      const pending = timeouts.get(ev.profileId);
      if (pending) { clearTimeout(pending); timeouts.delete(ev.profileId); }
      const existing = chains.get(ev.profileId);
      const steps = existing ? [...existing.steps] : [];
      steps[ev.stepIndex] = applyStep(steps[ev.stepIndex], ev);
      const now = ev.startedAt ?? ev.finishedAt ?? Date.now();
      chains.set(ev.profileId, existing
        ? { ...existing, steps, currentStepIndex: ev.stepIndex, lastEventAt: now }
        : { profileId: ev.profileId, startedAt: now, lastEventAt: now, steps, currentStepIndex: ev.stepIndex, overallStatus: "running" });
    },

    handleDone(profileId: string, success: boolean) {
      const existing = chains.get(profileId);
      if (!existing) return;
      chains.set(profileId, { ...existing, overallStatus: success ? "done" : "error" });
      if (success) lastResults.set(profileId, "success");
      else lastResults.set(profileId, existing.steps.some((s) => s.status === "done") ? "partial" : "error");
      scheduleCleanup(profileId);
    },

    handleError(profileId: string) {
      lastResults.set(profileId, "error");
      const existing = chains.get(profileId);
      if (existing) { chains.set(profileId, { ...existing, overallStatus: "error" }); scheduleCleanup(profileId); }
    },

    clearChain(profileId: string) { chains.delete(profileId); },

    shutdown() {
      timeouts.forEach((t) => clearTimeout(t));
      timeouts.clear();
      if (watchdogInterval) { clearInterval(watchdogInterval); watchdogInterval = null; }
    },
  };
}
```

- [ ] **Step 3: Run, verify pass**

Run: `pnpm --dir frontend test chain-store.test -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/launcher/chain-store.ts frontend/src/hub/launcher/chain-store.test.ts
git commit -m "feat(launcher): chain-store cleanup with clearTimeout + watchdog (cut 5.1b)"
```

### Task 5.1c: chain-store — subscribe + useSyncExternalStore (suscripción selectiva)

**Files:**
- Modify: `frontend/src/hub/launcher/chain-store.ts`
- Create: `frontend/src/hub/launcher/chain-store.subscribe.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
import { renderHook, act } from "@testing-library/react";
import { ChainRunnerProvider, useChainState, useLastResult } from "./chain-store";

it("useChainState only re-renders when subscribed profileId changes", () => {
  const wrapper = ({ children }: any) => <ChainRunnerProvider>{children}</ChainRunnerProvider>;
  const { result, rerender } = renderHook(() => useChainState("p1"), { wrapper });
  // ... mock events, assert that useChainState("p2") doesn't trigger rerender of useChainState("p1").
});
```

- [ ] **Step 2: Implement Provider + hooks**

```tsx
import { createContext, useContext, useSyncExternalStore, useMemo, useRef } from "react";

const ChainStoreContext = createContext<ReturnType<typeof createChainStore> | null>(null);

export function ChainRunnerProvider({ children }: { children: React.ReactNode }) {
  const storeRef = useRef<ReturnType<typeof createChainStore> | null>(null);
  if (!storeRef.current) storeRef.current = createChainStore();
  // Suscribirse a eventos Wails una vez.
  useEffect(() => {
    const store = storeRef.current!;
    const offStep = Events.On("launcher:chain:step", (e: any) => {
      const p = e.data;
      if (p) store.handleStep({ profileId: p.profileId, stepIndex: p.stepIndex, appId: p.appId, status: p.status, startedAt: p.startedAt, finishedAt: p.finishedAt, message: p.message, pid: p.pid });
    });
    const offDone = Events.On("launcher:chain:done", (e: any) => { if (e.data?.profileId) store.handleDone(e.data.profileId, e.data.success); });
    const offError = Events.On("launcher:chain:error", (e: any) => { if (e.data?.profileId) store.handleError(e.data.profileId); });
    return () => { offStep(); offDone(); offError(); };
  }, []);
  return <ChainStoreContext.Provider value={storeRef.current}>{children}</ChainStoreContext.Provider>;
}

export function useChainState(profileId: string): ChainState | undefined {
  const store = useContext(ChainStoreContext);
  if (!store) throw new Error("useChainState must be used within ChainRunnerProvider");
  return useSyncExternalStore(
    (cb) => store.subscribe(profileId, cb),
    () => store.getChain(profileId),
    () => undefined
  );
}

export function useLastResult(profileId: string): LastResult | undefined {
  const store = useContext(ChainStoreContext);
  if (!store) throw new Error("useLastResult must be used within ChainRunnerProvider");
  return useSyncExternalStore(
    (cb) => store.subscribe(profileId, cb),
    () => store.getLastResult(profileId),
    () => undefined
  );
}
```

Añadir `subscribe` al store:

```ts
subscribe(profileId: string, cb: () => void): () => void {
  // Suscripción selectiva: el callback se llama solo cuando cambia la entrada de este profileId.
  // Implementación interna: usar un Set por profileId.
  // ...
}
```

- [ ] **Step 3: Run, verify pass**

Run: `pnpm --dir frontend test chain-store -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/launcher/chain-store.ts frontend/src/hub/launcher/chain-store.subscribe.test.ts
git commit -m "feat(launcher): ChainRunnerProvider with useSyncExternalStore selective subscription (cut 5.1c)"
```

### Task 5.2: ProfileCard con JSX completo + count en tooltip + early return a Timeline

**Files:**
- Modify: `frontend/src/hub/launcher/ProfileCard.tsx`
- Test: `frontend/src/hub/launcher/ProfileCard.test.tsx`

- [ ] **Step 1: Write the failing tests (4 tests reales)**

```tsx
it("renders full card with name, description, time, apps, telemetry, lastResult badge", () => { /* ... */ });
it("renders early return to ProfileCardTimeline when chain active", () => { /* ... */ });
it("count appears in tooltip on hover, not on the face", () => { /* ... */ });
it("renders favorite badge in header when isFavorite", () => { /* ... */ });
```

- [ ] **Step 2: Implement ProfileCard con JSX completo**

```tsx
import { useChainState } from "./chain-store";
import { ProfileCardTimeline } from "./ProfileCard.timeline";

export function ProfileCard({ profile, apps, onLaunch, onCancel, onEdit }: Props) {
  const chain = useChainState(profile.id);
  if (chain) {
    return <ProfileCardTimeline chain={chain} apps={apps} onCancel={() => onCancel(profile.id)} />;
  }

  const launchable = isProfileLaunchable(profile, apps);
  const estimatedMs = estimateChainDuration(profile, apps);
  const realMs = profile.avgChainDurationMs;
  const timeLabel = realMs ? `≈${Math.round(realMs / 1000)}s` : estimatedMs ? `≈${Math.round(estimatedMs / 1000)}s` : "—";

  return (
    <article className={`card-sleek rounded-xl p-5 ${className ?? ""}`} data-testid={`profile-card-${profile.id}`}>
      <div className="flex items-start justify-between gap-3">
        <div>
          <h2 className="font-display font-bold text-lg text-white flex items-center">
            {profile.name}
            {profile.isFavorite && <span className="text-amber-400 ml-2" data-testid={`profile-favorite-badge-${profile.id}`} aria-label="Favorita">★</span>}
          </h2>
          {profile.description && <p className="text-xs text-vantare-textMuted mt-1">{profile.description}</p>}
        </div>
        <div className="flex items-center gap-2">
          <button onClick={() => onLaunch(profile.id)} disabled={!launchable}
            title={launchable ? undefined : t("launcher.profile.unlaunchable")}
            className="px-3 py-1.5 rounded-lg bg-accent text-[10px] uppercase tracking-[.18em] font-bold text-black hover:opacity-90 disabled:opacity-40 disabled:cursor-not-allowed"
            data-testid={`profile-launch-${profile.id}`}>{t("launcher.profile.start")}</button>
        </div>
      </div>

      <p className="text-xs text-vantare-textMuted mt-2" data-testid={`profile-time-${profile.id}`}>
        {timeLabel}
      </p>

      {profile.lastLaunchedAt && (
        <p className="text-xs text-vantare-textDim mt-1" data-testid={`profile-last-${profile.id}`}>
          Último: {new Date(profile.lastLaunchedAt).toLocaleString()}
        </p>
      )}

      <ul className="mt-3 flex flex-col gap-1.5" data-testid="profile-steps">
        {profile.steps.length === 0 && <li className="text-xs text-vantare-textDim">Sin pasos configurados.</li>}
        {profile.steps.map((step, index) => {
          const app = apps.find((a) => a.id === step.appId);
          return (
            <li key={`${step.appId}-${index}`} className="flex items-center gap-2 rounded-md bg-black/20 px-2 py-1.5">
              {app ? <AppBadge app={app} size="sm" /> : <span className="text-xs text-vantare-textDim">App desconocida ({step.appId})</span>}
              <span className="ml-auto text-[10px] uppercase tracking-[.18em] text-vantare-textDim">{step.delay}s</span>
            </li>
          );
        })}
      </ul>

      <div className="mt-3 flex items-center gap-2 justify-end" data-testid={`profile-actions-${profile.id}`}>
        <span className="text-[10px] uppercase tracking-[.18em] text-vantare-textMuted mr-auto"
              title={`Lanzado ${profile.launchCount ?? 0} veces`}
              data-testid={`profile-count-${profile.id}`}>
          {profile.launchCount ? `${profile.launchCount}×` : ""}
        </span>
        <button onClick={() => onEdit(profile.id)} data-testid={`profile-edit-${profile.id}`}
          className="px-3 py-1.5 rounded-lg border border-white/10 text-[10px] uppercase tracking-[.18em] text-vantare-textMuted hover:border-accent/40 hover:text-white transition-colors">
          Editar
        </button>
      </div>
    </article>
  );
}
```

- [ ] **Step 3: Run, verify pass**

Run: `pnpm --dir frontend test ProfileCard -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/launcher/ProfileCard.tsx frontend/src/hub/launcher/ProfileCard.test.tsx
git commit -m "feat(launcher): ProfileCard full UI + early return to timeline + count in tooltip (cut 5.2)"
```

### Task 5.3: ProfileCardTimeline con cian para audio

**Files:**
- Create: `frontend/src/hub/launcher/ProfileCard.timeline.tsx`
- Create: `frontend/src/hub/launcher/ProfileCard.timeline.test.tsx`
- Create: `frontend/src/hub/launcher/ProfileCard.timeline.a11y.test.tsx`

- [ ] **Step 1: Write the failing tests**

```tsx
it("renders one block per step", () => { /* ... */ });
it("applies launching pulse to the current step", () => { /* ... */ });
it("uses cyan for audio category to avoid collision with done green", () => { /* ... */ });
```

- [ ] **Step 2: Implement ProfileCard.timeline.tsx**

```tsx
import { motion } from "motion/react";

const CATEGORY_COLORS: Record<string, string> = {
  simulator: "#ff3b3b", streaming: "#302e31", audio: "#06b6d4",
  telemetry: "#f59e0b", utility: "#3b82f6",
};

export function ProfileCardTimeline({ chain, apps, onCancel }: Props) {
  return (
    <div className="card-sleek rounded-xl p-5" data-testid="profile-timeline" role="status" aria-live="polite">
      <button onClick={onCancel} data-testid="profile-cancel"
        aria-label="Cancelar lanzamiento"
        className="float-right px-3 py-1.5 rounded-lg border border-amber-500/40 text-[10px] uppercase tracking-[.18em] text-amber-300">Cancelar</button>
      <h3 className="font-display font-bold text-lg text-white mb-3">Lanzando…</h3>
      <div className="flex flex-wrap gap-2">
        {chain.steps.map((step, i) => {
          const app = apps.find((a) => a.id === step.appId);
          const color = app ? CATEGORY_COLORS[app.category] : "#6b7280";
          const bg = step.status === "launching" ? `${color}33` : step.status === "done" ? "#10b98126" : step.status === "failed" ? "#ef444426" : "rgba(255,255,255,0.05)";
          const border = step.status === "launching" ? color : step.status === "done" ? "#10b98166" : step.status === "failed" ? "#ef444466" : "rgba(255,255,255,0.1)";
          return (
            <motion.div key={`${step.appId}-${i}`}
              data-testid={`timeline-step-${i}`}
              className="rounded-md p-2 min-w-[80px]"
              style={{ background: bg, border: `1px solid ${border}` }}
              animate={step.status === "launching" ? { opacity: [0.7, 1, 0.7] } : {}}
              transition={step.status === "launching" ? { duration: 1.5, repeat: Infinity, ease: "easeInOut" } : {}}>
              <div className="text-xs font-bold text-white">{app?.abbreviation ?? step.appId}</div>
              <div className="text-[10px] text-vantare-textMuted">{step.status}</div>
            </motion.div>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: a11y test**

```tsx
it("has role=status and aria-live=polite", () => {
  const chain = { profileId: "p1", startedAt: 0, lastEventAt: 0, steps: [{ appId: "lmu", status: "launching" as const }], currentStepIndex: 0, overallStatus: "running" as const };
  render(<ProfileCardTimeline chain={chain} apps={[]} onCancel={() => {}} />);
  expect(screen.getByTestId("profile-timeline")).toHaveAttribute("role", "status");
  expect(screen.getByTestId("profile-timeline")).toHaveAttribute("aria-live", "polite");
});
```

- [ ] **Step 4: Run, verify pass**

Run: `pnpm --dir frontend test ProfileCard.timeline -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/launcher/ProfileCard.timeline.tsx frontend/src/hub/launcher/ProfileCard.timeline.test.tsx frontend/src/hub/launcher/ProfileCard.timeline.a11y.test.tsx
git commit -m "feat(launcher): ProfileCard.timeline with cyan audio + a11y (cut 5.3)"
```

### Task 5.4: HubToast (fallback) + integración con chain:done

**Files:**
- Create: `frontend/src/hub/launcher/HubToast.tsx`
- Create: `frontend/src/hub/launcher/HubToast.test.tsx`
- Modify: `frontend/src/hub/launcher/chain-store.ts` (emite señal de toast al cerrar cadena)

- [ ] **Step 1: Write the failing test**

```tsx
it("renders success toast with retry button on partial", () => { /* ... */ });
it("emits retry event on retry button click", () => { /* ... */ });
```

- [ ] **Step 2: Implement HubToast**

```tsx
type ToastVariant = "success" | "partial" | "error";
type Props = { variant: ToastVariant; message: string; onRetry?: () => void; onClose: () => void };

export function HubToast({ variant, message, onRetry, onClose }: Props) {
  const color = variant === "success" ? "border-emerald-500/40 bg-emerald-500/5" : variant === "partial" ? "border-amber-500/40 bg-amber-500/5" : "border-vantare-red-400/30 bg-vantare-red-400/5";
  return (
    <div data-testid="hub-toast" className={`fixed top-4 right-4 z-50 flex items-center gap-2 text-sm rounded-lg border px-4 py-2 ${color}`}>
      <span className="flex-1">{message}</span>
      {onRetry && <button onClick={onRetry} data-testid="hub-toast-retry" className="px-2 py-1 rounded border border-white/10 text-[10px] uppercase tracking-[.18em] text-vantare-textMuted hover:text-white">Reintentar fallidos</button>}
      <button onClick={onClose} data-testid="hub-toast-close" className="px-1 text-vantare-textDim hover:text-white">✕</button>
    </div>
  );
}
```

En el `ChainRunnerProvider`, al recibir `chain:done`, emitir un toast:

```tsx
const offDone = Events.On("launcher:chain:done", (e: any) => {
  const { profileId, success } = e.data;
  if (profileId) {
    store.handleDone(profileId, success);
    setToast({ variant: success ? "success" : store.getLastResult(profileId) === "partial" ? "partial" : "error", message: `Perfil ${profileId}: ${success ? "listo" : "con fallos"}`, onRetry: () => Events.Emit("launcher:profile:retry:failed", { id: profileId }) });
  }
});
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/hub/launcher/HubToast.tsx frontend/src/hub/launcher/HubToast.test.tsx frontend/src/hub/launcher/chain-store.ts
git commit -m "feat(launcher): HubToast fallback with retry button (cut 5.4)"
```

### Task 5.5: Montar ChainRunnerProvider en HubApp + visual compare con lastResult dock y stale chain

**Files:**
- Modify: `frontend/src/hub/HubApp.tsx`
- Modify: `frontend/scripts/launcher-visual-compare.mjs`

- [ ] **Step 1: Verify HubApp location y montar provider**

```tsx
import { ChainRunnerProvider } from "./launcher/chain-store";

export function HubApp() {
  return (
    <ChainRunnerProvider>
      {/* ... resto del árbol ... */}
    </ChainRunnerProvider>
  );
}
```

- [ ] **Step 2: Extend launcher-visual-compare.mjs**

Añadir capturas:
- 11. Dock con `lastResult` badge success (verde).
- 12. Dock con `lastResult` badge error (rojo).
- 13. Perfil en estado stale chain (>30s sin eventos) → card marcada como error.

- [ ] **Step 3: Run visual compare**

Run: `cd frontend && node scripts/launcher-visual-compare.mjs`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/hub/HubApp.tsx frontend/scripts/launcher-visual-compare.mjs
git commit -m "feat(launcher): mount ChainRunnerProvider + visual compare stale/lastResult (cut 5.5)"
```

### Task 5.6: Verify final del corte 5

- [ ] **Step 1: Run all tests**

Run: `pnpm --dir frontend test && go test ./...`
Expected: PASS.

- [ ] **Step 2: Build and lint**

Run: `pnpm --dir frontend build && pnpm --dir frontend lint`
Expected: PASS.

---

## Corte 6 — Dock sincronizado con lastResult y tooltip

**Files:**
- Modify: `frontend/src/hub/components/LauncherDock.tsx`
- Modify: `frontend/src/hub/components/LauncherDock.test.tsx`

- [ ] **Step 1: Write the failing tests**

```tsx
it("renders lastResult badge when no chain active but profile has last result", () => { /* ... */ });
it("count appears in tooltip on hover", () => { /* ... */ });
it("renders SVG ring during active chain", () => { /* ... */ });
it("favorites first, alphabetical after", () => { /* ... */ });
```

- [ ] **Step 2: Implement dock with useChainState + useLastResult**

```tsx
import { useChainState, useLastResult } from "../launcher/chain-store";

function DockProfileButton({ profile, onLaunch, onCancel }: Props) {
  const chain = useChainState(profile.id);
  const lastResult = useLastResult(profile.id);

  const ringColor = chain ? (() => {
    const step = chain.steps[chain.currentStepIndex];
    // (en producción, mapear category → color).
    return "#3b82f6";
  })() : null;

  return (
    <button onClick={onLaunch} title={`${profile.name} (lanzado ${profile.launchCount ?? 0} veces)`}
      data-testid={`dock-profile-${profile.id}`}
      aria-label={`Lanzar perfil ${profile.name}`}
      className="v52-dock-item relative">
      {chain ? (
        <svg viewBox="0 0 24 24" className="w-5 h-5">
          <circle cx="12" cy="12" r="10" stroke="rgba(255,255,255,0.1)" strokeWidth="2" fill="none" />
          <circle cx="12" cy="12" r="10" stroke={ringColor ?? "#3b82f6"} strokeWidth="2" fill="none"
            strokeDasharray="62.83" strokeDashoffset={62.83 - 62.83 * (chain.currentStepIndex + 1) / chain.steps.length}
            style={{ transform: "rotate(-90deg)", transformOrigin: "12px 12px" }} />
        </svg>
      ) : (
        <ProfileGlyph name={profile.name} />
      )}
      {profile.isFavorite && <span className="absolute -top-1 -right-1 w-2 h-2 rounded-full bg-amber-400" data-testid={`dock-favorite-${profile.id}`} />}
      {!chain && lastResult === "success" && <span className="absolute inset-0 rounded border border-emerald-500/40" data-testid={`dock-lastresult-success-${profile.id}`} />}
      {!chain && lastResult === "error" && <span className="absolute inset-0 rounded border border-vantare-red-400/50" data-testid={`dock-lastresult-error-${profile.id}`} />}
      {!chain && lastResult === "partial" && <span className="absolute inset-0 rounded border border-amber-500/40" data-testid={`dock-lastresult-partial-${profile.id}`} />}
    </button>
  );
}
```

- [ ] **Step 3: Visual compare del corte 6**

Extender `launcher-visual-compare.mjs` con capturas de dock con anillo + lastResult.

- [ ] **Step 4: Run, verify pass**

Run: `pnpm --dir frontend test LauncherDock -v && cd frontend && node scripts/launcher-visual-compare.mjs`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/components/LauncherDock.tsx frontend/src/hub/components/LauncherDock.test.tsx frontend/scripts/launcher-visual-compare.mjs
git commit -m "feat(launcher): dock with lastResult badge + SVG ring + count in tooltip (cut 6)"
```

### Task 6.1: Verify final del corte 6

- [ ] **Step 1: Run all tests + build + lint**

Run: `pnpm --dir frontend test && pnpm --dir frontend build && pnpm --dir frontend lint && go test ./...`
Expected: PASS.

---

## Corte 7 — Auto-launch + hotkeys + cierre robusto + wiring completo de main.go

### Task 7.1: autostart_windows.go

**Files:**
- Create: `internal/app/launcher/autostart_windows.go`
- Test: `internal/app/launcher/autostart_windows_test.go`

- [ ] **Step 1: Write the failing tests**

```go
func TestParseLaunchFlag(t *testing.T) {
    cases := []struct {
        args []string
        wantID string
        wantOK bool
    }{
        {[]string{"--launch=creator"}, "creator", true},
        {[]string{"--launch="}, "", false},
        {[]string{"--launch"}, "", false},
        {[]string{""}, "", false},
        {[]string{"--other=foo"}, "", false},
    }
    for _, c := range cases {
        id, ok := ParseLaunchFlag(c.args)
        if id != c.wantID || ok != c.wantOK { t.Errorf("ParseLaunchFlag(%v): got (%q, %v), want (%q, %v)", c.args, id, ok, c.wantID, c.wantOK) }
    }
}

func TestRegisterUnregisterAutostart(t *testing.T) {
    // Verificar idempotencia: registrar 2 veces con mismo path = 1 entrada.
}

func TestAutostartUnknownProfileIDRemovesRegistryEntry(t *testing.T) {
    // Verificar que un profileID inexistente borra la entrada de HKCU\...\Run.
}
```

- [ ] **Step 2: Implement autostart_windows.go**

```go
package launcher

import (
    "fmt"
    "os"
    "golang.org/x/sys/windows/registry"
)

const autostartKeyPath = `Software\Microsoft\Windows\CurrentVersion\Run`

func autostartValueName(profileID string) string { return fmt.Sprintf("Vantare.%s", profileID) }

func RegisterAutostart(profileID string) error {
    exe, err := os.Executable()
    if err != nil { return err }
    value := fmt.Sprintf(`"%s" --launch=%s`, exe, profileID)
    k, _, err := registry.CreateKey(registry.CURRENT_USER, autostartKeyPath, registry.SET_VALUE)
    if err != nil { return err }
    defer k.Close()
    return k.SetStringValue(autostartValueName(profileID), value)
}

func UnregisterAutostart(profileID string) error {
    k, err := registry.OpenKey(registry.CURRENT_USER, autostartKeyPath, registry.SET_VALUE)
    if err != nil { return err }
    defer k.Close()
    return k.DeleteValue(autostartValueName(profileID))
}

func ParseLaunchFlag(args []string) (string, bool) {
    for _, a := range args {
        if len(a) > 9 && a[:9] == "--launch=" {
            id := a[9:]
            if id != "" { return id, true }
        }
    }
    return "", false
}
```

- [ ] **Step 3: Commit**

```bash
git add internal/app/launcher/autostart_windows.go internal/app/launcher/autostart_windows_test.go
git commit -m "feat(launcher): autostart_windows.go + ParseLaunchFlag (cut 7)"
```

### Task 7.2: hotkey_windows.go con RegisterHotkey, re-registro en WM_POWERBROADCAST, whitelist

**Files:**
- Modify: `internal/app/launcher/hotkey_windows.go`
- Test: `internal/app/launcher/hotkey_windows_test.go`

- [ ] **Step 1: Write the failing tests**

```go
func TestParseHotkeyString(t *testing.T) {
    mods, vk, err := ParseHotkeyString("ctrl+shift+1")
    if err != nil { t.Fatal(err) }
    if mods & MOD_CONTROL == 0 { t.Error("expected ctrl") }
    if mods & MOD_SHIFT == 0 { t.Error("expected shift") }
    if vk != 0x31 { t.Errorf("expected VK_1=0x31, got %x", vk) }
}

func TestHotkeyWhitelistRejectsReserved(t *testing.T) {
    if IsHotkeyAllowed("ctrl+c") { t.Error("ctrl+c should be rejected") }
    if !IsHotkeyAllowed("ctrl+shift+1") { t.Error("ctrl+shift+1 should be allowed") }
}

func TestHotkeyReregistrationIdempotent(t *testing.T) {
    // Re-registrar 2 veces con mismo combo = 1 efecto.
}
```

- [ ] **Step 2: Implement hotkey_windows.go**

```go
package launcher

import (
    "fmt"
    "strings"
    "sync"
    "golang.org/x/sys/windows"
)

var reservedCombos = map[string]bool{
    "ctrl+c": true, "ctrl+v": true, "ctrl+x": true, "ctrl+z": true,
    "alt+f4": true, "alt+tab": true, "win+l": true,
}

func IsHotkeyAllowed(combo string) bool {
    return !reservedCombos[strings.ToLower(combo)]
}

func ParseHotkeyString(s string) (modifiers uint32, vk uint32, err error) {
    parts := strings.Split(strings.ToLower(s), "+")
    if len(parts) < 2 { return 0, 0, fmt.Errorf("invalid hotkey: %s", s) }
    for _, p := range parts[:len(parts)-1] {
        switch p {
        case "ctrl": modifiers |= windows.MOD_CONTROL
        case "shift": modifiers |= windows.MOD_SHIFT
        case "alt": modifiers |= windows.MOD_ALT
        case "win": modifiers |= windows.MOD_WIN
        default: return 0, 0, fmt.Errorf("unknown modifier: %s", p)
        }
    }
    last := parts[len(parts)-1]
    if len(last) == 1 {
        vk = uint32(last[0])
    } else {
        return 0, 0, fmt.Errorf("invalid key: %s", last)
    }
    return modifiers, vk, nil
}

// HotkeyManager gestiona el registro de hotkeys de perfil.
type HotkeyManager struct {
    mu     sync.Mutex
    nextID int
    active map[string]int    // profileID -> hotkeyId
    combos map[int]string    // hotkeyId -> combo string
}

func NewHotkeyManager() *HotkeyManager {
    return &HotkeyManager{active: map[string]int{}, combos: map[int]string{}}
}

func (h *HotkeyManager) Register(profileID, combo string) error {
    if !IsHotkeyAllowed(combo) { return fmt.Errorf("hotkey reserved: %s", combo) }
    mods, vk, err := ParseHotkeyString(combo)
    if err != nil { return err }
    h.mu.Lock()
    defer h.mu.Unlock()
    if existing, ok := h.active[profileID]; ok {
        windows.UnregisterHotKey(0, uint32(existing)) // best-effort.
        delete(h.active, profileID)
    }
    id := h.nextID; h.nextID++
    if !windows.RegisterHotKey(0, uint32(id), mods, vk) {
        return fmt.Errorf("RegisterHotKey failed for %s", combo)
    }
    h.active[profileID] = id
    h.combos[id] = combo
    return nil
}

func (h *HotkeyManager) Unregister(profileID string) {
    h.mu.Lock()
    defer h.mu.Unlock()
    if id, ok := h.active[profileID]; ok {
        windows.UnregisterHotKey(0, uint32(id))
        delete(h.active, profileID)
        delete(h.combos, id)
    }
}

func (h *HotkeyManager) ReRegisterAll() {
    h.mu.Lock()
    defer h.mu.Unlock()
    // Capturar snapshot de (profileID, combo) y re-registrar cada uno.
    snapshot := make([]struct{ id, combo string }, 0, len(h.active))
    for id, hid := range h.active {
        snapshot = append(snapshot, struct{ id, combo string }{id, h.combos[hid]})
    }
    for _, s := range snapshot {
        h.Unregister(s.id)
        _ = h.Register(s.id, s.combo) // best-effort; si falla, queda desactivada.
    }
}
```

- [ ] **Step 3: Commit**

```bash
git add internal/app/launcher/hotkey_windows.go internal/app/launcher/hotkey_windows_test.go
git commit -m "feat(launcher): HotkeyManager with Register/Unregister/ReRegisterAll (cut 7)"
```

### Task 7.3: Handlers de main.go (consolidación de cancel, retry, stats, autostart, hotkey, app:favorite, app:update, launch flag, window close)

**Files:**
- Modify: `cmd/vantare/main.go`
- Test: `cmd/vantare/main_test.go`

- [ ] **Step 1: Write the failing tests (10 tests reales)**

```go
func TestHandleProfileCancel(t *testing.T) { /* ... */ }
func TestHandleProfileRetryFailed(t *testing.T) { /* ... */ }
func TestHandleProfileStatsSave(t *testing.T) { /* ... */ }
func TestHandleProfileHotkeySet(t *testing.T) { /* ... */ }
func TestHandleAutostartToggle(t *testing.T) { /* ... */ }
func TestHandleAppFavorite(t *testing.T) { /* ... */ }
func TestHandleAppUpdate(t *testing.T) { /* ... */ } // ya cubierto en 2.2, referencia.
func TestHandleLaunchFlag(t *testing.T) { /* ... */ }
func TestHandleWindowCloseCancelsChains(t *testing.T) { /* ... */ }
```

- [ ] **Step 2: Implementar todos los handlers en main.go**

```go
func (a *App) handleProfileCancel(event wailsEvent) {
    id, _ := event.Payload["id"].(string)
    a.chainRunner.Cancel(id)
}

func (a *App) handleProfileRetryFailed(event wailsEvent) {
    id, _ := event.Payload["id"].(string)
    a.chainRunner.RetryFailed(id)
}

func (a *App) handleProfileStatsSave(event wailsEvent) {
    id, _ := event.Payload["id"].(string)
    durationMs, _ := event.Payload["durationMs"].(float64)
    if err := launcher.RecordProfileSuccess(a.settings, id, int64(durationMs)); err != nil { a.logError("stats save", err) }
}

func (a *App) handleProfileHotkeySet(event wailsEvent) {
    profileID, _ := event.Payload["id"].(string)
    combo, _ := event.Payload["combo"].(string)
    if combo == "" {
        a.hotkeyManager.Unregister(profileID); return
    }
    if err := a.hotkeyManager.Register(profileID, combo); err != nil {
        a.emit("launcher:profile:hotkey:error", map[string]any{"id": profileID, "message": err.Error()}); return
    }
    a.emit("launcher:profile:hotkey:set", map[string]any{"id": profileID, "combo": combo})
}

func (a *App) handleAutostartToggle(event wailsEvent) {
    profileID, _ := event.Payload["id"].(string)
    enabled, _ := event.Payload["enabled"].(bool)
    if enabled {
        if err := launcher.RegisterAutostart(profileID); err != nil { a.logError("autostart on", err); return }
    } else {
        if err := launcher.UnregisterAutostart(profileID); err != nil { a.logError("autostart off", err) }
    }
    a.emit("launcher:autostart:updated", map[string]any{"id": profileID, "enabled": enabled})
}

func (a *App) handleAppFavorite(event wailsEvent) {
    id, _ := event.Payload["id"].(string)
    fav, _ := event.Payload["isFavorite"].(bool)
    if err := a.settings.SetLauncherAppFavorite(id, fav); err != nil { a.logError("app favorite", err); return }
    a.emit("launcher:apps:updated", map[string]any{"apps": a.settings.GetLauncherApps()})
}

func (a *App) handleLaunchFlag(args []string) {
    id, ok := launcher.ParseLaunchFlag(args)
    if !ok { return } // no --launch=; arranque normal.
    profile, found := a.settings.GetProfileByID(id)
    if !found {
        // Perfil inexistente: limpiar entrada del registro y arrancar normal.
        _ = launcher.UnregisterAutostart(id); return
    }
    a.chainRunner.StartChain(profile)
}
```

Wire en el routing de eventos. Wire `Wails.OnShutdown` para cancelar cadenas:

```go
wails.OnShutdown(func() {
    a.chainRunner.CancelAll()
})
```

- [ ] **Step 3: Run, verify pass**

Run: `go test ./cmd/vantare/... -v`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add cmd/vantare/main.go cmd/vantare/main_test.go
git commit -m "feat(launcher): all backend handlers wired in main.go (cut 7)"
```

### Task 7.4: i18n keys restantes (chain, autostart, hotkey, appDetails, toast)

- [ ] **Step 1: Add keys**

En `frontend/src/i18n/locales/{es,en,pt,it}.ts`:
- `launcher.chain.states.{pending,launching,done,failed,cancelled}`
- `launcher.autostart`
- `launcher.hotkey`
- `launcher.appDetails.{path,args}`
- `launcher.toast.{success,partial,error,retry}`
- `launcher.profile.unlaunchable`

- [ ] **Step 2: Verify paridad test (añadido en Task 3.4 cubre la mayoría; añadir las nuevas)**

- [ ] **Step 3: Commit**

```bash
git add frontend/src/i18n/
git commit -m "feat(launcher): i18n keys for chain states, autostart, hotkey, app details, toast (cut 7)"
```

### Task 7.5: Verify final del corte 7

- [ ] **Step 1: Run all checks**

```bash
pnpm --dir frontend test
pnpm --dir frontend build
pnpm --dir frontend lint
go test ./...
go build ./...
cd frontend && node scripts/launcher-visual-compare.mjs
```

Expected: all PASS, visual compare exit 0.

---

## Code Review Multi-Revisor (al final de los 9 cortes)

### Regla de quórum del code review

Tras cerrar los 9 cortes, se ejecuta el code review con 3 subagentes `oracle` en paralelo sobre el código commiteado. **Regla de merge**:

- **Se mergea si 0 issues bloqueantes** entre los 3 reportes.
- **1 issue bloqueante de cualquier lente** (rendimiento / fiabilidad / mantenibilidad) fuerza un ciclo de fixer.
- **Prioridad de desempate** entre lentes en caso de contradicciones: **fiabilidad > mantenibilidad > rendimiento**. Si un issue de fiabilidad bloquea y otro de rendimiento dice "se puede mergear", gana fiabilidad.
- **Definición de "issue bloqueante"**: un issue que puede causar pérdida de datos, crash del Hub, o comportamiento observable incorrecto en el caso de uso real (piloto de Le Mans Ultimate). Issues cosméticos o "nice to have" NO son bloqueantes.

### Base de commits para el review

```bash
# Encontrar la base automáticamente.
BASE=$(git log --oneline | grep -m1 "feat(launcher): add v2 implementation plan" | awk '{print $1}')
HEAD=$(git rev-parse HEAD)
git log --oneline "$BASE".."$HEAD"
```

(Esto evita el SHA hardcodeado del plan original.)

### Tareas del code review

- **Review Task 1**: Lanzar 3 subagentes `oracle` en paralelo (background: true). Aliases: `review-perf`, `review-reliability`, `review-maint`. Cada uno recibe el spec validado + el rango de commits + instrucción: emitir reporte con issues `file:line`, sugerencias de fix, y veredicto individual.
- **Review Task 2**: Reconciliar los 3 reportes. Si hay issues bloqueantes, pasarlos a un `fixer` con mini-plan. Si no, marcar el lanzamiento v2 como listo.
- **Review Task 3**: Re-verificar después de fixes (`pnpm test`, `pnpm build`, `go test ./...`, visual compare). Si pasa, commit final + tag (decisión del usuario, no automática).

---

## Resumen de entregables

- **Backend Go**: 11 archivos modificados/creados. Settings atómico con retry+backoff+sidecar+.bak+mutex. ChainRunner con backend, liveness probe, PIDs, rechazo doble-lanzamiento. 10 handlers de backend. SchemaVersion + migración. Sanitización de diagnostics.
- **Frontend**: 16 archivos modificados/creados. Store externo con useSyncExternalStore selectivo. ProfileCard con JSX completo + count en tooltip + lastResult dock. ProfileEditor side-panel completo. AddNonSteamGameModal single-select. HubToast fallback. i18n paridad es/en/pt/it.
- **Tests**: 30+ tests nuevos Go y TS. TDD real. Sin placebos.
- **Visual compare**: 13 capturas, exit 0.
- **Code review**: 3 revisores aprueban o se reabre con fixer. Regla de quórum explícita.
- **Sin commit, sin tag, sin release** (decisión del usuario).
