import type {TelemetrySnapshot} from "../../core/telemetry-snapshot";import type {WidgetViewModelBase} from "../../core/widget-definition";import {readDamage} from "../shared/damage-reader";import type {CarDamageVisualContent} from "./car-damage-visual-definition";export type CarDamageVisualViewModel=WidgetViewModelBase&{type:"car-damage-visual";body?:number;aero?:number;suspension?:number;tyres?:readonly number[];showPercent:boolean;showAero:boolean};export function buildCarDamageVisualViewModel(snapshot:TelemetrySnapshot,content:CarDamageVisualContent):CarDamageVisualViewModel{const damage=readDamage(snapshot);return{type:"car-damage-visual",status:damage?"ready":"missing",...damage,showPercent:content.showPercent,showAero:content.showAero};}
