import React from 'react';

/**
 * Relative overlay — full implementation in Task 8.
 * This is a placeholder stub until Wave 2.
 */
export default function RelativePlaceholder() {
  return <div data-testid="relative-overlay">Relative overlay placeholder</div>;
}
