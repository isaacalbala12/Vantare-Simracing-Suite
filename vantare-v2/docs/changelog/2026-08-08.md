# Changelog — 2026-08-08

Sesión de madrugada (00:09–01:29, 21 commits) + trabajo en curso al cierre.
Ramas: `nightly`, `vantareapp/isa-12-ajustes-retirar-avanzado-y-traducir`, `vantareapp/isa-303-cal-01-*`, `vantareapp/isa-str-09b-saneamiento-workspace`.

## Actualizador y release (ISA-302 / REL-02, PR #181) — `d2cf778`

- La versión embebida ahora se deriva del tag publicado (parte numérica en recursos de Windows, tag completo con sufijo en el binario), en vez de un valor fijo que hacía que la app siempre dijera ser la v0.1.0.4 y se ofreciera la misma actualización en bucle.
- Los sufijos pre-release se comparan numéricamente: `nightly.10` ya no queda por debajo de `nightly.9` (`internal/updater/version.go`).
- La lista de releases se ordena por versión, no por fecha de publicación.
- El endpoint de releases apunta al nombre correcto del repositorio (antes dependía de la redirección de GitHub).
- `fix(release) 2ce7403`: el manifest de release se comprueba antes de construir nada.
- Fragments de changelog `ISA-302.json` e `ISA-303.json` creados en `docs/changelog/fragments/`.

## Calendario (ISA-303) — `e4868a1`

- Bug de zona horaria: el número de día del grid mensual se leía de la zona de visualización y mostraba el día desplazado una casilla fuera de UTC. Ahora cada celda lee el día de su propia fecha local; las horas de los eventos siguen formateándose en la zona elegida.

## Ajustes (ISA-12 / ISA-118)

- `de636fc`: se retira `deltaMode` del modelo de ajustes; se conserva `cpuSampling`.
- `e18b182`: pasada visual final de la página de Ajustes (SaveStatus, DowngradeModal, secciones reescritas).
- `f76bbbe`: docs de lo que hicieron los recortes C y D del plan de la sección.
- `f5bd15e`: navegación de secciones (`HubSubnav`) anclada bajo la navegación global.
- `1553b29`: Vantare puede abrirse al iniciar sesión en Windows, opcionalmente minimizado (servicio `startup` + ajustes).
- `b6279d8` (ISA-118): cada guardado atómico usa su propio archivo temporal (evita colisiones en guardados concurrentes).
- `e4b0a9d`: la sección de almacenamiento muestra qué ocupa Vantare en disco y permite reclamarlo (abrir carpeta en Explorer, downgrade, etc.).

## Estrategia — solver determinista

- `c61985b`: unificación de los dos modelos de neumáticos frontend en el dominio Go (paridad `strategy-tyre.ts`).
- `6fbb5f6`: el dominio Go de neumáticos pasa a ser la autoridad en runtime (bridge Wails, plan de uso, inventario).
- `3bac38a`: solver determinista (histórico + `solver.go`).
- `047693d`: comparador de planes fast / robust / conservative.

## Overlay Studio

- `eaf0472`: el commit de interacción lee el ref del input durante el commit (fix de flakiness de drag/resize).

## Frontend / CI — saneamiento de tests

- `a2a5e1a` + `d5f55dc`: los tests de scripts pueden superar sus waits y se deja de empaquetar `node:test` (oculto del bundler por completo).
- `e881850` + `abe927c`: la deuda de tests registrada vuelve a correr en los jobs bloqueantes y el allowlist queda fijado a vacío.

## Trabajo en curso al cierre (sin commit)

- `internal/updater`: sigue la comparación numérica de sufijos y ajustes de `updater.go`/`github.go` (mismo corte ISA-302).
- `.github/workflows/release.yml`, `build/sync_version.go` y `build/windows/Taskfile.yml`: sync de versión desde el tag (sin commitear aún).
- `strategy-base.html` (raíz del repo): base visual de estrategia, 235 líneas nuevas.
- Baselines visuales `overlay-studio-visual/*.png` regenerados (Crystal/Original, 4 widgets).
- Docs vivos: `AGENTS.md`, `docs/agent-workflow.md`, `docs/current-plan.md`.
- Configs locales tocadas: `configs/calendar-lmu.json`, `configs/license-cache.json`.
- Nuevos sin trackear: `VISUAL_DESIGN_RESEARCH.md`, `supabase/` (funciones `testing-center-dossier` y `testing-center-rejection`), `../vantare-workshop/`.
