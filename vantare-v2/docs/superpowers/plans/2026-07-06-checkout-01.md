# CHECKOUT-01 — Checkout real en PaywallScreen

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar el placeholder "Pago en línea próximamente" del `PaywallScreen` por un checkout real de Stripe que abra el navegador externo y regrese a la app con entitlements.

**Architecture:** El `PaywallScreen` llama a un endpoint de la Edge Function (`POST /functions/v1/create-checkout-session`) que crea una `Stripe.Checkout.Session` con el price ID correspondiente al plan elegido, y devuelve la URL. El frontend abre la URL con `Browser.OpenURL` (mismo patrón que OAuth). El webhook ya maneja `checkout.session.completed` y escribe entitlements. Al volver a la app, `LicenseProvider` revalida automáticamente y `useAccess` desbloquea Pro/Suite.

**Tech Stack:** React 19, TypeScript, Wails runtime (`Browser.OpenURL`), Supabase Edge Function (nuevo handler en `stripe-webhook`), Stripe API.

> **⚠️ CORRECCIONES APLICADAS (revisión 2026-07-06):**
> - **F2 (redirect→JSON):** el handler NO debe devolver `303 redirect`. El frontend hace `res.json()` para leer la URL, así que el handler devuelve `200` + `{ url }`. El test del handler se corrige a esperar `200` y `json().url`.
> - **B (URL de retorno):** `success_url`/`cancel_url` usan el servidor local ya existente (`http://127.0.0.1:39261/checkout/callback`), no `vantare://hub` (la app no registra ese esquema).
> - **F5 (orden de ruta):** `/create-checkout-session` se inserta ANTES de la verificación de firma Stripe en `handleRequest` (el frontend llama sin firma).
> - **C (Customer Portal):** se añade un endpoint EF `create-portal-session` + botón "Gestionar suscripción" en `AccountSettings.tsx` (Task 3).
> - Orden respecto a I18N-03b: I18N-03b se ejecuta ANTES (F1) y traduce los strings de `PaywallScreen`; este plan respeta esos `t()`.
---

## Contexto

- `PaywallScreen.tsx:29-34` tiene `handleSubscribe` que solo setea `pendingPlan` y muestra "próximamente". Es el punto de cambio único documentado.
- La Edge Function (`supabase/functions/stripe-webhook/index.ts`) ya tiene `STRIPE_SECRET_KEY` como secreto. Puede añadirse un handler para crear `Checkout.Session`.
- `STRIPE-01` creó los price IDs reales; `DEPLOY-01` deployó la EF.
- `Browser.OpenURL(url)` abre el navegador del sistema (ya usado en `LoginScreen.handleOAuth`).
- `LicenseProvider` emite `license:validate` al montar; tras checkout exitoso (webhook → entitlements), la revalidación desbloquea el plan.

## File Structure

| Action | File | Responsibility |
|--------|------|----------------|
| Modify | `supabase/functions/stripe-webhook/index.ts` | Añadir handler `POST /create-checkout-session` |
| Create | `supabase/functions/stripe-webhook/_utils/checkout.ts` | Helper de creación de sesión |
| Modify | `supabase/functions/stripe-webhook/index.test.ts` | Tests del nuevo handler |
| Modify | `frontend/src/hub/auth/PaywallScreen.tsx` | Reemplazar handleSubscribe |
| Modify | `frontend/src/hub/auth/PaywallScreen.test.tsx` | Tests de checkout |
| Modify | `docs/release-02-licensing-auth-stage.md` | Marcar `CHECKOUT-01` ✅ al cerrar |
> **Nota de retorno (decisión B):** el `success_url` apunta al servidor local de la app en `127.0.0.1:39261`. La app ya escucha ahí para el callback de OAuth. Para el checkout, Stripe redirige al navegador a esa URL; un handler en el servidor local debe reconocer `/checkout/callback` y avisar a la app (mismo patrón que OAuth) para que revalide la licencia. Si el servidor local aún no maneja `/checkout/callback`, es un trabajo menor de backend que se anota aquí: añadir esa ruta al servidor HTTP local que ya existe para OAuth.

**Forbidden files:** backend Go, layout/overlay runtime, launcher, calendar.

---

### Task 1: Añadir handler de create-checkout-session a la Edge Function

**Files:**
- Modify: `supabase/functions/stripe-webhook/index.ts`
- Create: `supabase/functions/stripe-webhook/_utils/checkout.ts`

Deno.test("create-checkout-session returns stripe checkout url as JSON", async () => {
  const ctx = createTestContext();
  const request = new Request("http://localhost/functions/v1/create-checkout-session", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      priceKey: "overlays",
      userId: "u1",
      email: "u1@example.com",
      successUrl: "http://127.0.0.1:39261/checkout/callback",
      cancelUrl: "http://127.0.0.1:39261/checkout/callback",
    }),
  });
  const response = await handleCreateCheckoutSession(ctx, request);
  assertEquals(response.status, 200); // JSON, no redirect
  const body = await response.json();
  assert(body.url.includes("https://checkout.stripe.com/"));
});
Deno.test("create-checkout-session rejects invalid priceKey", async () => {
  const ctx = createTestContext();
  const request = new Request("http://localhost/functions/v1/create-checkout-session", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      priceKey: "nonexistent",
      userId: "u1",
    }),
  });
  const response = await handleCreateCheckoutSession(ctx, request);
  assertEquals(response.status, 400);
});
```

- [ ] **Step 2: Run test (RED)**

```bash
cd supabase/functions/stripe-webhook
deno test --allow-env
```
Expected: FAIL — `handleCreateCheckoutSession is not exported` / module not found.

- [ ] **Step 3: Implementar `_utils/checkout.ts`**

```typescript
import Stripe from "stripe";

function getStripeClient(): Stripe {
  const key = Deno.env.get("STRIPE_SECRET_KEY") ?? "";
  if (!key) throw new Error("STRIPE_SECRET_KEY is required");
  return new Stripe(key, { apiVersion: "2025-02-24.acacia" });
}

interface CheckoutRequest {
  priceKey: string;
  userId: string;
  email?: string;
  successUrl?: string;
  cancelUrl?: string;
}

export async function handleCreateCheckoutSession(
  _ctx: unknown,
  request: Request,
): Promise<Response> {
  const body: CheckoutRequest = await request.json();
  const mapping = loadPriceMapping();

  // Buscar price_id(s) para el product_key solicitado
  const priceIds: string[] = [];
  for (const [priceId, keys] of Object.entries(mapping)) {
    if (keys.includes(body.priceKey) || (body.priceKey === "suite" && keys.includes("overlays") && keys.includes("engineer"))) {
      // Para "suite", seleccionamos el price_id que mapea a ["overlays","engineer"]
      if (body.priceKey === "suite" && keys.length >= 2 && keys.includes("overlays") && keys.includes("engineer")) {
        priceIds.push(priceId);
        break;
      }
      if (body.priceKey !== "suite" && keys.includes(body.priceKey)) {
        priceIds.push(priceId);
        break;
      }
    }
  }

  if (priceIds.length === 0) {
    return new Response(JSON.stringify({ error: `No price found for key: ${body.priceKey}` }), { status: 400 });
  }

  const successUrl = body.successUrl ?? "http://127.0.0.1:39261/checkout/callback";
  const cancelUrl = body.cancelUrl ?? "http://127.0.0.1:39261/checkout/callback";

  const stripe = getStripeClient();
  const session = await stripe.checkout.sessions.create({
    line_items: [{ price: priceIds[0], quantity: 1 }],
    mode: "subscription",
    success_url: successUrl,
    cancel_url: cancelUrl,
    client_reference_id: body.userId,
    customer_email: body.email,
  });

  if (!session.url) {
    return new Response(JSON.stringify({ error: "No checkout URL returned" }), { status: 500 });
  }

  // Devuelve JSON { url }, NO un redirect. El frontend lee res.json().
  return new Response(JSON.stringify({ url: session.url }), { status: 200 });
}

function loadPriceMapping(): Record<string, string[]> {
  const raw = Deno.env.get("PRICE_ID_TO_PRODUCT_KEYS");
  if (!raw) return {};
  try {
En `index.ts`, añadir al principio de `handleRequest`, ANTES de la verificación de firma Stripe (Issue 3b):

```typescript
// ⬇️ AÑADIR ANTES de la verificación de stripe-signature (el frontend llama sin firma)
if (url.pathname === "/create-checkout-session" && req.method === "POST") {
  return await handleCreateCheckoutSession(ctx, req);
}

if (url.pathname === "/create-portal-session" && req.method === "POST") {
  return await handleCreatePortalSession(ctx, req);
}
```

Importar ambos handlers desde sus `_utils`.
Importar `handleCreateCheckoutSession` desde `_utils/checkout.ts` y `handleCreatePortalSession` desde `_utils/portal.ts`.

- [ ] **Step 5: Run tests (GREEN)**

```bash
cd supabase/functions/stripe-webhook
deno test --allow-env
```
Expected: todos PASS (incluye los tests existentes de webhook + los 2 nuevos de checkout).

- [ ] **Step 6: Commit**

```bash
git add supabase/functions/stripe-webhook/
git commit -m "feat(stripe): add create-checkout-session handler to Edge Function (CHECKOUT-01)"
```
### Task 3: Customer Portal — `create-portal-session` + botón en Cuenta

**Files:**
- Create: `supabase/functions/stripe-webhook/_utils/portal.ts`
- Modify: `frontend/src/hub/settings/AccountSettings.tsx` (añadir botón "Gestionar suscripción")
- Modify: `frontend/src/hub/settings/AccountSettings.test.tsx`

**Contexto:** Stripe Customer Portal permite al usuario cancelar / cambiar método de pago / ver facturas sin que la app toque dinero. La auditoría §5 exige habilitarlo. Se crea una sesión de portal en la EF (no en el cliente) y la app abre la URL con `Browser.OpenURL`.

- [ ] **Step 1: `_utils/portal.ts`**

```typescript
import Stripe from "stripe";

function getStripeClient(): Stripe {
  const key = Deno.env.get("STRIPE_SECRET_KEY") ?? "";
  if (!key) throw new Error("STRIPE_SECRET_KEY is required");
  return new Stripe(key, { apiVersion: "2025-02-24.acacia" });
}

interface PortalRequest {
  stripeCustomerId: string;
  returnUrl?: string;
}

export async function handleCreatePortalSession(
  _ctx: unknown,
  request: Request,
): Promise<Response> {
  const body: PortalRequest = await request.json();
  if (!body.stripeCustomerId) {
    return new Response(JSON.stringify({ error: "stripeCustomerId required" }), { status: 400 });
  }
  const stripe = getStripeClient();
  const session = await stripe.billingPortal.sessions.create({
    customer: body.stripeCustomerId,
    return_url: body.returnUrl ?? "http://127.0.0.1:39261/checkout/callback",
  });
  if (!session.url) {
    return new Response(JSON.stringify({ error: "No portal URL returned" }), { status: 500 });
  }
  return new Response(JSON.stringify({ url: session.url }), { status: 200 });
}
```

> El frontend pasa el `stripeCustomerId` del usuario, que vive en `public.stripe_customers` (la EF lo escribe en `checkout.session.completed`). El frontend necesita leerlo.

- [ ] **Step 2: Botón en AccountSettings**

Añadir un botón "Gestionar suscripción" que:
1. Lee el `stripeCustomerId` del usuario.
2. `fetch` a `/functions/v1/create-portal-session` con `{ stripeCustomerId }`.
3. `Browser.OpenURL(data.url)`.

```tsx
<button type="button" onClick={handleManageSubscription} className="...">
  Gestionar suscripción
</button>
```

- [ ] **Step 3: Contrato pendiente (decisión al ejecutar)**

El `get_account_entitlements` del Go (y su `AccountInfo`) NO devuelve `stripe_customer_id`. Para el botón de portal el frontend lo necesita. Dos opciones:
- **(a)** Añadir `stripe_customer_id` al RETURNS TABLE de `get_account_entitlements` (SQL-01 se amplía); el Go ignora el campo extra en el JSON. **(recomendada)**
- **(b)** Nuevo RPC `get_stripe_customer_id(user_id)` con RLS.

- [ ] **Step 4: Tests + commit**

```bash
cd supabase/functions/stripe-webhook && deno test --allow-env
corepack pnpm --dir frontend test -- AccountSettings
git add supabase/functions/stripe-webhook/_utils/portal.ts frontend/src/hub/settings/AccountSettings.tsx frontend/src/hub/settings/AccountSettings.test.tsx
git commit -m "feat(paywall): add Stripe Customer Portal button (CHECKOUT-01)"
```

---

### Task 2: Cablear el checkout en PaywallScreen

**Files:**
- Modify: `frontend/src/hub/auth/PaywallScreen.tsx`
- Modify: `frontend/src/hub/auth/PaywallScreen.test.tsx`

- [ ] **Step 1: Escribir test (RED)**

En `PaywallScreen.test.tsx`:

```typescript
import { Browser } from "@wailsio/runtime";

vi.mock("@wailsio/runtime", () => ({ Browser: { OpenURL: vi.fn() } }));

it("opens external browser on Suscribirse click with checkout URL", async () => {
  global.fetch = vi.fn().mockResolvedValueOnce({
    ok: true,
    json: () => Promise.resolve({ url: "https://checkout.stripe.com/c/pay/cs_test_xxx" }),
  });

  render(<PaywallScreen email="u@example.com" />);
  fireEvent.click(screen.getAllByRole("button", { name: /suscribirse/i })[0]);

  await waitFor(() => {
    expect(global.fetch).toHaveBeenCalledWith(
      expect.stringContaining("/create-checkout-session"),
      expect.objectContaining({
        method: "POST",
        body: expect.stringContaining("overlays"),
      }),
    );
    expect(Browser.OpenURL).toHaveBeenCalledWith("https://checkout.stripe.com/c/pay/cs_test_xxx");
  });
});

it("shows error when checkout session creation fails", async () => {
  global.fetch = vi.fn().mockResolvedValueOnce({
    ok: false,
    json: () => Promise.resolve({ error: "No price found" }),
  });

  render(<PaywallScreen email="u@example.com" />);
  fireEvent.click(screen.getAllByRole("button", { name: /suscribirse/i })[0]);

  await waitFor(() => {
    expect(screen.getByText(/no price found/i)).toBeTruthy();
  });
});

it("removes coming-soon banner (checkout is real)", () => {
  render(<PaywallScreen email="u@example.com" />);
  // El banner "Pago en línea próximamente" solo debe aparecer si pendingPlan !== null
  // y no hay error de checkout. Con checkout real, pendingPlan ahora dispara fetch.
  expect(screen.queryByTestId("paywall-coming-soon")).toBeNull();
});
```

- [ ] **Step 2: Run test (RED)**

```powershell
corepack pnpm --dir frontend test -- PaywallScreen
```
Expected: FAIL — todavía usa el handler viejo con `pendingPlan` y `paywall-coming-soon`.

- [ ] **Step 3: Reescribir handleSubscribe**

En `PaywallScreen.tsx`, reemplazar:

```typescript
const handleSubscribe = useCallback((planKey: string) => {
  setPendingPlan(planKey);
}, []);
```

Por:

```typescript
const handleSubscribe = useCallback(async (planKey: string) => {
  setPendingPlan(planKey);
  setErrorPlan(null);
  try {
    const supabaseUrl = (import.meta.env.VITE_SUPABASE_URL as string | undefined) ?? "";
    const efUrl = `${supabaseUrl}/functions/v1/create-checkout-session`;
    const res = await fetch(efUrl, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        priceKey: planKey,
        userId: result?.userId ?? "",
        email,
        successUrl: "http://127.0.0.1:39261/checkout/callback",
        cancelUrl: "http://127.0.0.1:39261/checkout/callback",
      }),
    });
    if (!res.ok) {
      const err = await res.json().catch(() => ({}));
      setCheckoutError(err.error ?? "Error al crear la sesión de pago.");
      return;
    }
    const data = await res.json();
    if (data.url) {
      setPendingPlan(null);
      await Browser.OpenURL(data.url);
    }
  } catch {
    setCheckoutError("Error de conexión. Intenta de nuevo.");
  }
}, [email, result?.userId, setPendingPlan]);
```

Añadir estados: `const [errorPlan, setCheckoutError] = useState<string | null>(null);`.

Eliminar/reemplazar el bloque `{pendingPlan ? (<div data-testid="paywall-coming-soon">...</div>) : null}` por un bloque de error y estado de loading:

```tsx
{errorPlan && (
  <div data-testid="paywall-error" className="...">
    {errorPlan}
    <button onClick={() => setErrorPlan(null)}>Reintentar</button>
  </div>
)}
{pendingPlan && !errorPlan && (
  <div data-testid="paywall-loading" className="...">
    Redirigiendo a Stripe...
  </div>
)}
```

- [ ] **Step 4: Run test (GREEN)**

```powershell
corepack pnpm --dir frontend test -- PaywallScreen
```
Expected: PASS. Los tests de checkout pasan; `paywall-coming-soon` ya no existe.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hub/auth/PaywallScreen.tsx frontend/src/hub/auth/PaywallScreen.test.tsx
git commit -m "feat(paywall): wire real Stripe Checkout via Edge Function (CHECKOUT-01)"
```

---
> **Nota de dependencia:** Task 3 requiere que SQL-01 añada `stripe_customer_id` a `get_account_entitlements` (ver Task 3 Step 3). Si SQL-01 ya corrió sin ese campo, es un ALTER/RPC mínimo posterior.

### Task 3: Verificación y cierre

- [ ] **Step 1: Full suite + tsc**

```powershell
corepack pnpm --dir frontend test
corepack pnpm --dir frontend exec tsc -b
```
Expected: PASS.

- [ ] **Step 2: Smoke manual con Stripe test mode**

Con el proyecto Supabase linkado y la EF deployada:
1. Abrir la app como usuario Free.
2. Ir a Paywall (estado `authenticated-no-entitlement`).
3. Clic "Suscribirse" en plan "Overlays".
4. Verificar que el navegador externo abre `https://checkout.stripe.com/c/pay/...`.
5. Completar el pago con tarjeta de test `4242 4242 4242 4242`.
6. Volver a la app → `license:validate` revalida → Hub se desbloquea a Pro.
7. En Supabase SQL Editor, confirmar `user_entitlements` tiene fila `overlays` con status `active`.

- [ ] **Step 3: Marcar cierre**

`docs/release-02-licensing-auth-stage.md`: `CHECKOUT-01` ⚪ PENDIENTE → ✅ HECHO.

- [ ] **Step 4: Commit stage doc**

```bash
git add docs/release-02-licensing-auth-stage.md
git commit -m "chore: close CHECKOUT-01"
```

---

## Self-Review

- [x] Spec coverage: creación de Checkout Session en la EF, apertura externa en Paywall, manejo de error, eliminación del banner "próximamente".
- [x] Placeholder scan: el price ID se obtiene del mapping de `STRIPE-01`; sin TBD.
- [x] Type consistency: `priceKey` del frontend coincide con los keys del mapping JSON.

Plan completo. Requiere `DEPLOY-01` completado, `AUTH-04` (para que el usuario tenga userId), y `STRIPE-01` (mapping de precios).