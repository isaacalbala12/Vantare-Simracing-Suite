import type{WidgetRendererProps}from"../../../core/design-system-definition";import type{CarDamageNumbersViewModel}from"../../../widget-types/car-damage-numbers/car-damage-numbers-view-model";const p=(v:number|undefined)=>v===undefined?"—":`${Math.round(v*100)}%`;export function CarDamageNumbersCrystal({model}:WidgetRendererProps<CarDamageNumbersViewModel>){return <section data-widget-system="vantare-crystal" data-widget-renderer="car-damage-numbers" data-status={model.status} className="vc-damage-numbers"><header>DAMAGE</header><div><span>AERO</span><b>{p(model.aero)}</b></div><div><span>BODY</span><b>{p(model.body)}</b></div><div><span>SUSP</span><b>{p(model.suspension)}</b></div>{model.showTyres?<footer>{model.tyres?.map((value,index)=><i key={index}>{p(value)}</i>)??"—"}</footer>:null}</section>;}
