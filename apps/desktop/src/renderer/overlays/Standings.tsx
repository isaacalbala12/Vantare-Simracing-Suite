import React from 'react';

/**
 * Standings overlay — full implementation in Task 7.
 * This is a placeholder stub until Wave 2.
 */
export default function StandingsPlaceholder() {
  return <div data-testid="standings-overlay">Standings overlay placeholder</div>;
}
