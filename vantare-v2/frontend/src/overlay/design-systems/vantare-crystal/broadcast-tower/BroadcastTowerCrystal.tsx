import type { WidgetRendererProps } from "../../../core/design-system-definition";
import type { BroadcastTowerViewModel } from "../../../widget-types/broadcast-tower/broadcast-tower-view-model";
export function BroadcastTowerCrystal({ model }: WidgetRendererProps<BroadcastTowerViewModel>) { return <section data-widget-system="vantare-crystal" data-widget-renderer="broadcast-tower" data-status={model.status} className="vc-broadcast-tower"><header><strong>VANTARE <span>BROADCAST</span></strong><small>{model.sessionLabel} · {model.lap === undefined ? "LAP —" : `LAP ${model.lap}`}</small></header><div className="vc-broadcast-tower-rows">{model.rows.map((row) => <div className={row.isPlayer ? "is-player" : ""} key={`${row.place}-${row.number}`}><b>{row.place}</b><b>{row.number}</b><span>{row.name}</span><small>{row.team}</small><em>{row.gap === undefined ? "—" : row.gap.toFixed(3)}</em></div>)}</div>{model.showWeather || model.showSof ? <footer>{model.showWeather ? "TRACK TEMP —" : null}{model.showWeather && model.showSof ? " · " : null}{model.showSof ? "SOF —" : null}</footer> : null}</section>; }
